# Knowledge Base Index

Evergreen documentation for Pyrycode. Updated as things change, not appended to.

Search with QMD: `mcp__qmd__query(collection: "pyrycode-docs", query: "your query")`

## Architecture

| File | Topic |
|------|-------|
| [system-overview.md](architecture/system-overview.md) | Module structure, data flows, platform support |

## Decisions

| # | File | Decision |
|---|------|----------|
| 001 | [001-go-language.md](decisions/001-go-language.md) | Go as the implementation language |
| 002 | [002-pty-supervisor.md](decisions/002-pty-supervisor.md) | PTY-level wrapping over alternatives |
| 003 | [003-session-addressable-runtime.md](decisions/003-session-addressable-runtime.md) | `internal/sessions` Pool wraps the supervisor for additive Phase 1.1+ |
| 004 | [004-fsnotify-for-rotation-detection.md](decisions/004-fsnotify-for-rotation-detection.md) | `fsnotify` for live `/clear` detection (over polling or raw inotify+kqueue) |
| 005 | [005-idle-eviction-state-machine.md](decisions/005-idle-eviction-state-machine.md) | Per-session two-state machine + explicit `Activate` for idle eviction / lazy respawn |
| 006 | [006-concurrent-active-cap-lru.md](decisions/006-concurrent-active-cap-lru.md) | `Config.ActiveCap` + LRU victim selection at `Pool.Activate`; force-eviction `Session.Evict` primitive |
| 007 | [007-bridge-iteration-boundaries.md](decisions/007-bridge-iteration-boundaries.md) | `Bridge` input path moves from `io.Pipe` to `chan []byte` + per-iteration cancel (`BeginIteration` / `EndIteration`) so the input pump terminates per `runOnce` iteration instead of leaking and racing the next one for typed bytes during a restart |
| 008 | [008-bridge-resize-seam.md](decisions/008-bridge-resize-seam.md) | `Bridge.Resize(rows, cols)` + `SetPTY(*os.File)` is the supervisor-side resize seam (over a method on `*Supervisor` or a `Run`-wired callback); reuses the `Session → Bridge` chain and `BeginIteration`/`EndIteration` hook points |
| 009 | [009-resize-wire-shape.md](decisions/009-resize-wire-shape.md) | Live SIGWINCH propagation rides a side-channel `VerbResize` on a fresh control connection (over a framed escape inside the attach byte stream); raw byte purity preserved, AC#2 satisfied structurally |
| 010 | [010-sessions-cli-sub-router.md](decisions/010-sessions-cli-sub-router.md) | `pyry sessions <verb>` dispatches via a `runSessions` sub-router that peels global pyry flags via `parseClientFlags` then dispatches on the first positional; each sub-verb owns its own `flag.NewFlagSet`; `sessionsVerbList` constant over a derived list |
| 011 | [011-cli-prefix-resolution.md](decisions/011-cli-prefix-resolution.md) | UUID-or-prefix resolution for `sessions.*` CLI verbs is client-side via `control.SessionsList` (mirroring `Pool.ResolveID`'s exact-then-prefix order) over extending the wire with a prefix-aware `sessions.rm` or a dedicated `sessions.resolve` verb |
| 012 | [012-attach-stdio-flag-vs-verb.md](decisions/012-attach-stdio-flag-vs-verb.md) | `pyry attach --stdio` is a flag on the existing verb (over a separate `attach-stdio` verb or auto-detect via `term.IsTerminal`); reuses `VerbAttach` with `Cols=0/Rows=0` riding `omitempty` off the wire — zero server change |
| 013 | [013-evict-activate-persist-ordering.md](decisions/013-evict-activate-persist-ordering.md) | `transitionTo` closes the per-direction wake channel AFTER `pool.persist` returns (over moving persist into `lcMu` — deadlocks via `saveLocked` — or serialising transitions through a goroutine — overkill since `Run` is the sole caller); `Activate`/`Evict` drop their already-in-target-state early-return and always wait on the channel so disk and memory agree at every return point |
| 014 | [014-get-or-create-take-or-create.md](decisions/014-get-or-create-take-or-create.md) | `Pool.GetOrCreate` is take-or-create rather than insert-or-error (no `CreateWithID`); validates UUIDv4 shape at the Pool boundary via `ValidID`; holds `p.mu` across register + persist + skip-set + `g.Go` so concurrent same-UUID callers cannot observe a registered entry before its lifecycle goroutine is parked on `activateCh` |
| 015 | [015-update-restart-probe-inline.md](decisions/015-update-restart-probe-inline.md) | `pyry update` daemon-restart probe (`os.Stat` of launchd plist / systemd user unit) lives inline in `cmd/pyry/update.go`, keeping `internal/update/restart.go` a pure decision function with no filesystem calls; restart executor is an injectable function-typed seam on `updateOptions` (mirrors `replace` / `executablePath` pattern) so the integration test asserts argv without exec'ing real `launchctl` / `systemctl` |
| 016 | [016-bootstrap-ignores-persisted-lifecycle-state.md](decisions/016-bootstrap-ignores-persisted-lifecycle-state.md) | Bootstrap session ignores persisted `lifecycle_state` on warm-start: `Pool.New`'s `pickBootstrap` arm forces `lcState = stateActive` regardless of disk because nothing under non-TTY stdin (launchd / systemd / piped wrapper) calls `Pool.Activate` on the bootstrap, so a persisted `evicted` parks `runEvicted` on `activateCh` forever (#202). Non-bootstrap sessions retain persisted state — lazy respawn on attach is correct there |
| 017 | [017-install-script-smoke-check.md](decisions/017-install-script-smoke-check.md) | `install.sh` post-install smoke check is bash + sentinel-grep against `Uptime: 2562047h47m16.854775807s` (over a Go subcommand or daemon-side watchdog); gates on currently-running default-named services only (`launchctl print | grep 'state = running'` / `systemctl --user is-active --quiet pyry`) so deliberately-disabled units aren't auto-started; four exit codes (0 healthy / 2 sentinel / 3 dial-fail / 4 restart-fail) preserve the diagnostic distinction AC #4 names; no manual sleep before the probe — `pyry status`'s built-in dial retry (#199) + 5s deadline IS the grace period |
| 018 | [018-config-overlay-decode.md](decisions/018-config-overlay-decode.md) | `internal/config.Load` initializes `cfg := DefaultConfig()` before `json.Unmarshal(data, &cfg)` so absent JSON fields keep their default values (over a two-pass `map[string]json.RawMessage` merge or pointer-fields distinguishing absent-vs-empty); malformed JSON wraps and surfaces (no silent fallback); error path returns `Config{}` not `DefaultConfig()` so callers that ignore `err` break loudly |
| 019 | [019-identity-loader-co-located.md](decisions/019-identity-loader-co-located.md) | `LoadOrCreate` lives in `internal/identity/store.go` (relaxed package doc-comment) over a sibling `internal/identitystore`; loader has no API surface beyond the type, future identifier-loaders (e.g. `devices.json`) compose by adding files rather than packages |
| 020 | [020-devices-registry-snapshot-then-write.md](decisions/020-devices-registry-snapshot-then-write.md) | `internal/devices.Registry.Save` snapshots the slice under `mu`, releases, then sorts + atomic-writes outside the lock (over `internal/sessions`'s hold-across-I/O); auth-path readers (`List`, `FindByTokenHash`, called per WS handshake) stay unblocked while pairing writes; `Device`'s all-value-type fields make shallow snapshot strictly safe |
| 021 | [021-pair-cli-order-of-operations.md](decisions/021-pair-cli-order-of-operations.md) | `pyry pair` runs all loads (config / devices / server-id) + relay resolution before `crypto/rand`, and `Registry.Save` before `pair.Render`; converts a silent-at-pair / fatal-at-connect failure (Render before Save) into a loud-at-pair / recoverable failure, and contracts the plaintext-token's lifetime to the shortest closed interval consistent with rendering it |
| 022 | [022-conversations-update-callback-under-lock.md](decisions/022-conversations-update-callback-under-lock.md) | `internal/conversations.Registry.Update` runs the caller's `func(*Conversation)` callback while holding the registry mutex (over snapshot-mutate-swap or CAS-loop alternatives); pointer-to-slice-element mutation pattern, no-callback-back-into-registry contract documented; AC-bound `bool`-only return — post-mutation snapshot read happens inside `fn` if needed |
| 023 | [023-activate-waits-pty-readiness.md](decisions/023-activate-waits-pty-readiness.md) | `Session.Activate` strengthened to wait for supervisor PTY readiness (`Supervisor.WaitForPTY` backed by a freshening `ptmxReadyCh`) at the tail, over making `WriteUserTurn` wait internally (would break the `TurnWriter` shape) or restructuring `transitionTo` to defer the `activeCh` close (would entangle the `#202` bootstrap warm-start invariant); closes the ~hundreds-of-ms gap between `transitionTo` closing `activeCh` and `runOnce` binding the new PTY master, eliminating the silent-drop-on-`ptmx == nil` failure mode the relay-routed `send_message` path hit on idle-evicted bootstraps (#396) |
| 024 | [024-noise-ik-mobile-e2e.md](decisions/024-noise-ik-mobile-e2e.md) | Mobile wire protocol v2 adopts Noise_IK over `Noise_IK_25519_ChaChaPoly_BLAKE2s` for E2E encryption phone↔binary through the stateless relay (#430); AEAD-seals the entire inner mobile-protocol envelope (relay sees only `{conn_id, frame}` outer + opaque ciphertext); per-binary static keypair at `~/.pyry/<daemon-name>/static_key.json` (`0600` enforced at startup, parent dir `0700`, `<daemon-name>` allowlist-canonicalised, `O_NOFOLLOW` on read) shared across paired phones, per-phone device-static keypair in Android Keystore; 1-hour time-based re-key + explicit `rekey_request` envelope (re-key `noise_init` early-data is empty — WireGuard/Tailscale precedent); hard cutover from v1 (no soft fallback, pre-flight `pyry pair list`-empty gate filed as #436) — v1's reserved `payload_encrypted` flag becomes structurally redundant and is removed not renamed; `flynn/noise` for Go (pinned in #431), three Kotlin options surfaced for mobile team to pick |

## Features

| File | Topic |
|------|-------|
| [sessions-package.md](features/sessions-package.md) | `internal/sessions` — `SessionID`, `Session`, `Pool`; supervisor wrapper for multi-session readiness; `Pool.Run` errgroup also hosts the conversations auto-archive sweep goroutine when `Config.ConversationsRegistry` is non-nil (#243); `Config.SweepInterval` overrides the sweep cadence for testing (zero = `conversations.SweepInterval`, #262) |
| [sessions-registry.md](features/sessions-registry.md) | `~/.pyry/<name>/sessions.json` — schema, atomic write, load semantics; sessions survive `pyry stop` |
| [jsonl-reconciliation.md](features/jsonl-reconciliation.md) | Startup scan of `~/.claude/projects/<encoded-cwd>/<uuid>.jsonl`; `Pool.RotateID` self-heals registry across `/clear` |
| [rotation-watcher.md](features/rotation-watcher.md) | Live `/clear` detection: fsnotify on the claude dir + per-PID FD probe (Linux `/proc/<pid>/fd`, macOS `lsof`) drives `Pool.RotateID` |
| [control-plane.md](features/control-plane.md) | `internal/control` — Unix-socket JSON server, `SessionResolver` seam, verb dispatch, attach handoff |
| [idle-eviction.md](features/idle-eviction.md) | Per-session active↔evicted state machine; idle timer + concurrent-active-cap (LRU) eviction triggers; `Activate` / `Evict` primitives |
| [e2e-harness.md](features/e2e-harness.md) | `internal/e2e` (build tag `e2e` or `e2e_install`) — `Harness` + `Start(t)` / `StartIn(t, home, flags...)` spawn pyry in temp / caller-supplied HOME with optional variadic flags (last-wins), `cap_test.go` (#116) drives cap-eviction LRU + cap/idle interleave via `sessions.new` over the control socket against a tiny shell-script claude stand-in (`Pool.Create` appends `--session-id` to ClaudeArgs, breaking sleep), `StartExpectingFailureIn(t, home)` for failed-start assertions, `Harness.Run(t, verb, args...)` auto-injects socket, `RunBare(t, args...)` drives daemon-free verbs, `Harness.Stop(t)` graceful mid-test teardown (idempotent with `t.Cleanup`); sibling `AttachHarness` + `StartAttach(t, sessionID)` (#125) wraps `pyry attach` in a `creack/pty` master/slave pair with `TestHelperProcess`-echo as supervised claude for interactive PTY round-trip tests; sibling `StdioAttachClient` + `startStdioAttach(t, label)` (#161) wraps `pyry attach --stdio` in plain `os.Pipe()` ends (no PTY anywhere on the client side — the SDK consumer shape) over the same helper-as-echo daemon, with `Write` / `ReadUntil(needle, deadline)` / `Close(t) int` and clean-detach via `inputW.Close()` → child exit 0; the proof-of-life `TestE2E_AttachStdio_BytesRoundTrip` was `t.Skip`'d pending #167 (`pyry attach --stdio` rejected by `parseClientFlags`), now skipped on #257 since #167's fix exposed a separate `--session-id`-vs-test-binary harness bug; sibling `TestE2E_AttachStdio_NoPTYInProcessTree` (#162, also `t.Skip`'d on #257) inspects the attach child's open fds via `/proc/<pid>/fd/` (linux) / `lsof -p <pid> -Fn` (darwin) and asserts no `/dev/ptmx` / `/dev/pts/*` / `/dev/ttys*` / `/dev/tty` is held — binary-level defence against a refactor that pulls a PTY into `cmd/pyry/runAttach`'s `--stdio` branch; sibling `ForegroundAutoAttachClient` + `startForegroundAutoAttach(t, label)` (#163) drives `pyry --session-id <uuid> …` (no `attach` verb) over plain `os.Pipe()`s, exercising the 1.3c-2 foreground-binary auto-attach path through `runSupervisor` → `tryAutoAttach` → `control.AttachStdio` (bypasses #167 / #257 entirely — calls `AttachStdio` in-process, no `parseClientFlags`, no `Pool.Create` argv-append against the test binary since the shell-wrapper claude absorbs `--session-id`); pairs the byte round-trip with a `pgrepChildren(pid)` (`pgrep -P`, identical surface on linux + darwin) zero-direct-children assertion that catches a hypothetical bug wiring auto-attach correctly *and* spawning a competing supervisor; uses an `echo-claude.sh` shell wrapper as supervised claude to absorb `Pool.Create`'s `--session-id` append (Go test binary's `flag.Parse()` would otherwise reject it); `safeBuffer` (mutex-wrapped `bytes.Buffer`) for `cmd.Stderr` to dodge the os/exec stderr-copy goroutine race; sibling `ForegroundSupervisedClient` + `startForegroundSupervised(t, sessionID, extraEnv)` (#164) is the inverse-direction peer that pins `tryAutoAttach`'s three fall-through arms (no matching session, no daemon, `PYRY_NO_AUTO_ATTACH=1`) — foreground gets its own socket path distinct from any daemon, structurally falls through via stat ENOENT, and `pgrepChildren ≥ 1` + `SessionsList` snapshot equality assert "supervisor mode reached, daemon registry untouched"; gate-discrimination stays at unit tier (six tests in `cmd/pyry/auto_attach_test.go`), e2e tier pins system-level fall-through-reaches-supervisor invariant; `spawnDaemonWithRegisteredSession` wraps `spawnAutoAttachDaemon` + `SessionsNew` for the "daemon present" tests; sibling `StartRotation(t, home, sessionsDir, initialUUID, trigger)` (#123) wires the fake-claude binary as the supervised child via `spawnWith(spawnOpts)` core + three `PYRY_FAKE_CLAUDE_*` env vars for rotation-watcher e2e; `AttachHarness.WaitDetach` + `AttachHarness.Run` (#127, sharing `runVerb` with `Harness.Run`) drive the documented `Ctrl-B d` detach + post-detach `pyry status` for the triple-invariant clean-detach proof; covers `stop`/`logs`/`version`/`status` (running + stopped), three restart-survival proofs (active, evicted-state, `lastActiveAt`), corrupt-registry fail-loud, missing-`~/.claude/projects/` clean-startup, idle-eviction + lazy-respawn (raw `VerbAttach` over the control socket), bootstrap warm-start ignores persisted `lifecycle_state` (#253, two-phase mutate-and-restart pinning ADR 016's carve-out plus the non-bootstrap-evicted boundary; negative-greps the v0.10.1 zero-time + max-int64 uptime sentinels), conversations sweep loop ticks + clean SIGTERM shutdown (#263, `conv_sweep_test.go` seeds promoted+unpromoted 60-day-idle entries, drives the daemon at `-pyry-conv-sweep-interval=100ms`, polls `conversations.Load` for the unpromoted removal under a 5s budget, then asserts `processAlive(pid) == false` + panic/`runtime/`/`goroutine ` substring scan over stderr after `h.Stop` — closes the daemon-wiring gap that the in-package `pool_conv_sweep_test.go` cannot reach: a regression in `cmd/pyry/main.go`'s `sessions.Config` construction silently sets `p.convReg == nil` and the in-package test still passes), attach round-trip bytes, attach clean-detach, attach-survives-claude-restart (the last surfaced + fixed the bridge input-pump leak — see [ADR 007](decisions/007-bridge-iteration-boundaries.md)), and attach SIGWINCH propagation (#126, e2e cover for the #133/#137/#136 live-resize chain via a `pty.Setsize`-on-master + `winsize rows=N cols=M` helper marker) at the binary boundary; SIGTERM/SIGKILL teardown, leak verification via re-exec |
| [fakeclaude-binary.md](features/fakeclaude-binary.md) | `internal/e2e/internal/fakeclaude` — test-only `package main` that opens a `<uuid>.jsonl` and rotates it on a triggerable signal (rotation-watcher e2e primitive) |
| [fakephone-harness.md](features/fakephone-harness.md) | `internal/e2e/internal/fakephone` (#296, extended #308) — in-process WS client speaking the phone half of the mobile↔relay protocol; `Dial(ctx, baseURL, serverID, token, deviceName)` opens `/v1/client` with the three required upgrade headers verbatim; `Send(env protocol.Envelope)` writes one text frame, `Receive(timeout)` reads one and unmarshals (deadline → `ErrReceiveTimeout`), `Close()` is idempotent + unblocks in-flight Receive with `ErrClosed`; pre-check + post-I/O recheck of a `closed` bool maps Close-mid-call to `ErrClosed` rather than wrapped library errors; speaks raw envelopes only (routing wrap is the relay's job); `coder/websocket` matching #247 / #295; timed-out Receive cannot be retried on the same Client (library closes conn on Read-ctx-cancel — documented constraint, fresh Client per timeout-plausible attempt); no consumers wired yet (roundtrip e2e test lands in follow-up ticket); #308 adds `LastCloseStatus() (websocket.StatusCode, bool)` — `Receive`'s error path captures the peer-side close status via `websocket.CloseStatus(err)` (skips `-1` for non-CloseErrors) into `lastCloseStatus` under the existing mutex; first consumer is `TestRelay_AuthReject_4401` asserting the auth-reject `4401` close code. |
| [fakerelay-harness.md](features/fakerelay-harness.md) | `internal/e2e/internal/fakerelay` (#295, extended #301/#308/#371) — in-process WS server speaking the routing half of the mobile↔relay protocol; `New(logger) → Server` with `/v1/server` (first-claim-wins on `x-pyrycode-server`) and `/v1/client` (requires `server`/`token`/`device-name` headers, mints `c-N` `conn_id`s) endpoints; wraps phone→binary frames as `protocol.RoutingEnvelope` and unwraps binary→phone frames; pre-upgrade HTTP 400/409/503 substitutes the production 4409/4404 WS close codes (deliberate test-ergonomic deviation, documented in the package comment); cleanup-cascades-on-binary-loss drops orphaned phones so `Close()` leaves no goroutines behind under `-race`; `coder/websocket` matching #247; #301 adds binary-direct `hello`/`hello_ack` dispatch (envelopes with empty `conn_id` decode-twice as `protocol.Envelope`, dispatch by `Type` — only `TypeHello` handled today, captured under `LastBinaryHello(serverID)`, replied with routing-wrapped `HelloAckPayload{ProtocolVersion:"v1", ServerID, ConnID:"-"}`), `RejectNextBinaryWith4409()` one-shot opt-in for the production WS-close-4409 code (next `/v1/server` upgrade accepts then closes with `websocket.StatusCode(4409)`, flag clears after one use; coexists with the existing HTTP-409 first-claim-wins path), and `ForceCloseBinary(serverID) bool` (closes with `StatusInternalError` for transport-reconnect e2e). First production consumer: the daemon-side relay e2e tests at `internal/e2e/relay_test.go` (#301); #308 adds token-injection on the first phone→binary frame per `conn_id` (`phoneConn` grows `token`/`firstFrameSent`/`tokMu`; `phoneRecvPump` reads-and-clears the upgrade-time `x-pyrycode-token` under `tokMu` and embeds it in `RoutingEnvelope.Token` on that one frame) and `CloseCode` honor on the binary→phone path (`binaryRecvPump` queues a `phoneSend{frame, closeCode}` tuple onto `pc.sendCh`; `phoneSendPump` writes `frame` first so the phone observes the error envelope before close, then issues `pc.conn.Close(websocket.StatusCode(closeCode), "")` — serialising close through the same send pump eliminates the race a direct `conn.Close` at the `binaryRecvPump` site would have introduced against an in-flight write); production consumer `internal/e2e/relay_auth_test.go` (`TestRelay_AuthReject_4401`) drives the auth-reject path end-to-end. **#371 (race fix):** adds `WaitBinary(ctx, serverID) bool` alongside `LastBinaryHello` — blocks until `s.binaries[serverID]` is registered or ctx fires (single fast-path check under `s.mu`, then `time.NewTicker(2ms)` polling so cancellation is responsive). Closes the upgrade→registration race in fixture probes: `websocket.Accept` writes the 101 response and unblocks the test's `websocket.Dial` *before* `handleBinary` finishes `s.binaries[serverID] = bc` under `s.mu` (`fakerelay.go:231-260`), so a test calling `ForceCloseBinary` immediately after `dialBinary` could land in that window under `-race` on a contended CI worker and observe a not-yet-registered binary. `LastBinaryHello`-polling already covered e2e tests that send a hello (`relay_test.go:145-158`); `WaitBinary` is the equivalent for raw-dial unit tests. `TestForceCloseBinary` now calls `WaitBinary` between `dialBinary` and `ForceCloseBinary`; ghost-case assertion unchanged. `ForceCloseBinary` semantics deliberately not modified (wrapping it internally would slow the ghost case or require two variants); `handleBinary`'s Accept-then-register order deliberately not modified (pre-Accept registration would impose new ordering constraints on the routing dispatcher). |
| [install-e2e.md](features/install-e2e.md) | `internal/e2e/install_{linux,darwin}_test.go` (build tag `e2e_install`) — `pyry install-service` × `systemctl --user` (Linux) / `launchctl bootstrap gui/<uid>` (macOS) round-trip + bug-#19 PATH inheritance regression guard + cleanup-on-fatal re-exec verification on both platforms |
| [e2e-realclaude.md](features/e2e-realclaude.md) | `internal/e2e/realclaude` (#361, build tag `e2e_realclaude`) — sibling Go package to `internal/e2e`, opt-in via `make e2e-realclaude`, scaffold home for the real-`claude`-binary trust-boundary tests #362/#363 will populate; `smoke_test.go` asserts `exec.LookPath("claude")` succeeds and `claude --version` runs zero-exit under a 10 s `context.WithTimeout` (fatal-not-skip — suite was opted into); single-tag `//go:build e2e_realclaude` deliberately distinct from `e2e`/`e2e_install` so a future `make e2e` won't pull real-claude tests in; `make test` skips via tag exclusion alone (no path filter), `make check` unchanged, per-PR CI continues to run only `make check`; no `-race` on the make target (I/O-bound exec, not goroutine-stress). **CI cadence:** no GitHub Actions workflow — the suite runs locally during the code-review phase of every dispatched ticket via the pipeline (see the code-review agent's `CLAUDE.md` for the invocation contract). The earlier nightly workflow (`.github/workflows/e2e-realclaude-nightly.yml`, #362) was removed in #379 the same day it landed (Max-plan tokens used locally are free vs. a paid `ANTHROPIC_API_KEY` GitHub secret; failure surface synchronised to dispatch cadence beats unpredictable 04:00 UTC failures; one fewer CI file to keep in lockstep with `self-check-daily.yml`). `make e2e-realclaude` is unchanged — still the entry point, just no longer invoked by CI. **Fixture helpers (#372):** `fixtures.go` adds `WithWorktree(t) string` (`t.TempDir()` + `t.Setenv("HOME", dir)`; pins HOME for both the in-test process and any subprocess so `os.UserHomeDir()` resolves identically on both sides — `t.Setenv` not `os.Setenv` is load-bearing for parallel-safe cleanup; no `.claude/` pre-creation, the runtime owns it), `ReadJSONL(t, workdir, sessionID) []JSONLEntry` (delegates path computation to `agentrun.EncodeProjectDir` then loops `jsonl.NewReader(f, jsonl.Config{}).Next()` until `io.EOF`; empty file → empty slice; open/parse failures `t.Fatalf` with the resolved path embedded in the message; private `resolveAndOpenJSONL(workdir, sessionID) (*os.File, string, error)` split keeps the missing-file path unit-testable as a returned error rather than via a `*testing.T`-wrapper trick), and `type JSONLEntry = jsonl.Event` (type **alias**, not wrapper struct — preserves `entry.EndOfTurn` / `entry.Usage.OutputTokens` field access without forcing callers to import `internal/agentrun/jsonl` directly). Five tagged tests in `fixtures_test.go` cover happy path / empty file / missing file (via the private split) / HOME-restore-after-subtest / alias compile-check; **Subprocess invocation helper (#373):** `fixtures.go` adds `RunPyryAgentRun(t, RunOpts) RunResult` — synchronous invoker that builds `pyry` once per test process via `sync.Once` (`PYRY_E2E_BIN` short-circuit + temp-dir cache, structurally cloned from `internal/e2e/harness.go:118-141` because disjoint build tags `e2e` vs. `e2e_realclaude` block direct reuse), writes `<Workdir>/prompt.txt` + `system.txt` from `opts.Prompt`/`opts.SystemPrompt` (0o600), invokes `pyry agent-run --prompt-file=… --system-prompt-file=… --allowed-tools=<comma-joined> --max-turns=<n> --effort=<…> --model=<…> --workdir=<…> --output-format=stream-json` (eight flags in `--flag=value` form so the argv-contract test's substring matches are order-independent), captures stdout/stderr into `bytes.Buffer`s, and returns `RunResult{ExitCode, SessionID, Stdout, Stderr}` where `SessionID` comes from a `bufio.Scanner` over stdout stopping at the first decoded `{"type":"system","subtype":"init","session_id":"…"}` line with non-empty session_id (init-only by design — lands earliest in stdout so failing tests still see the id even when the run aborted before producing a `result` trailer; non-JSON and non-init lines skip silently; `parseInitSessionID` is inline ~14 LoC, no dependency on `internal/agentrun/streamjson` to keep the dependency direction one-way). **Non-zero subprocess exit is NOT a `t.Fatalf`** — callers assert on `RunResult.ExitCode` themselves so the real CLI's error paths can be tested; only structural failures (validation, build, exec start, `context.DeadlineExceeded`) call `t.Fatalf`, with the timeout branch embedding both stdout and stderr in the message so the operator sees what was running. `RunOpts` mirrors `cmd/pyry/agent_run.go:66-152`'s flag surface 1:1 (`Workdir`, `Prompt`, `SystemPrompt`, `AllowedTools`, `MaxTurns`, `Effort`, `Model`, `ExtraEnv`, `Timeout` defaulting to 5 min) — **no `SessionID` input** (pyry agent-run has no resume flag; session_id is an output, not an input), **no `Mode` enum** (post-#391 architecture has no second mode at this layer — `pyry agent-run` IS the stream-json forwarder). Private `validateRunOpts(opts) error` returns first-violation-wins on the seven required fields (`Workdir`/`Prompt`/`SystemPrompt`/`AllowedTools` non-empty/`MaxTurns > 0`/`Effort`/`Model`), exposed as a returned error so tests assert on it without intercepting `t.Fatalf`; the `--effort` enum is NOT re-validated locally — pyry rejects bad values and the helper surfaces that as a non-zero exit (one place to update). Test infrastructure: `TestMain` re-execs the test binary as a fake `pyry` when `GO_TEST_HELPER_PROCESS=1`, pins `PYRY_E2E_BIN=os.Args[0]` for every other test so `ensurePyryBuilt` short-circuits to the fake, and the fake selects behaviour from `PYRY_E2E_FAKE_MODE` (`happy` emits init + assistant + result trailer, `fail` exits 2 before any stream-json, `sleep` blocks 30s, `argv` echoes its argv to stderr). Seven tests cover: happy-path session_id + stdout asserts, `validateRunOpts` positive + negative table (Workdir/Prompt/SystemPrompt/AllowedTools/MaxTurns≤0/Effort/Model), init-event-absent (ExitCode 2, SessionID empty, no fatal), timeout (re-execs itself with `PYRY_REALCLAUDE_TIMEOUT_INNER=1` so the inner `t.Fatalf` is captured as a subprocess failure — cleaner than wrapping `*testing.T`), argv-contract (eight flags substring-matched in stderr), and a `parseInitSessionID` table (empty / init / empty-session_id / non-init-first / trailer-only / malformed-before-init). Production code lands at ~140 non-comment LoC vs. the spec's ~95 LoC budget — two exported structs (9+4 fields) + public helper + validator + build-cache clone + parser renders close to this footprint by construction; flagged at commit time, reviewer accepted. Composition pattern downstream tests use: `WithWorktree` → `RunPyryAgentRun` → `ReadJSONL`. **First consumer (#364):** `prompt_fidelity_test.go` adds `TestRealClaude_PromptFidelity` — runs `pyry agent-run` against the real `claude` CLI with a distinctive ASCII-only literal (`const distinctivePrompt = "INTEGRATION_TEST_PROMPT_PROMPTFIDELITY_2N7Q4R8W"`, JSON-safe by construction so substring matching is deterministic), then asserts the literal survives byte-for-byte into the first `user`-kinded entry of claude's JSONL session via `bytes.Contains(event.Raw, []byte(literal))`. **`Event.Raw` substring-match instead of decoding `message.content`** — `jsonl.Event` only decodes assistant-shaped fields, and user lines carry `content` as either a string OR `[{type:"text",text:…}]` (see `internal/agentrun/jsonl/reader.go:137` comment); matching the verbatim line bytes covers both shapes without owning a second decoder. Run-level asserts (`ExitCode == 0`, `SessionID != ""`) come BEFORE the JSONL assert so setup/auth/network failures surface as themselves rather than as confusing "no user entry" messages; per-shape failure-message dump policy is **kinds for "missing", body for "found-but-wrong"** (no event bodies in the missing-kind dump — kind tags alone redirect debugging without bulk noise or sensitive bodies). Private `jsonlPathFor(workdir, sessionID)` recomputes the resolved JSONL path for failure messages via `os.UserHomeDir()` + `agentrun.EncodeProjectDir(workdir)` and returns a `<unresolved: …>` sentinel on error rather than escalating diagnostic-helper failures into test failures. `RunOpts` uses `--max-turns=1`, `--effort=low`, `--model=claude-haiku-4-5` (literal model name, matching `fixtures_test.go:163`, not the `haiku` short alias) to minimise per-run cost (~$0.01); `SystemPrompt` discourages prompt echo, `AllowedTools: []string{"Read"}` is the minimum non-empty value satisfying `validateRunOpts`. Pins the streamrunner envelope's text round-trip end-to-end — would catch any future preprocessing, escaping drift, or wiring change that bypasses streamrunner (the original `/doctor` poisoning class, made structurally impossible by #391). One real haiku call per `make e2e-realclaude` run; `make test` still excludes via build tag. No new exports, no helper additions — entire test body is a fixture composition. **Second consumer (#365):** `allowed_tools_enforcement_test.go` adds `TestRealClaude_AllowedToolsEnforcement` — runs `pyry agent-run --allowed-tools Read` against the real `claude` CLI with an explicit Bash-attractive prompt (`"List the files in the current working directory. Use the Bash tool to run \`ls -la\`."`) and asserts no assistant entry in the resulting JSONL emits a `tool_use` content block with `name == "Bash"`. Pins the **runtime behavior** of the agent-run argv shape that `cmd/pyry/agent_run_test.go:484` already pins statically (no `--settings`, no `--permission-mode`, no `--session-id`): with `--dangerously-skip-permissions --allowed-tools Read`, the real claude binary refuses Bash. File-local 12-LoC detector `bashInvokedInRaw` mirrors `internal/agentrun/selfcheck/selfcheck.go:284-302` exactly (exact-case `Type=="tool_use" && Name=="Bash"` match, decode-error → skip silently per selfcheck.go:283) — **duplicated rather than imported** to keep the dependency direction one-way (e2e tests don't depend on the daemon's audit package); the lockstep coupling is named in a one-line comment so a future `tool_use` → `tool_invocation` rename moves both fixtures together. Complements `internal/agentrun/selfcheck` (#336), which probes the boot-time `--settings`/`defaultMode=deny` path; this probes the spawned agent-run `--allowed-tools` + `--dangerously-skip-permissions` path. **Obsolete-matrix history:** originally framed as a `defaultMode ∈ {deny, default, dontAsk}` matrix; under post-#391 architecture the per-spawn settings file is gone and `--allowed-tools` is the sole enforcement configuration on the agent-run path, so the matrix collapsed to one row (named in a header comment so a future reader who finds the original framing in git history understands why). Prompt is intentionally explicit ("Use the Bash tool to run …") and the `SystemPrompt` is minimal/neutral with no "don't use Bash" guidance — the property under test is the **gate**, not the model's judgment; a weak prompt would risk a green test for the wrong reason. Failure-message discipline on a positive hit: cite the resolved JSONL path (via the package-private `jsonlPathFor` helper #364 introduced — now two consumers but stays file-private, YAGNI bar for promoting to a fixture export visible but not yet met), never echo `e.Raw` (mirrors selfcheck.go:217-219 — never log raw assistant content, pattern consistency across the codebase). `RunOpts` uses `--max-turns=2` (one turn for the model to attempt-and-fail, a second for a final reply so a hung run is distinguishable from a refused run in diagnostics), `--effort=low`, `--model=claude-haiku-4-5` (~$0.01). Reuses the canonical four-phase shape from #364 (worktree allocate → run → run-level asserts → property assertion); no new exports, no helper additions, ~94 LoC. **Operator-visible signal extension (#420):** appends a phase-5/6 disjunctive assertion to the same test after the gate-held loop completes — channel-agnostic by design (the contract is "operator gets *some* interpretable signal on denial", NOT "operator gets *this specific* signal"). Two new file-local detectors: `assistantTextRefusalHit(events) (bool, int)` walks `e.Kind=="assistant"` entries decoding `message.content[].{type,text}` and returns true on the first lowercased-substring match against fixed set `denialKeywords = ["cannot","can't","unable","not allowed","permission"]`; `structuredDenialHit(stdout) (bool, int)` scans the captured stream-json stdout with `bufio.Scanner` (default 64 KiB buffer — `result` lines are well under that, confirmed against the spike fixture set + `internal/agentrun/streamjson/testdata/captured_run.jsonl`) and returns true on the first `{"type":"result", "is_error":bool, "permission_denials":[]json.RawMessage}` line where `len(PermissionDenials) > 0 || IsError`. Combine point is `if !textHit && !structHit { t.Fatalf(...) }` — either alone is sufficient; both miss is the silent-no-op failure mode being guarded against. The `is_error` branch is forward-compat tolerance for a future claude that routes denial through `is_error=true` + a subtype change without touching `permission_denials` (canonical today). Detector deliberately stops there — accepting `subtype != "success"` would conflate denial with unrelated failure modes (max-turns hit, timeout, API rejection), muddling the contract. Keyword set kept narrow on purpose (broader markers like "available"/"access" false-positive in non-refusal contexts); the disjunctive design tolerates a keyword miss as long as the structured channel fires, so the right discipline if the test ever flakes is to expand the keyword set in a follow-up, NOT weaken to "any non-empty assistant text". JSON unmarshal errors in either detector skip silently (selfcheck.go:283 pattern). Both detectors return scanned-entry counts so the disjunctive-miss `t.Fatalf` names `denialKeywords` literally, assistant-entry count `N`, stdout-line count `M`, and the resolved JSONL path — never raw bytes (selfcheck.go:217-219 discipline applied even though the prompt is synthetic). **Architect picked extension, not sibling test**: same argv/model/prompt/RunResult as #365 — a sibling would re-run the same haiku call (~$0.01) with no behavioural difference and invite asymmetric drift where a future maintainer fixes the prompt for one test and forgets the other exists. Co-locating the two assertions makes the one contract (`pyry agent-run --allowed-tools=Read` is a deny-by-default gate that surfaces some signal to the operator) visible in one place. Companion to #418 (captured-trace null-findings fixture-walk on the direct-`claude` spike argv) — no overlap, different argv. Adds +111 LoC to `allowed_tools_enforcement_test.go`, zero per-run cost increase (two synchronous in-memory walks over the same `events` slice and `result.Stdout` bytes; no extra subprocess, no extra model call). See [`codebase/420.md`](codebase/420.md) for the disjunctive-design rationale. **Third consumer (#376):** `tool_loop_test.go` adds `TestRealClaude_ToolLoopIntegrity` — positive-path counterpart to #365. Seeds the worktree with `hello.txt`/`world.txt` (`os.WriteFile` 0o600), runs `pyry agent-run --allowed-tools Bash --max-turns=3 --effort=low --model=claude-haiku-4-5` with `"Use the Bash tool to run \"ls -1\" in the current directory, then tell me how many .txt files you see."`, and asserts the full internal tool loop end-to-end. Two assertion phases — (a) single-pass state machine over on-disk JSONL events with three flags (`bashToolUseID`, `sawToolResult`, `sawFinalText`) advanced in event order so the first missing piece is the one reported on failure: capture `tool_use` id from the first `{Type:"tool_use", Name:"Bash"}` content block under an `assistant`-kinded entry's `message.content[]`, then on `user`-kinded entries scan for matching `{Type:"tool_result", ToolUseID==bashToolUseID}`, then on subsequent `assistant` entries scan for `{Type:"text", Text:non-empty}`; (b) `bufio.Scanner` over `result.Stdout` for the `type:"result"` trailer line with `subtype=="success"`, `stop_reason=="end_turn"`, `num_turns>=2`. **Two load-bearing insights pinned structurally:** `tool_use`/`tool_result` are content blocks INSIDE `assistant`/`user` entries (not top-level events — `jsonl.Event.Kind`'s whitelist technically permits those values but that path is unreachable for production claude on disk), and the `type:"result"` trailer is exclusive to pyry's stdout (emitted by `streamjson/emitter.go.Close`), NOT on disk — assertions on the trailer therefore read `result.Stdout`, never `events`. The AC's "result event" wording did not disambiguate; the spec resolved it explicitly and pinned `parseResultTrailer` to stdout. Two file-local helpers: `parseContentBlocks(raw json.RawMessage) ([]contentBlock, error)` decodes the verbatim line into `{Message:{Content:[]contentBlock}}` with a single struct (`Type`, `Name`, `ID`, `Text`, `ToolUseID`, all `omitempty`-tagged) that handles `tool_use`/`tool_result`/`text` blocks one-shot — parse errors are a per-line skip mirroring `bashInvokedInRaw`/`selfcheck.go:283`; `parseResultTrailer(stdout []byte) (*resultTrailer, error)` scans stdout via `bufio.NewScanner(bytes.NewReader(stdout))` and returns the first decoded line where `Type=="result"` (non-JSON and non-result lines skip silently; missing the entire trailer is a hard `t.Fatalf` with stdout prefix because the trailer is a structural guarantee of every successful run). Default 64 KiB scanner line limit is sufficient vs. pyry's ~400-byte trailer; helper comment notes the `scanner.Buffer(make([]byte, 1<<20), 1<<20)` extension recipe for a future >64 KiB line. **`resultTrailer.PermissionDenials *[]json.RawMessage` pointer-typed** with `omitempty` so callers distinguish "field absent" (nil) from "field present, empty slice" (non-nil, len 0); pyry's emitter (`streamjson/emitter.go:251-275`) emits eleven trailer fields and `permission_denials` is NOT in that set (claude-only field, dropped by pyry's re-emitter), so the conditional check is dormant today — retained per the AC ("if present in the schema") as a forward-compat guard against either pyry's emitter growing the field or a future test variant comparing against claude's native stdout. **`num_turns >= 2`** lower bound: a tool-loop run necessarily has ≥2 assistant entries (tool_use turn + final text turn); `≥` absorbs any post-tool clarification turns within `MaxTurns: 3`. **Seeded fixtures (`hello.txt` "hello\n", `world.txt` "world\n", 0o600)** exist so the Bash invocation has deterministic targets to enumerate; not asserted against — the test pins protocol shape, not response content. **Prompt-as-enforcement-fixture discipline (#365 pattern)**: the property under test is the loop completing, not the model's judgment, so the prompt phrases Bash as the obvious tool and gives the model a concrete numeric question to answer in its final turn — exercising both the `tool_use`→`tool_result` hop AND the subsequent text turn. **SystemPrompt** minimal and neutral (`"You are an e2e regression-guard test. When asked to inspect the filesystem, use the Bash tool."`) — non-empty load-bearing per `validateRunOpts`. `MaxTurns: 3` is one turn of slack against haiku occasionally running a verification command. **Failure-message discipline:** on-disk-walk failures cite the resolved JSONL path via the file-package-private `jsonlPathFor` (#364, third consumer now — YAGNI bar for promoting to exported fixture helper visible but still not met); stdout-trailer failures embed the first 1 KiB of stdout; never echo `e.Raw` (consistent with #365 + `selfcheck.go:217-219`). Reuses canonical phase shape extended to five (worktree allocate → **seed text files** → run → run-level asserts → property assertion split into JSONL-walk + stdout-trailer-parse); no new exports, no helper additions, ~202 LoC. One real haiku call per run, ~$0.02. **Fourth consumer (#381):** `per_agent_test.go` adds five named top-level tests — one per dispatcher role — `TestRealClaude_PO_RoleLoop` / `_Architect_RoleLoop` / `_Developer_RoleLoop` / `_CodeReview_RoleLoop` / `_Documentation_RoleLoop`. Per-role-drift regression guard: the 2026-05-14 `/doctor` poisoning bug failed all five roles together; the next failure of this class will likely hit one role only, so five separate top-level functions (not a table loop) give five independent pass/fail signals on the nightly board. File-private `dispatcherBaseTools []string` mirrors `agents/dispatcher/src/dispatch.ts:1183` from the sibling `agent-dispatcher` repo — 21 entries (Bash/Read/Write/Edit/Glob/Grep/TodoWrite + 4 qmd + 2 context7 + 7 codegraph; **no figma entries** despite the ticket-body parenthetical — the dispatcher source-of-truth literal has none, see "Lessons learned" in `codebase/381.md`). `dispatcherAllowedToolsForRole(role)` returns `dispatcherBaseTools` for po/developer/documentation, `dispatcherBaseTools + "Agent"` for architect/code-review, mirroring `dispatch.ts:1184-1186`'s `needsAgent` membership check; cite comments above both are the renumber-detection breadcrumbs. **Defensive `make+copy` on both branches** so a caller's slice mutation cannot corrupt the package-level constant; unknown role panics (programmer error at the 5 call sites, not a runtime condition). System prompts are deliberate 1-2 line stand-ins ("You are a Pyrycode product-owner agent. You refine issue bodies. Use Read/Edit/qmd/codegraph for research."), NOT verbatim from `agents/<role>/CLAUDE.md` — the property under test is the (allowed-tools, system-prompt shape) wiring, not operator-prompt fidelity (which would make the suite expensive and falsely sensitive to prompt-edit churn). User prompts are role-appropriate one-shot tasks ("Rewrite this one-line ticket title…", "Implement a Go function `Add(a, b int) int`…", "Summarize this one-line commit message…") that resolve in ≤4 turns with haiku/low and produce a single end-of-turn assistant text block (no `tool_use`); each explicitly constrains reply shape to keep haiku from drifting into exploratory tool use. Shared `runRoleSmokeTest(t, role, systemPrompt, userPrompt)` body consolidates the six identical assertions — `ExitCode == 0`, `SessionID != ""`, `parseResultTrailer(result.Stdout)` succeeds, `PermissionDenials` empty (nil-or-zero-length, pointer-vs-empty distinction from #376 preserved), `NumTurns >= 1` (single-shot no-tool minimum; #376's `>= 2` is tool-loop specific), LAST `assistant`-kinded JSONL event has `EndOfTurn == true && TextChars > 0`. **Shared helper is not a table loop** — each `Test…_RoleLoop` is its own top-level function so per-role failure attribution surfaces on the nightly board. Reuses #376's package-private `parseResultTrailer` + `resultTrailer` directly (second consumer; no fixture-export widening) and #364's `jsonlPathFor` (fourth consumer). File-local `truncate([]byte) string` (1 KiB cap, suffixed `... (truncated)`) — extracted file-locally because the five assertion blocks invoke it 2-3 times each; the analogous inline pattern in #376/#365 is unchanged. `RunOpts: MaxTurns=4, Effort="low", Model="claude-haiku-4-5"`, no `Timeout` override (5-minute default is the runaway guard, not the SLO). Production dispatch uses opus/high; this deliberate haiku/low downgrade is the cost/coverage trade — a future nightly opus suite is a separate file, not an additive flag here. `t.Parallel()` is NOT called — matches the existing realclaude convention, keeps cost predictable, avoids API rate-limit interactions. ~$0.10 total per run (5 haiku calls, cache warm); ~30 s per test, ~150 s aggregate wall time. **Lesson:** first-pass implementation pulled three figma read-only entries into `dispatcherBaseTools` from the ticket body's parenthetical; the spec named `dispatch.ts:1183` as the source of truth and that literal has zero figma entries. Code review caught this as a MUST FIX (commit `be2d1a0`); shipping a pre-drifted mirrored constant defeats the suite's purpose. The cite comment was also corrected from the ticket-body line numbers (1190/1191-1193) to the actual source-of-truth lines (1183/1184-1186) — the cite is the renumber-detection breadcrumb and must land on the correct line. ~174 LoC. **Opt-in real-API fixture (#409):** `fixtures.go` adds `WithWorktreeAuthenticated(t) string` — sibling to `WithWorktree` for tests that need a real Anthropic API response (not argv-shape probes). Reads `ANTHROPIC_API_KEY` from the outer test-runner env; if unset, calls `t.Skipf` with a named-variable message **before** allocating `t.TempDir()` so a skipped run never strands an empty workdir (AC#5 mandates skip-not-fail so the suite stays green on contributor machines without API keys; AC#4 reconciles with #5 only by routing the absent-credential branch through `t.Skip` inside the helper rather than at every consumer). When present, delegates to `WithWorktree(t)` for `$HOME` isolation, then `t.Setenv("ANTHROPIC_API_KEY", key)` re-pins the credential into the test process. **Mechanically redundant** with `RunPyryAgentRun`'s `cmd.Env = append(os.Environ(), opts.ExtraEnv...)` inheritance at `fixtures.go:169` — the re-pin is intent-documenting, not bug-fixing: it forecloses silent regression if a future refactor inserts an env-scrubbing helper between this call and the subprocess spawn, and the `t.Setenv` cleanup ordering restores the value on test exit even if other in-test code mutates it. **No credential-shape validation** (no `strings.HasPrefix("sk-ant-")`) — claude is the only authority on key validity and surfaces bad values as a non-zero subprocess exit; same one-place-to-update discipline `validateRunOpts` uses for `--effort`. **No imports added** (`os`/`testing` already there). Doc comment (5 lines) names `ANTHROPIC_API_KEY` literally, restricts the helper to real-API tests, and surfaces the skip path — `go doc` is the only documentation surface required by AC#3. Closes the gap that #383's permission-protocol spike surfaced: a `$HOME`-pinned subprocess can't find `~/.claude/credentials` and returns a synthetic `assistant` envelope with `"text":"Not logged in · Please run /login"`, `error:"authentication_failed"`, `exit_code: 1`, `duration_ms: 402`, zero tool_use frames, zero permission events — claude exits at the auth gate before any model/tool/permission logic runs. Plain `WithWorktree(t)` is unchanged. **End-to-end test `TestWithWorktreeAuthenticated_RealAssistant`** (`fixtures_test.go:60-102`, ~43 LoC) self-skips when credential absent, otherwise runs `pyry agent-run` with minimum-viable shape (`Prompt:"Reply with the single word 'pong' and nothing else."`, `SystemPrompt:"You are a minimal e2e authentication probe. Keep replies under 10 words."`, `AllowedTools:["Read"]` minimum-non-empty, `MaxTurns:1`, `Effort:"low"`, `Model:"claude-haiku-4-5"`, no Timeout override) and asserts the canonical five-phase shape from `runRoleSmokeTest`: `ExitCode==0` → `SessionID!=""` → **negative-pin** against the #383 failure mode (`bytes.Contains(stdout, "\"model\":\"<synthetic>\"")` and `bytes.Contains(stdout, "\"error\":\"authentication_failed\"")` both rejected, with stdout in the diagnostic capped via `truncate` at 1 KiB) → walk `ReadJSONL` for the first `Kind=="assistant" && EndOfTurn && TextChars > 0` event, citing the resolved JSONL path via the file-local `jsonlPathFor` (fifth consumer now) on failure. The structural asserts would already fail on a synthetic-auth envelope (`ExitCode != 0`, no `EndOfTurn` text); the substring checks produce a clearer diagnostic for the auth-failed mode specifically — belt-and-suspenders against a *named* failure mode (#383's literal trace), not generic defensive coding. The 1 KiB stdout truncation isn't a leakage defense against today's stream-json (which does not echo the API key) but cheap insurance against any future claude diagnostic change. `t.Parallel()` is NOT called — matches the existing realclaude convention. ~$0.01 per `make e2e-realclaude` run when `ANTHROPIC_API_KEY` is exported, ~10-30 s wall time; zero cost / time when absent (Go's standard `SKIP` output). No change to `RunPyryAgentRun`; AC#1 satisfied by the existing inheritance path, AC#3/#4 by the in-helper gate, AC#5 by the test's pass-on-credentials / skip-on-absence behaviour. ~55 LoC total. **Fifth consumer (#382):** `resilience_test.go` adds four named top-level protocol-resilience tests — `TestRealClaude_BashTool_NonZeroExit` (RunPyryAgentRun + AllowedTools:["Bash"] + prompt `"Use the Bash tool to run \"ls /nonexistent-path-for-pyrycode-382\". Report what you see."`; walks JSONL events with three independent observation flags `bashToolUseID` / `sawToolResult` / `sawFinalText` so a failure message names the actual missing event rather than a downstream consequence — first-pass coupled `sawFinalText` on a derived `toolResultErr` flag and review caught this as a SHOULD FIX (`0e8e364`); five JSONL assertions then `parseResultTrailer` → `trailer.Subtype == "success"` (do NOT assert on `StopReason` — `end_turn` and `tool_use_error` both satisfy the AC, the "structured signal not pipe break" contract IS `Subtype == "success"`)), `TestRealClaude_PrematureStdinClose` (bypasses pyry, calls `claude` directly via `exec.CommandContext` with 60 s deadline; writes the production-shape user-turn envelope `{"type":"user","message":{"role":"user","content":[{"type":"text","text":"…"}]}}` via local `buildUserTurnEnvelope` then closes stdin — exactly the streamrunner production pattern; asserts `ctx.Err() != context.DeadlineExceeded` AND `exitCode == 0`; `WithWorktreeAuthenticated(t)` required because claude actually processes the envelope), `TestRealClaude_MalformedStreamJSON` (direct exec, plain `WithWorktree(t)` — claude rejects before reaching the model so no API cost; writes garbage `[]byte("this is not stream-json\n}}{{not json either\n")`, asserts `ctx.Err() != DeadlineExceeded`, `exitCode != 0`, `len(stderr) > 0`, **stderr predicate is OR-of-keywords** `{"json","parse","input","format","envelope"}` case-insensitive — pinning a specific phrase couples the test to claude's diagnostic copy; if a future claude version emits zero matches, expand the keyword set, keep the OR-shape, never pin a phrase), `TestRealClaude_LargePromptNearContextWindow` (RunPyryAgentRun + `strings.Repeat("All work and no play makes Jack a dull boy. ", 1200)` → ~52 KB prompt + `WithWorktreeAuthenticated`; disjunctive assertion as a single `if !branchA && !branchB { t.Fatalf(...) }` so the failure message lists both branches on "neither" — branch A = `trailer.Subtype == "success"` AND `trailer.StopReason NOT IN {"context_overflow","max_context_length"}`, branch B = `trailer.Subtype != "success"` AND `result.ExitCode != 0`; an unknown new stop_reason is treated as the structured-error branch by virtue of the explicit-set membership check; the failure mode being guarded against is "neither" — no result trailer at all (crash) or success-with-overflow-shaped-stop-reason (silent truncation)). **File-local helpers (`resilience_test.go:271-378`):** `resolveClaudeBin(t)` honours `PYRY_CLAUDE_BIN` unless its absolute path equals `os.Args[0]`'s (defense against the 2026-05-16 fork-bomb pattern that `RunOpts.UseTestBinaryAsFakePyry` also encodes — second occurrence of the pattern; `t.Fatalf`-not-skip because pointing the env var at the test binary is a misconfigured run, not an absent claude); falls back to `exec.LookPath("claude")` + `t.Skipf` on miss. `directClaudeArgs() []string` returns the eight-flag argv mirroring `buildClaudeArgs` minus `--append-system-prompt-file` (direct-exec tests don't need a system prompt). `runClaudeDirect(t, ctx, bin, workdir, stdinBytes) (int, []byte, error)` wraps `exec.CommandContext` + `StdinPipe` + `Start` + `Write` + `Close` + `Wait`; **stdin-write errors are `t.Logf`-not-`t.Fatalf`** because Test 3's fast-reject path produces an EPIPE-wrapped `*os.PathError` from claude having already closed its end — first-pass had `errors.Is(err, io.ErrClosedPipe)` predicate which doesn't match the actual wrapping chain, review caught this as a NIT and the fix drops the predicate entirely. Stdout is captured but not returned — neither caller asserts on response content, capturing prevents pipe backpressure. `buildUserTurnEnvelope(prompt) ([]byte, error)` builds the envelope locally rather than importing `internal/agentrun/streamrunner` to keep dependency direction one-way. **Two-field extension to `contentBlock` in `tool_loop_test.go`:** added `Content json.RawMessage \`json:"content,omitempty"\`` and `IsError bool \`json:"is_error,omitempty"\``. Both `omitempty`-tagged; no behavior change to #376's `TestRealClaude_ToolLoopIntegrity` (does not read either field). Doc-comment gained a four-line note explaining why `tool_result` content uses `json.RawMessage` — the body is a string OR an array of nested content blocks per claude's stream-json contract, and `RawMessage` holds either shape. **MUST FIX in code review (`0e8e364`):** first-pass read `b.Text` for the tool_result content non-emptiness check; `b.Text` is always `""` for `tool_result` blocks (the field is populated only for `text` blocks), so the assertion would have fired on every nightly regardless of claude's output. Fix adds `Content json.RawMessage` to the struct and asserts on its length. Lesson: when extracting a field from a foreign CLI's structured output, verify the field name + shape against real captured testdata (`internal/agentrun/jsonl/testdata/*.jsonl` — 19 `"is_error":...` occurrences confirmed the `is_error` field presence without spending a real API call). Bounded timeouts: 60 s on Tests 2 + 3 (tight, property is "no hang"), 5-min `RunPyryAgentRun` default on Tests 1 + 4 (runaway guard, not SLO). Three real haiku calls per run (Tests 1, 2, 4; Test 3 rejects pre-model), ~$0.08 total with cache warm. `t.Parallel()` NOT called — matches the existing realclaude convention. No edits to `fixtures.go` (spec explicitly forbade it); `runClaudeDirect` stays file-local until a third raw-exec test materialises. ~380 LoC. **MCP server smoke suite (#384):** `mcp_smoke_test.go` adds four named top-level tests, one per MCP server the dispatcher agents depend on (`TestRealClaude_MCP_QMD` → `mcp__qmd__query`; `TestRealClaude_MCP_Context7` → `mcp__plugin_context7_context7__resolve-library-id`; `TestRealClaude_MCP_CodeGraph` → `mcp__codegraph__codegraph_search`; `TestRealClaude_MCP_Figma` → `mcp__plugin_figma_figma__get_metadata`). Each drives a haiku turn through `pyry agent-run` and asserts the named MCP tool fires with non-empty `tool_result`; protocol drift on any server (renamed tool, changed parameter shape, broken stdio handshake) surfaces as a structured failure rather than silently wedging a dispatched agent. **HOME is deliberately NOT pinned** — `WithWorktree`/`WithWorktreeAuthenticated` both pin `$HOME` to a tempdir, which disables claude's MCP server discovery (claude reads `$HOME/.claude.json` + `$HOME/.claude/plugins/...`); tests allocate `t.TempDir()` workdir and leave HOME pointing at the outer operator's home so claude resolves the same MCP server set the operator has configured. Per-test JSONL files land under `~/.claude/projects/<encoded-tempdir>/`, bounded by tempdir uniqueness; acceptable because the suite is not fully hermetic today (`WithWorktreeAuthenticated` already leaks `ANTHROPIC_API_KEY`) and CI runners have controlled HOME. Inline `ANTHROPIC_API_KEY`-unset skip per test (mirrors `WithWorktreeAuthenticated`'s message shape, inlined because the helper's HOME pin is incompatible). File-local pre-flight probe `mcpServerHealthy(t, displayName) (bool, string)` runs `claude mcp list` under a 10 s `context.WithTimeout`, scans for `<displayName>:` prefix + `✓ Connected` sentinel, returns `(true, reason)` for absent / `! Needs authentication` / `✗ Failed to connect` / probe-timeout so callers `t.Skipf` with operator-actionable context; `PYRY_CLAUDE_BIN` intentionally NOT honored (reserved for stubbed pyry tests; fork-bomb defense lives in `resilience_test.go:resolveClaudeBin`). Plugin-prefixed names (`plugin_context7_context7`, `plugin_figma_figma`) pinned because that's what claude actually emits in JSONL `tool_use.name`; the mismatch with the unprefixed `mcp__context7__*` form in `dispatcherBaseTools` (#381) is invisible there because role smoke prompts don't compel context7 calls — a follow-up to align the dispatcher list is scoped out. Codegraph test additionally seeds a one-symbol Go source file (`func PyrycodeSeedSentinel384() {}`) + runs `codegraph index .` (30 s timeout) in the workdir before driving the call — an MCP server with no index returns empty results regardless of protocol health, so the seed closes the false-negative window; `exec.LookPath("codegraph")` miss skips cleanly. Figma test pins a public Figma Community file URL (`figmaTestFileURL = "https://www.figma.com/community/file/1035203688168086460/material-3-design-kit"`) as a file-level const with a top-of-file swap-license comment ("if this URL stops resolving, replace with another stable public file — the test asserts the protocol round-trip, not any one file's content"). Shared `assertMCPToolUsed(t, workdir, result, toolName)` helper consolidates the six identical assertions (`ExitCode == 0`, `SessionID != ""`, assistant `tool_use` with `name == toolName` and non-empty `id`, matching user `tool_result` with `tool_use_id` equal and `len(content) > 0`, trailer `PermissionDenials` nil-or-empty); non-emptiness sufficient because content text is server- and version-dependent and pinning literal content would couple the test to one server's output shape. **Fourth consumer of `parseContentBlocks` + `contentBlock`** (#376 originated; #382 extended with `Content json.RawMessage` + `IsError` — both consumed here; the `Content` field is what makes the `tool_result` non-emptiness check correct per #382's lesson about `b.Text` always being `""` for tool_result blocks). **Fourth consumer of `parseResultTrailer`**; **seventh of `jsonlPathFor`**; **third of `truncate`**. Tight shared system prompt keeps the model on a single-call rail. `RunOpts: MaxTurns=3, Effort="low", Model="claude-haiku-4-5"`. Four real haiku calls per run (~$0.08 with cache warm, matching AC). `t.Parallel()` NOT called — keeps cost predictable, avoids two concurrent `claude mcp list` invocations contending on the same MCP server stdio sockets. ~290 LoC, zero edits to `fixtures.go`, zero production-source changes. **Budget guardrails (#385):** `budget_test.go` adds two top-level tests pinning cost-relevant guarantees the suite did not previously exercise end-to-end. `TestRealClaude_CacheHitWarmsAcrossRuns` runs the same `RunOpts` skeleton twice through `RunPyryAgentRun` against `WithWorktreeAuthenticated(t)` and asserts the second invocation's trailer reports `Usage.CacheReadInputTokens > 0` (primary, `t.Fatalf`) — pinning Anthropic prompt-cache alignment within the 1-hour TTL when (system-prompt, allowed-tools, model, effort) are identical; a regression that breaks cache-key alignment (dynamic content in the system prompt, per-invocation tool-list churn) shows `== 0` here. Diagnostic-only check on the first run's `CacheCreationInputTokens > 0` uses `t.Errorf` (not `t.Fatalf`) so a sub-threshold system prompt surfaces as a soft signal rather than masking the primary on a passing run; the (0+pass vs. 0+0) interpretation matrix is documented inline. `cacheHitSystemPrompt` is a deterministic 5-sentence string concatenation (~300 tokens) sized to clear Haiku 4.5's ~2048-token implicit-cache minimum by margin; the doc comment names the "no dynamic content" constraint (date, run id) explicitly because that is the regression class the test catches. `TestRealClaude_MaxTurnsHonored` runs `pyry agent-run --max-turns=2` against a prompt that natural-completion would require ≥5 turns (numbered 5-line Bash sequence with explicit "do NOT combine" guidance) and asserts five fields on the trailer: `Subtype == "error_max_turns"`, `TerminalReason == "max_turns"`, `NumTurns == 2` (exact — off-by-one fires here), `StopReason != "end_turn"`, `IsError == true`. **`ExitCode == 0` is correct** — `pyry agent-run` exits 0 on a successfully-emitted result trailer regardless of trailer `is_error`; the budget-exhaustion signal lives in the trailer fields, not the subprocess exit code, and a code comment pins this so a future maintainer doesn't "fix" the assertion to `!= 0`. Tool-call-collapse risk pinned in a comment above `maxTurnsPrompt`: if a future haiku revision is smart enough to fire all five `echo`s in one tool_use block (or otherwise complete in ≤2 turns naturally), the assertions fail loudly and the right fix is to bump the prompt to force more turns (e.g. 8 sequential `read X.txt` calls), not to weaken the assertion. Three-field extension to the `resultTrailer` struct in `tool_loop_test.go` adds `IsError bool`, `TerminalReason string`, and `Usage resultTrailerUsage` (plus the new `resultTrailerUsage` sub-struct emitting all four token-count fields); all `omitempty`-tagged so pre-#385 consumers (#376/#381/#382/#384) decode unchanged — single-source extension over duplicating the parser in `budget_test.go`. **Fifth consumer of `parseResultTrailer`**. File-local `truncateStdout([]byte) string` mirrors the inline pattern from `tool_loop_test.go:127`. `RunOpts`: cache-hit uses `MaxTurns=1, AllowedTools=["Read"]`; max-turns uses `MaxTurns=2, AllowedTools=["Bash"]`; both `Effort="low", Model="claude-haiku-4-5"`. Three real haiku calls per run (2+1), ~$0.04 total with cache warm, matching the AC. `t.Parallel()` NOT called — matches the existing realclaude convention; also load-bearing on the cache-hit test where concurrent runs would muddy the "cache warmed by run 1 specifically" diagnostic. ~199 LoC, zero edits to `fixtures.go`, zero production-source changes. **Long-running session JSONL append integrity (#421):** `long_session_test.go` adds `TestRealClaude_LongSessionJSONLIntegrity` — the **first ≥10-turn test in the suite** and **eighth consumer of the fixture trio**, closing the long-turn-count gap left by peer tests that cap at `MaxTurns ∈ {1,2,3,4}`. Seeds the workdir with a 10-line `numbers.txt` (`"1\n2\n…\n10\n"`, `0o600`) then drives a real `claude-haiku-4-5` session through ten distinct single-command Bash operations against it (`wc -l`, `head -n 3`, `tail -n 3`, `sort`, `uniq`, `cat`, `grep 5`, `wc -c`, `awk '{s+=$1} END {print s}'`, `ls -l`) under `RunOpts: MaxTurns=12, Effort="low", AllowedTools=["Bash"], Model="claude-haiku-4-5", Timeout=10*time.Minute`. `MaxTurns=12` is a deliberate 2-turn headroom above the ≥10 floor — early signal if haiku starts collapsing turns despite the anti-chain steering, never trips on the natural-completion path. `Timeout` bumped from the 5-minute `RunPyryAgentRun` default to 10 minutes to absorb cold-network/queue tail latency on a 10-turn run without inflating the success-path budget on short tests — per-test override over fixture-default mutation. Two file-scope prompt constants mirror `budget_test.go`'s `maxTurnsSystemPrompt` / `maxTurnsPrompt` shape (count bumped from 5 to 10, `error_max_turns` framing dropped): `longSessionSystemPrompt` is the one-paragraph anti-chain steering ("use the Bash tool one command at a time, wait for each result before continuing, do NOT combine commands, and do NOT chain commands with && or ;"); `longSessionUserPrompt` enumerates the ten numbered single-command operations and ends with an explicit "Do NOT combine these. Do NOT use && or ; to chain them. After all ten results, summarize what you saw in one short sentence" — the **summary tail is load-bearing for the last-event assertion**: it nudges the model to emit one more `assistant`-kinded `end_turn` text block, giving the last-event-`EndOfTurn && TextChars > 0` assertion something to land on without depending on the model spontaneously producing a closing turn (without it, the LAST assistant entry can be a `tool_use`-only message with `TextChars == 0`, failing the assertion on a non-regression). Six sequential `t.Fatalf` assertions in evaluation order: (1) `ExitCode == 0` (truncated stderr on failure); (2) `SessionID != ""` (truncated stdout on failure); (3) `parseResultTrailer(result.Stdout)` succeeds (truncated stdout on failure); (4) `trailer.NumTurns >= 10` — **failure message names the prompt-design recourse** ("if the model is collapsing turns, expand the prompt or strengthen anti-chain steering — do NOT lower the threshold") at the point of failure so the next maintainer reads the correct fix where it'll be seen, not in a doc buried under newer tickets; (5) single-pass JSONL walk via `ReadJSONL` counting `e.Kind == "assistant" && EndOfTurn` entries (must be ≥10) AND tracking the last `e.Kind == "assistant"` entry, which must satisfy `EndOfTurn && TextChars > 0` (failures include `jsonlPath` from `jsonlPathFor`, the observed counts, and the last entry's `EndOfTurn`/`TextChars` values — same shape as `per_agent_test.go:128-132`); (6) **forward-defensive negative tripwire** `!bytes.Contains(result.Stderr, []byte("bufio.Scanner: token too long"))` — the **literal stdlib error text pinned verbatim** (paraphrasing would silently disable the trip), expected to pass trivially today because no production `bufio.Scanner` exists in the relevant path and `ReadJSONL`'s underlying `jsonl.NewReader` has a 16 MiB cap (`internal/agentrun/jsonl/reader.go:27-30`, read-path is structurally safe); its job is to fire the day someone wires a stdlib `bufio.Scanner` into pyry's production stdout/stderr surface without bumping the buffer and hits a long claude line in this multi-turn run — failure message points the maintainer at the fix (`tool_loop_test.go:210` precedent: bump to 1 MiB). The Scanner tripwire deliberately anchors on the long-running multi-turn test, not a short one — a 1-turn test would not produce a long-enough line to trigger the regression class, so the assertion has to live where the symptomatic input shape exists, even though the assertion itself is forward-defensive. Regression classes guarded together: Scanner buffer downgrade (the literal-stderr trip), off-by-one trailer write (trailer `num_turns` assertion), trailing-newline drift (JSONL walk counting `EndOfTurn`), scanner reset across turn boundaries (≥10 distinct end-of-turn entries), buffer flush gap swallowing the last event (last-assistant-entry `EndOfTurn && TextChars > 0`). Seeded-Bash variant chosen over the documented text-only fallback ("list 10 facts about Helsinki, one per turn") because ten distinct shell commands give the model ten concrete, separable tasks (`wc -l` ≠ `head -n 3` ≠ `tail -n 3` etc.) that resist collapsing into a single combined turn even under brevity pressure; the fallback stays documented as recourse if a future haiku revision collapses the Bash prompt. Reuses `WithWorktreeAuthenticated` (eighth consumer) + `RunPyryAgentRun` + `ReadJSONL` + `parseResultTrailer` (sixth consumer) + `jsonlPathFor` (eighth consumer) + `truncate` (fourth consumer) unchanged — **zero new helpers, zero new exported types, zero new struct fields, zero edits to `fixtures.go` or any peer test file**. One real haiku call per `make e2e-realclaude` run, ~$0.05–$0.10 (higher per-test than peers because of the turn count, but bounded — same order of magnitude as `budget_test.go`'s cache-hit + max-turns pair); `t.Parallel()` NOT called — matches the existing realclaude convention. ~140 LoC including the two prompt constants and inline comments, zero production-source changes. See [`codebase/421.md`](codebase/421.md) for the design rationale. **SIGTERM-mid-tool_use cleanup regression guard (#422):** `sigterm_mid_tool_use_test.go` adds `TestRealClaude_SigtermMidToolUse` — the **ninth consumer of the fixture trio** and the SIGTERM-mid-tool_use cell of the resilience matrix `resilience_test.go` (#382) opened. Pins three production invariants together when pyry receives SIGTERM with a Bash subprocess in flight: (a) no orphan subprocess survives in pyry's process group, (b) the on-disk session JSONL ends at a complete envelope boundary (no half-written trailing line a future `--continue` would choke on), and (c) pyry exits within 5 s of SIGTERM. Same family as the 2026-05-16 fork-bomb incident (`ca8b688`, different shape — test binary self-execing via `PYRY_E2E_BIN` inheritance — same cleanup-paths-leaking class). Flow: `WithWorktreeAuthenticated(t)` → write `prompt.txt` (`"Use the Bash tool to run \`sleep 30\`. Do nothing else."`) + `system.txt` (anti-chain steering mirroring `longSessionSystemPrompt`) at `0o600` → `ensurePyryBuilt(t)` → `spawnPyryAgentRun(t, …)` returns `*exec.Cmd` started under its own process group (`cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}` so `pgid == cmd.Process.Pid`) → `time.Sleep(3 * time.Second)` (architect-budgeted to clear claude's `tool_use` envelope write to disk AND fork of the Bash subprocess — raise to 5 s on flakes, do NOT go below 2 s, do NOT lower `--max-turns` or shorten the prompt) → `cmd.Process.Signal(syscall.SIGTERM)` → bounded `cmd.Wait` in a goroutine raced against `time.After(5 * time.Second)`. **The 5 s deadline IS the production contract**, not a test-tuning budget: it matches `internal/agentrun/streamrunner/runner.go:40`'s `killGrace = 5 * time.Second` which pyry sets as `cmd.WaitDelay` on the claude child; production behaviour is exactly SIGTERM → ctx cancel from `signal.NotifyContext(ctx, syscall.SIGTERM, syscall.SIGINT)` in `cmd/pyry/agent_run.go:runAgentRun` → streamrunner forwards SIGTERM to claude → `cmd.Wait` returns within `killGrace` (SIGKILL fallback via `WaitDelay`); a future PR that bumps `killGrace` would correctly break this test, forcing the maintainer to read both sides together. On timeout the test calls `cmd.Process.Kill` then drains the wait channel under a 2 s ceiling so the stderr buffer read in the `t.Fatalf` message is happens-after the stdlib forwarder writes (avoids `-race` flake on a wedged child). **Orphan check via `pgrep -g <pgid>`, not `pgrep -P <pyry-pid>`**: after pyry exits, claude (and its Bash descendant) reparent to init/launchd, so the parent-pid linkage is gone before the test can observe it. Process-group membership is preserved across reparenting, so `-g <pgid>` catches the actual orphan; `-P <pid>` (direct-children only) would falsely report "no orphans" regardless of whether the orphan is still alive. Architect-picked shape "most reliable on macOS + Linux" (procps-ng on Linux, stock pgrep on macOS since 10.8); fails loudly with "pgrep -g is required" if a future CI runner ships without pgrep — recourse is then either replace the shell-out with `syscall.Kill(-pgid, 0)` + `errors.Is(err, syscall.ESRCH)` or install pgrep in the image. Filter drops `pgid` itself from the result list (defense against transient zombie visibility). On a positive hit the test calls `syscall.Kill(-pgid, SIGKILL)` before `t.Fatalf` to salvage the orphaned `sleep 30` so it doesn't linger past the test run; an unconditional `t.Cleanup(func() { _ = syscall.Kill(-pgid, syscall.SIGKILL) })` registered immediately after `cmd.Start` is the defense-in-depth safety net for any early `t.Fatalf` (e.g. `Process.Signal` returning unexpectedly) that trips before the salvage runs — registered cleanups run after the test goroutine exits, so the orphan-check assertion still has teeth. **JSONL terminal-shape pin: branch B** (clean stream truncation at a complete envelope boundary), chosen by the architect over branch A (structured trailer with non-success subtype) on the reasoning that the on-disk JSONL is claude's session-state file for `--continue`, NOT a stream-json result stream — there is no evidence claude flushes a structured trailer line to disk on signal (the stream-json result trailer is a **stdout** feature, not an on-disk JSONL feature). Assertion sequence: (1) `parseInitSessionID` over captured stdout (failure points at the 3 s sleep being too tight on this runner), (2) `os.ReadFile` of `jsonlPathFor(workdir, sessionID)`, (3) **byte-tail check** — `len(jsonlBytes) > 0` AND `jsonlBytes[len-1] == '\n'`; failure message names trailing-byte count + last-newline index because **this is the ONLY way to surface a half-written tail** (`ReadJSONL` silently retains trailing partial bytes per `internal/agentrun/jsonl/reader.go:188-262`; the production reader is streaming over an open log being appended to, so the silent-retain behaviour is correct for production but defeats a naive event-walk assertion), (4) walk via `ReadJSONL` for the first `assistant`-kinded event whose content blocks include `{Type:"tool_use", Name:"Bash", ID:!=""}` and capture `bashToolUseID` (failure → raise pre-SIGTERM sleep), (5) walk subsequent `user`-kinded events and assert NONE contain a `{Type:"tool_result", ToolUseID==bashToolUseID}` content block (failure → reduce pre-SIGTERM sleep or extend Bash duration — `sleep 30` finishing within an 8 s window is impossible, so the failure indicates the test setup is broken). Each failure message distinguishes "test setup wrong" from "production broken" so the next maintainer doesn't paper over a real regression by tweaking the sleep. If a future probe reveals branch A actually holds, flip assertion (5) to "find a result envelope with `subtype != success`" — same surface, opposite sign; the spec contract that the shape is **pinned** in one direction or the other is unchanged. **Two file-local helpers**: `spawnPyryAgentRun(t, bin, workdir, promptPath, systemPath, stdoutBuf, stderrBuf) *exec.Cmd` (~20 LoC) constructs the same eight-flag argv as `RunPyryAgentRun` but does NOT wait for completion — the test needs PID access to send SIGTERM mid-run, and `RunPyryAgentRun` runs synchronously and provides no PID access during the run. `processesInProcessGroup(t, pgid int) []int` (~15 LoC) shells out to `pgrep -g <pgid>` and parses the output (exit code 1 with empty output → no matches; exit code 0 with PID lines → orphans; any other failure → `t.Fatalf`). Per the ticket's "Technical Notes" guidance and `resilience_test.go`'s `resolveClaudeBin` / `runClaudeDirect` precedent (the same file-local-until-second-consumer pattern for raw-exec helpers), both stay file-local; promote to `fixtures.go` only when a second test needs the same shape. **Prompts**: `sigtermSystemPrompt` is the one-paragraph regression-guard steering mirroring `longSessionSystemPrompt`'s anti-chain wording ("use the Bash tool exactly once, run the command verbatim, do NOT chain commands with && or ;, do NOT comment, and do NOT do anything else"); `sigtermPrompt` is `"Use the Bash tool to run \`sleep 30\`. Do nothing else."`. `sleep 30` is far longer than the combined 3 s pre-SIGTERM + 5 s post-SIGTERM window so the Bash subprocess is guaranteed alive when the signal lands. `--max-turns=2` is the minimum that lets claude reach the `tool_use` event (turn 1 is the assistant `tool_use`; `1` would not give claude a budget to call a tool; turn 2 never completes because SIGTERM tears claude down before the Bash subprocess returns). **Reuses unchanged**: `WithWorktreeAuthenticated` (ninth consumer — the architect chose this over plain `WithWorktree` because the assertions depend on claude actually invoking Bash; a synthetic-auth envelope would skip the `tool_use` event entirely and the "no Bash tool_use observed" assertion would fire on a non-regression), `ReadJSONL` + `JSONLEntry` alias, `ensurePyryBuilt` (same-package private access — build-once-per-process via `sync.Once` + `PYRY_E2E_BIN` short-circuit), `parseInitSessionID` (same-package private access), `parseContentBlocks` + `contentBlock` (#376 originated, #382 extended; **fifth consumer** — the struct's existing `Name`/`ID`/`ToolUseID` fields cover exactly what assertions (4) and (5) need, no struct extension required), `jsonlPathFor` (#364, **ninth consumer**), `truncate` (#381, **fifth consumer**). Zero new fixture-package exports, zero new struct fields, zero edits to `fixtures.go` or any peer test file, zero production-source changes. One real haiku call per run, ~$0.005 with cache warm (lower than peer haiku-low tests because the run is truncated at SIGTERM before claude generates the final response turn — only the `tool_use` envelope is produced and billed before pyry tears the call down). `t.Parallel()` is NOT called — matches the existing realclaude convention. ~280 LoC including the package-level comment block and inline rationale. See [`codebase/422.md`](codebase/422.md) for the design rationale. **Large tool-output regression sensor (#423):** `large_tool_output_test.go` adds `TestRealClaude_LargeToolOutput_ExceedsDefaultScannerBuffer` — the **tenth consumer of the fixture trio** and the >64 KiB-on-one-line cell of the Scanner-buffer regression matrix. Orthogonal to #421's long-session test: that test fires when many short lines accumulate; this one fires when a single line on pyry's stream-json stdout exceeds the 64 KiB stdlib `bufio.Scanner` cap. Today only `permission_protocol_spike_test.go:133` extends a Scanner past the default; if that buffer extension is ever dropped — or if a similarly truncating scanner is wired into pyry's stream-json forwarding path — large tool output gets silently corrupted (Scanner returns `Scan() == false` with no error on a too-long token); this test fails. Drives a real `claude-haiku-4-5` session through a single Bash invocation producing ~80 KiB of stdout in one `tool_result` content block via `RunPyryAgentRun(t, RunOpts{Workdir, Prompt:largeToolOutputUserPrompt, SystemPrompt:largeToolOutputSystemPrompt, AllowedTools:["Bash"], MaxTurns:2, Effort:"low", Model:"claude-haiku-4-5"})`. **Deterministic prompt** (`printf '%80000s' '' | tr ' ' 'A'` — exactly 80,000 literal `A` characters on a single line, no trailing newline) preferred over `/dev/urandom` per the AC so any future fixture-snapshot work doesn't churn; at ~80 KiB it clears the 64 KiB stdlib cap by ~16 KiB and stays comfortably under the 1 MiB extended cap and the 16 MiB on-disk reader cap. The `"exactly as given"` wording in `largeToolOutputSystemPrompt` is load-bearing because paraphrasing risks fewer bytes and a missed 70 KiB threshold; the system prompt also forbids chaining (`"Do not chain commands with && or ;"`) to keep the model on a single-call rail. `MaxTurns: 2` is the structural minimum for the loop `assistant tool_use → user tool_result → assistant text → end_turn`; bumping to 3 is the documented recourse if a future haiku revision collapses turns or refuses the literal command (do NOT weaken the byte-length thresholds or the exact-match assertion). Pins four contracts together: (1) trailer reports `subtype="success"` AND `stop_reason="end_turn"`, (2) the on-disk JSONL `tool_result` content block exceeds 70 KiB (headroom under the ~80 KiB target absorbs framing/encoding), (3) pyry's stdout-forwarded `tool_result` content block has **byte-equal length** to the on-disk twin (the regression sensor — the emitter is contracted to re-emit `ev.Raw` verbatim per `internal/agentrun/streamjson/emitter.go:149-150`, so any non-zero delta means a scanner truncated the forwarding path), (4) stderr does NOT contain `bufio.Scanner: token too long`. **Why a local scanner — NOT a call to `parseResultTrailer`**: the shared `parseResultTrailer` (`tool_loop_test.go:211`) uses the stdlib's 64 KiB default; with an 80 KiB `user`/`tool_result` line on stdout **before** the trailer line, the default scanner returns `Scan() == false` (no error) on the long line, exits the loop, and returns `"no type:result line in stdout"` — a false negative that would mask the very regression this test exists to catch. **The shared `parseResultTrailer` helper is NOT edited** — the regression scope stays one file; the local `findResultTrailer` walks pre-scanned 1 MiB-capped lines from `scanLargeStdoutLines` instead (canonical extension pattern: `scanner.Buffer(make([]byte, 0, 64*1024), 1024*1024)`, mirrors `permission_protocol_spike_test.go:133`). Each returned line is a fresh `make([]byte, len(scanner.Bytes())) + copy` so callers can retain it past subsequent reads (Scanner reuses its internal buffer between `Scan` calls). `scanner.Err()` is propagated so a future buffer-exhaustion regression (a line above 1 MiB) fails loudly rather than silently truncating — silently discarding the error would re-introduce the exact failure mode the test is meant to catch, just at a different threshold. **Exact byte-length match, not a tolerance band** — the emitter writes `ev.Raw + '\n'` verbatim, so the disk-side and stdout-side bytes are byte-identical modulo the trailing newline; comparing `len(stdoutContent) == len(jsonlContent)` is the strongest possible assertion and any non-zero delta IS the regression being guarded against. Tolerance bands have to be justified against an absent transformation step; here there is none, so the tolerance is zero. The failure message names this directly (`"tool_result content length mismatch: stdout=%d bytes, jsonl=%d bytes — this is the regression sensor for #423; a scanner in pyry's stdout forwarding path has truncated the tool_result content block (the emitter is contracted to re-emit ev.Raw verbatim, see internal/agentrun/streamjson/emitter.go:149-150)"`) AND cites the emitter line so a future PR that introduces a transformation step has to update both the assertion and the contract together. **Invariant to `Content` shape**: claude may emit `tool_result.content` as either a bare JSON string (common for raw Bash stdout) OR a nested `[{type:"text",text:"..."}]` array; both shapes are byte-equal through `Emit`, so comparing raw `json.RawMessage` byte lengths between disk and stdout is shape-invariant — the test does NOT decode `Content` further. The 70 KiB threshold is on the raw byte length including JSON framing (quotes for the string shape, the wrapping array for the nested shape) and absorbs both. **Four file-local helpers** (`scanLargeStdoutLines`, `findResultTrailer`, `findBashToolResultBlock`, `findToolResultContentByID`, ~70 LoC combined) and two file-scope prompt constants (`largeToolOutputSystemPrompt`, `largeToolOutputUserPrompt`); helpers stay file-local until a second test needs the same shape, mirroring `resilience_test.go`'s precedent (`resolveClaudeBin`/`runClaudeDirect`) and #422's `spawnPyryAgentRun`/`processesInProcessGroup`. `findBashToolResultBlock` walks JSONL events in a single pass: on each `assistant` entry, decode content blocks via the shared `parseContentBlocks`, capture the first `{Type:"tool_use", Name:"Bash", ID:!=""}`; on subsequent `user` entries, find the first `{Type:"tool_result", ToolUseID==bashID}` and return its `Content` (`json.RawMessage`) plus block index. `findToolResultContentByID` walks the pre-scanned stdout lines and decodes each via `parseContentBlocks`, returning the first `tool_result` block whose `tool_use_id` matches. Per-line parse errors are silently skipped in both (matches `tool_loop_test.go:75-80`) — user envelopes with a plain-string `content` (not an array) fail the array-decode and are correctly ignored. **Reuses unchanged** (no new fixture-package surface, no struct extensions): `WithWorktreeAuthenticated` (tenth consumer — chosen over plain `WithWorktree` because the assertions depend on claude actually invoking Bash with the literal command; a synthetic-auth envelope would skip the `tool_use` event entirely and the "no Bash tool_use observed" assertion would fire on a non-regression), `RunPyryAgentRun`+`RunOpts`+`RunResult`, `ReadJSONL`+`JSONLEntry`, `parseContentBlocks`+`contentBlock` (#376 originated, #382 extended; **sixth consumer** — the existing fields `Type`/`Name`/`ID`/`ToolUseID`/`Content` cover exactly what the helpers need, and **`Content json.RawMessage` extended in #382 is the load-bearing field** that makes the byte-length equality assertion work without a struct extension — pre-#382 the test would have needed a struct extension), `resultTrailer` (the local `findResultTrailer` decodes into it directly; the `parseResultTrailer` function itself is bypassed for the 64 KiB-cap reason above), `jsonlPathFor` (#364, **tenth consumer**), `truncate` (#381, **sixth consumer**). **Failure-message discipline** distinguishes "test setup wrong" (`"no Bash tool_use observed in JSONL assistant entries (model may have refused to run the literal command; sharpen the system-prompt's \"exactly as given\" wording or bump MaxTurns to 3)"`, `"JSONL tool_result content byte length = %d, want > 70000 (target ~80 KiB; model may have paraphrased the printf/tr pipeline — inspect the JSONL for the actual command and stdout it ran)"`) from "production broken" (the byte-length mismatch message named above) — the next maintainer routes the response correctly at the point of failure rather than triage at first by guess. **Forward-defensive stderr substring tripwire** (`!bytes.Contains(result.Stderr, []byte("bufio.Scanner: token too long"))`) mirrors `long_session_test.go:135-139` — pins the literal stdlib error text verbatim (paraphrasing would silently disable it); failure message points the maintainer at the fix ("bump its buffer to 1 MiB (see tool_loop_test.go:210 note)"). One real haiku call per run, ~$0.02 with cache warm (large `tool_result` inflates token count above peer haiku-low tests but is bounded). `t.Parallel()` is NOT called — matches the existing realclaude convention. ~252 LoC including the package-level comment block, two prompt constants, and inline rationale, zero edits to `fixtures.go` or any peer test file, zero production-source changes. See [`codebase/423.md`](codebase/423.md) for the design rationale. |
| [permission-protocol-spike.md](features/permission-protocol-spike.md) | `--permission-prompt-tool stdio` captured-trace spike (#383) — `internal/e2e/realclaude/permission_protocol_spike_test.go` invokes `claude` directly (NOT `pyry agent-run`) with `--allowed-tools Read --permission-prompt-tool stdio --permission-mode <mode>` under `WithWorktreeAuthenticated` (#409); writes every stdout line verbatim to `internal/e2e/realclaude/testdata/permission_protocol_v<version>_<mode>.json`. Test PASSES on any non-structural outcome (the on-disk fixture IS the artefact); only "zero events captured AND deadline tripped" is a `t.Fatalf`. Matrix sweep is manual: edit the file-private `permissionMode` constant, rerun, rename the version-only fixture to `_<mode>.json` before the next overwrite — the rename discipline is load-bearing for diffing across reruns; resist embedding as a `for mode := range modes` loop (cost is per-call to a real API). Six fixtures committed at claude `v2.1.143`, one per mode: `default`/`acceptEdits`/`auto`/`plan`/`dontAsk`/`bypassPermissions`. Argv divergence from `buildClaudeArgs`: NO `--dangerously-skip-permissions` (would defeat the spike), ADDS `--permission-prompt-tool stdio` + `--permission-mode <mode>`, OMITS `--append-system-prompt-file` + `--effort` (minimal input surface across reruns). 90 s hard timeout via `context.WithTimeout`. 1 MiB `bufio.Scanner` line cap (64 KiB default too small for model output). Atomic fixture write (`os.WriteFile(path+".tmp", …)` → `os.Rename`). Stdin envelope matches `internal/agentrun/streamrunner`'s production shape (`{"type":"user","message":{"role":"user","content":[{"type":"text","text":"…"}]}}`); the architect's alternative `{"type":"user","content":"…"}` was not probed — the production shape was accepted on the first try with no stderr error. File-local `truncateString(string, int) string` is the string variant of `per_agent_test.go:138`'s `truncate([]byte) string` (same-name collision caught by `go vet -tags e2e_realclaude`); lifting to a shared helper is deferred to a fourth consumer. `packageDir(t)` uses `os.Getwd()` rather than `runtime.Caller`-based discovery — Go test runs each package's tests in its own source directory; `runtime.Caller` would resolve to a temp build path on some toolchain configurations. **Findings (all six modes, identical event shape `system/init → assistant(thinking) → assistant(tool_use Bash) → user(tool_result) → assistant(thinking) → assistant(text) → result/success`):** (1) **no permission-gate event fires on stdout in any mode** — the architect's pre-spike `{"type":"control_request","request_id":"…",…}` hypothesis is unsupported; (2) **`--allowed-tools Read` is NOT enforced under this argv** — Bash invokes against the worktree in every mode and `result.permission_denials` is empty (compare `allowed_tools_enforcement_test.go`, which asserts the allowlist IS enforced when `--dangerously-skip-permissions` is also present — the contrast is the basis for the interaction-order finding); (3) **`--permission-mode auto` is echoed back as `default`** in `init.permissionMode` (synonym, not a distinct mode); pyrycode-mobile should treat them as the same mode; (4) **`init.tools` is the full 27-tool registry**, NOT the `--allowed-tools` allowlist — readers verifying allowlist behavior must look at `tool_use` events or `result.permission_denials`, NOT the bootstrap event. **Most plausible interpretation** (unverified by the spike): `--permission-prompt-tool <tool>` expects the name of a registered tool (typically MCP-prefixed, e.g. `mcp__permissions__approve`) that claude consults synchronously per non-allowlisted invocation; the literal string `stdio` does not resolve to a known tool, so claude silently no-ops the gate and the allowlist becomes advisory. **Mobile relay implication:** the deny-by-default posture must combine `--dangerously-skip-permissions --allowed-tools <relay-vetted-set>` for hard enforcement AND replicate the prompt-tool flow separately through a registered MCP tool. **Expected response shape on stdin: not inferable from this spike** — no request envelope observed, so no captured `request_id` or request shape to mirror; the architect's `{"type":"control_response","request_id":"…","response":{…}}` hypothesis remains unverified. Per AC#4, no follow-up assertion-based test is filed — the "filed only if a real event fires" branch is not triggered (zero events in zero modes). **Environment-leaky fixtures by design:** the `tool_result` payload contains the spike-runner's macOS POSIX username (`drwxr-xr-x@ 4 juhanailmoniemi  staff …`) — Bash runs against a real worktree; reviewers diffing fixtures across spike-runners on different machines should expect machine-local variance in `tool_result` payloads; the load-bearing data is event *shape*, not payloads. Side-channel capture (FD 3+, IPC, Unix-domain socket) is out of scope — current read surface is `cmd.Stdout` only. ~$0.08 total across the six modes (~$0.014 per cache-cold haiku run, ~5-10 s each). **Regression contract (#418):** `permission_protocol_regression_test.go` walks every fixture matching `testdata/permission_protocol_v*_*.json` and asserts the four null findings as a regression contract — pure `json.Unmarshal` on committed `testdata/`, zero API cost, build-tag gated. Per fixture: no `stdout_events[i].type` is `control_request` / `permission_request` / `tool_permission_request` (finding #1); `result.permission_denials` is empty (finding #1/#2); `init.tools` contains `"Bash"` (finding #4 — bootstrap envelope echoes the full registry, NOT the `--allowed-tools` allowlist); `init.permissionMode` echoes the `<mode>` token parsed from the filename via `^permission_protocol_v[^_]+_([A-Za-z]+)\.json$` with the `auto → default` synonym (finding #3) applied at the filename parser, not at the assertion site; structural sanity `exit_code == 0` + `context_deadline_tripped == false` + `len(stdout_events) >= 7`. Closes the regression-alarm gap left by #383's AC#4 — the spike test pins the on-disk artefact and PASSES on rerun regardless of whether the protocol still behaves the same way, so a future `claude` release that started emitting permission events on stdio (or enforcing the allowlist under this argv) would silently regenerate the fixtures and flip the mobile-relay design implication. The filename glob is the source of truth for matrix coverage — `t.Fatalf` on zero matches (deleted fixture set must be loud); a future spike rerun that adds a mode drops a new fixture and this test picks it up automatically with no code change. **`t.Errorf` (not `t.Fatalf`) for per-finding assertions** so a single subtest run reports every flipped finding simultaneously — load-bearing diagnostic shape for a release where `claude` changes multiple behaviours at once; `t.Fatalf` reserved for structural blockers (unreadable file, malformed top-level JSON, unparseable filename). Each failure message names `<fixture-filename>:` and *which* finding flipped (e.g. `init.permissionMode = "auto", want "default" (spike finding #3 flipped)`) so the spike-runner is pointed directly at the findings section above to revisit. Single pass over `stdout_events` simultaneously flags forbidden event types via `slices.Contains`, captures the first `system/init` raw bytes, and captures the first `result/success` raw bytes; narrow `json.RawMessage` parse decodes only the four shapes the assertions need (top-level + event header + init body + result body) so future record-field churn in the spike test does not break this regression contract. Per-line `json.Unmarshal` failure is per-event `t.Errorf` (one bad line cannot mask other assertions — same shape as `allowed_tools_enforcement_test.go:60-67`). Subtest naming via `t.Run(filepath.Base(path), …)` so the `go test` failure header names the offending fixture directly; `t.Parallel()` on inner subtests (independent reads, cheap I/O, no shared state). Stdlib only (`encoding/json`, `os`, `path/filepath`, `regexp`, `slices`, `testing`); no edits to `fixtures.go`, no production-source changes, ~183 LoC. Zero per-CI-run cost; `make check` unaffected. |
| [update-package.md](features/update-package.md) | `internal/update` — building blocks for `pyry update`: `ParseLatestRelease` + `CompareVersions` (#179, leading-`v` + pre-release suffix tolerance); `AssetName` + `ParseChecksumsFile` + `VerifySHA256` (#180, GoReleaser tarball filename for `linux/darwin × amd64/arm64` plus SHA-256 verify); `DetectRestartCommand` (#181, `RestartProbe` → launchctl/systemctl argv, launchd wins on tie); `Fetcher` (#182, zero-value-usable `net/http` wrapper with `FetchLatestRelease` + `FetchAsset`); `ExtractBinary` (#186, in-memory tar.gz → binary bytes via `filepath.Base` matcher); `AtomicReplace` (#187, write-temp-in-same-dir + fchmod + fsync + rename); sentinels `ErrMalformedRelease` / `ErrInvalidVersion` / `ErrUnsupportedPlatform` / `ErrAssetNotInChecksums` / `ErrMalformedChecksums` / `ErrChecksumMismatch` / `ErrBinaryNotInArchive` / `ErrMalformedArchive` for `errors.Is` branching |
| [install-script.md](features/install-script.md) | `install.sh` — curl-piped release downloader (`PYRY_VERSION`, `PYRY_INSTALL_DIR`) + post-install smoke check (#203) that detects a default-named running service (`launchctl print | grep 'state = running'` on darwin, `systemctl --user is-active --quiet pyry` on linux), restarts it, and probes `${INSTALL_DIR}/pyry status` for the `Uptime: 2562047h47m16.854775807s` sentinel from #202; four exit codes (0 healthy / 2 sentinel / 3 dial-fail / 4 restart-fail); leans on `pyry status`'s built-in dial retry (#199) + 5s deadline as the grace period (no extra `sleep`); `internal/install/test_smoke_classify.sh` is the bash-only unit test for `classify_status` |
| [config-package.md](features/config-package.md) | `internal/config` — `Config` (with `RelayURL`), `DefaultConfig()`, `Load(path)` overlay-decode loader for `~/.pyry/config.json`; foundation for Phase 3 (mobile + relay), no consumers wired in this slice |
| [identity-package.md](features/identity-package.md) | `internal/identity` — `ServerID` (UUIDv4-shaped string newtype), `NewServerID()` (crypto/rand + version/variant nibbles, panic-on-rng-fail), `ParseServerID(s)` returning `ErrInvalidServerID` for malformed input, `LoadOrCreate(path)` (#207, first-run mint + atomic-rename persist at mode 0600 / parent 0700, single trailing newline tolerated, never rewrites on parse failure — corruption is operator-escalated, never auto-regenerated, since paired devices bind tokens to a specific server-id); foundation for Phase 3 pairing/relay routing, no consumers wired in this slice |
| [keys-package.md](features/keys-package.md) | `internal/keys` (#438) — binary-side X25519 static keypair for Mobile Protocol v2 (Noise_IK). `StaticKey` (unexported `priv`/`pub [32]byte`) + by-value `PrivateKey() [32]byte` / `PublicKey() [32]byte` accessors (mutating the return does not alias internal storage, pinned by `TestStaticKey_AccessorsReturnByValue`); `LoadOrCreate(baseDir, daemonName string) (*StaticKey, error)` as the package's single entry point, owning the full `(baseDir, daemonName) → filepath.Join(baseDir, daemonName, "static_key.json")` mapping so a caller-precomputed path cannot bypass the allowlist; `validDaemonName` (unexported, white-box tested) — length 1..64, every byte in `[a-z0-9_-]`, no leading `-`, character-by-character scan with no regex, rejects empty/`.`/`..`/`/`/`\`/uppercase/whitespace/NUL/multi-byte UTF-8/leading hyphen/oversize-65; reject path returns `fmt.Errorf("keys: invalid daemon name %q: %w", daemonName, ErrInvalidDaemonName)` and performs zero filesystem operations (pinned by `TestLoadOrCreate_InvalidDaemonName` which asserts `baseDir` has zero entries after every reject row); `newStaticKey()` via stdlib `crypto/ecdh.X25519().GenerateKey(rand.Reader)` (panics on `crypto/rand` failure mirroring `identity.NewServerID`'s discipline — zero-entropy keys would break the Noise_IK authentication model), `.Bytes()`/`.PublicKey().Bytes()` give the raw 32-byte material wire-compatible bit-for-bit with `flynn/noise.DHKey`. On-disk schema locked to `docs/protocol-mobile.md` § Static keys — binary side: `onDiskKey{Version int, Algorithm string, PrivateKey, PublicKey string (base64.StdEncoding padded), CreatedAt time.Time (RFC 3339 UTC via MarshalJSON after t.UTC() to strip monotonic-clock)}` with constants `schemaVersion=1`, `algorithmName="Noise_25519"`, `filename="static_key.json"` — `base64.StdEncoding` chosen deliberately over `RawURLEncoding` (opposite of `internal/pair`'s QR/wire encoding) because on-disk JSON gets padded base64. Atomic-write recipe: `os.MkdirAll(dir, 0o700)` → `os.CreateTemp(dir, ".static-key-*.tmp")` → **`os.Chmod(tmp, 0o600)` BEFORE write** (defeats macOS umask collapsing the requested mode, same defence as `internal/identity/store.go:77`) → `json.Marshal` body → `f.Write` → `f.Sync` → `f.Close` → `os.Rename(tmp, path)`; `defer os.Remove(tmp)` cleans partial state on any earlier failure; rename is the commit point so no observer sees partial bytes. Three-way switch on `os.ReadFile` error (`nil` → parse; `errors.Is(err, fs.ErrNotExist)` → mint+persist; else → wrapped non-`ErrCorruptKeyFile` error — preserves I/O-vs-corruption sentinel distinction, mirrors `internal/identity/store.go:38-46`, trapped by `TestLoadOrCreate_NonENOENTReadErrorIsNotCorruption` which makes `static_key.json` itself a directory). Seven-step load-side validation (all fail-fast returning `ErrCorruptKeyFile`-wrapped errors): `json.Unmarshal` syntax → `version == schemaVersion` → `algorithm == algorithmName` → `base64.StdEncoding.DecodeString(private_key)` succeeds + length 32 → same for `public_key` → `!created_at.IsZero()` (encoding/json's `time.Time` unmarshaller already rejects non-RFC3339 strings at step 1 so one `IsZero()` covers both absent and malformed) → `ecdh.X25519().NewPrivateKey(priv).PublicKey().Bytes()` matches stored `pub` via `crypto/subtle.ConstantTimeCompare` (anti-tamper; constant-time buys nothing for public material but matches project hygiene posture per `internal/devices.VerifyToken`). **Existing files are NEVER overwritten on the load path even on validation failure** — paired phones bind to a specific keypair and silent regeneration would invalidate every pairing without operator awareness (same invariant as `internal/identity.LoadOrCreate`; pinned by `TestLoadOrCreate_CorruptJSONReturnsSentinel` which asserts post-call bytes are byte-identical to the seeded fixture). Sentinels `ErrInvalidDaemonName` + `ErrCorruptKeyFile` (both `errors.Is`-matchable); bare wrapped `fmt.Errorf` (no sentinel) covers filesystem I/O errors so an operator can distinguish corruption from permissions/disk-full. Error messages include the file path (operator-actionable) but NEVER include file contents, base64 fields, or decoded bytes — pinned by `TestLoadOrCreate_CorruptJSONErrorDoesNotLeakPrivateKey` which seeds a known base64 private key, triggers `algorithm` mismatch, asserts `err.Error()` substring-free of the known base64 (single defensive assertion against future error-message refactors). SECURITY contract in package doc-comment + `StaticKey.PrivateKey()` doc-comment: returned `[32]byte` is the X25519 static secret, MUST NOT be logged / wrapped into error / emitted on a wire (mirrors `internal/devices`'s plaintext-token contract); no `slog` calls in the package at all. Daemon-name validator deliberately NOT consolidated with `cmd/pyry/main.go:sanitizeName` — `sanitizeName` is a transformer that replaces bad chars with `_` and permits `.`/uppercase, whereas this REJECTS; reusing would silently defeat the path-traversal defence (`sanitizeName("..") == ".."`). Two-file split (`static_key.go` pure type + validator + generator; `store.go` `LoadOrCreate` + JSON schema + atomic write) mirrors `internal/identity` (`server_id.go` + `store.go`); same-package (white-box) tests so unexported `validDaemonName`/`newStaticKey`/`onDiskKey`/`schemaVersion` are directly testable. Concurrency: not safe for concurrent use against the same path; bootstrap runs once at daemon startup before any goroutines fan out. Package owns the full `(baseDir, daemonName) → absolute-path` mapping (caller-precomputed path would bypass the allowlist); package does NOT validate `baseDir` (the allowlist defends against `daemonName` injection, not against an attacker-controlled `baseDir`). Fifth instance of the per-daemon JSON-file pattern + atomic-write recipe (after `internal/sessions`, `internal/identity`, `internal/devices`, `internal/conversations`); `docs/PROJECT-MEMORY.md` § Project-level conventions named "a fifth registry forces extraction" but the architect re-evaluated at this boundary and concluded the helper stays duplicated until a sixth — the umask-defence variant matters and the recipe is ~25 lines, extraction risks one helper shared across stores where the defence matters and stores where it does not. **Filesystem hardening landed in #439** as the hard prerequisite for any production consumer (#432 QR payload, #433 Noise wrapper): two new `errors.Is`-matchable sentinels `ErrInsecureKeyDirMode` / `ErrInsecureKeyFileMode` (distinct because the operator's chmod target differs — lumping under one `ErrInsecurePermissions` would force parsing the wrapped message); `ensureSecureKeyDir(dir)` rejects parent-dir `mode & 0o077 != 0` (or non-directory) with a wrapped sentinel naming the path and observed mode in `%#o`, runs `MkdirAll(0o700)` + re-stat on first run (defensive against ACLs / future stdlib changes widening the requested mode — `mkdir(2)` cannot itself widen under a normal umask); `LoadOrCreate` switched from `os.ReadFile` to `os.Lstat` + strict-equality `0o600` check + `openSecureKeyFile` (`os.OpenFile(path, O_RDONLY|syscall.O_NOFOLLOW, 0)` then `io.ReadAll`); belt-and-suspenders symlink defence is two-fabric — `Lstat` catches symlink-at-rest (a symlink's own mode `0755`/`0777` fails the `== 0600` check before the open ever runs), `O_NOFOLLOW` catches the dynamic swap during the TOCTOU window between Lstat and open with `ELOOP`; **no auto-`chmod` ever** (the loud-failure contract IS the security feature — auto-repair would hide packaging bugs that unpacked the key as `0644`, and would hand a hostile sibling process under the same UID the exact moment of vulnerability they need); error wrapping `keys: %s: mode %#o: %w` names path + observed mode + sentinel and never the file contents, decoded bytes, or resolved symlink target (`*os.PathError.Path` is the caller-supplied path, not the resolved target; no `os.Readlink` enrichment); `syscall.O_NOFOLLOW` canonical on both Linux + macOS (no `golang.org/x/sys/unix` dep added). Directory check uses `& 0o077 != 0` (permits narrower modes like `0600`/`0500` — security contract is "no g/o access", not "exactly 0700"; narrower modes are non-functional and the daemon fails loudly with `EACCES` on subsequent operation); file check is strict `!= 0o600` because the package writes at exactly `0600` and anything else (including narrower) indicates tampering. Pre-existing `TestLoadOrCreate_NonENOENTReadErrorIsNotCorruption` continues to pass after the refactor — the directory-at-key-path trap now surfaces as `ErrInsecureKeyFileMode` (Lstat sees directory mode ≠ `0600`) before the read happens, neither `Is(ErrCorruptKeyFile)` nor `Is(ErrInvalidDaemonName)` per the test's assertions. New tests `TestLoadOrCreate_InsecureDirModeRejected` (table `0700` accept / `0750`/`0755`/`0701` reject; asserts message contains dir path + observed mode `%#o` and no private-key leak, file bytes unchanged), `TestLoadOrCreate_InsecureFileModeRejected` (table `0644`/`0640`/`0660`/`0666` reject; chmod-back-to-`0600` proves the reject path didn't mutate content), `TestLoadOrCreate_FreshCreateUnderHostileUmaskStill0700` (`syscall.Umask(0)` is process-global so non-parallel; asserts post-create dir mode `0700` and file mode `0600`), `TestLoadOrCreate_SymlinkRefusedOnRead` (symlink decoy with sentinel keypair; asserts `errors.Is(ErrInsecureKeyFileMode)` and message contains neither decoy path nor key bytes), `TestLoadOrCreate_InsecureFileModeErrorDoesNotLeakPrivateKey` (extension of the existing no-leak shape for the new sentinel). `io.ReadAll` on the opened file is uncapped — CR `[INFO]` finding consistent with spec § Security threat #7 (hostile process under same UID is out-of-scope today); revisit `io.LimitReader` if the threat model expands. |
| [v2-session-manager.md](features/v2-session-manager.md) | `internal/relay` v2 session manager (#445, extended #446/#452/#454/#453/#450) — fourth surface of `internal/relay` (alongside `connection.go` outbound dial, `auth.go` v1 first-frame gate, `handlers/` per-envelope-type processors): per-`conn_id` state machine that completes a Mobile Protocol v2 Noise_IK handshake, validates the AEAD-sealed device-token piggybacked in IK message 1 early-data, emits `noise_resp` (IK message 2 carrying `hello_ack`) on success, and refuses every out-of-state inner frame at the WS-close layer. **No application dispatch** — `noise_msg` frames that reach `open` are dropped silently; the open-state handler chain in `internal/relay/handlers/` is structurally unreachable from this slice (the load-bearing invariant the gating test pins for AC #4). Four-state lifecycle `V2StateAwaitingInit` → `V2StateHandshakeComplete` → `V2StateOpen` → `V2StateClosed` with `handshakeComplete` observably distinct from `open` even though both can be set inside the same `noise_init` handler — the externally-controlled `state` field exists so a same-package gating test (`TestV2Session_Gating_NoiseMsgInHandshakeComplete_4401`) directly assigns the substate + injects hand-rolled `(send, recv)` CipherStates and structurally proves the "handler chain unreachable from handshakeComplete" invariant; any future refactor that defers token validation cannot silently remove the gating row. Two new exported WS close-code constants `StatusProtocolMismatch websocket.StatusCode = 4421` (state-machine / discriminator violations) and `StatusHandshakeFailure websocket.StatusCode = 4426` (Noise_IK failure before CipherStates exist) declared alongside the existing `StatusUnauthorized = 4401` (reused unchanged from `auth.go`); style mirrors `auth.go`'s exported-close-code pattern. New wire-format types in `internal/protocol/v2envelope.go` (~28 LOC): `V2Version = 2` const, three discriminator constants `TypeNoiseInit` / `TypeNoiseResp` / `TypeNoiseMsg`, `InnerFrameV2{Version int, Type string, Data string}` struct (Data is `base64.StdEncoding`-padded; decoded length cap 65535 bytes = Noise framework per-message limit, enforced at the JSON-decode boundary so oversized payloads never reach `Responder.ReadInit`). One-line additive modification to `internal/protocol/handshake.go`: appends `Token string \`json:"token,omitempty"\`` to `HelloClientPayload` — in-band carrier of the device-pairing token under v2 (`docs/protocol-mobile.md` § Authentication line 420 moves the token from the WS-header / routing envelope into AEAD-sealed early-data); `omitempty` keeps v1 round-trip byte-identical for existing fixtures and tests; the v1 `RoutingEnvelope.Token` field is NOT removed in this slice (v1 dispatcher still consumes it). Manager surface: `V2Session` (unexported fields incl. `*noise.Responder` + paired `*noise.CipherState` send/recv), `(*V2Session).State() V2SessionState` accessor, `V2SessionConfig{Frames, Outbound, StaticPriv, Devices, ServerID, Logger}`, `V2SessionManager`, `NewV2SessionManager(cfg) (*V2SessionManager, error)` (panics on nil Frames/Logger like `dispatch.New`; wrapped error on nil Outbound/Devices, empty ServerID, wrong-length StaticPriv), `(*V2SessionManager).Run(ctx) error` (blocks until Frames closes or ctx is cancelled; every per-conn session is dropped on return). Outbound is a function (not channel) so production wires `(*relay.Connection).Send` directly and tests substitute an in-memory recorder; non-nil errors from Outbound are log-and-drop (mirrors v1's `cmd/pyry/relay.go` forwarder posture). **Single dispatch goroutine** owns all `*V2Session` mutation; `m.sessions` map mutated exclusively by `Run`; no lock — the loop IS the lock for the non-goroutine-safe `flynn/noise` `CipherState` mutable nonce counter (same contract as #433 pushed to the per-conn-dispatch boundary). Intentionally simpler than `internal/dispatch.Dispatcher`'s per-conn fan-out — this slice runs no handlers, every frame is synchronous Noise (~100 µs) or sub-µs JSON, so single-goroutine fan-in is correct; per-conn fan-out is a #446 concern. State transitions: `noise_init` in `awaitingInit` runs the handshake — lazy-construct `noise.Responder` from `cfg.StaticPriv` (one X25519 derivation per real handshake, not at session-creation; zero cost for conns that never send a frame) → `Responder.ReadInit(data)` → `protocol.Envelope` decode + `Type == TypeHello` check + `HelloClientPayload` decode → marshal `HelloAckPayload{ProtocolVersion:"v2", ServerID, ConnID}` envelope → `Responder.WriteResp(ackJSON)` returns response bytes + paired `(send, recv)` CipherStates → state → `handshakeComplete` → marshal `InnerFrameV2{Type: noise_resp, Data: base64(respMsg)}` → `cfg.Devices.Validate(hello.Token)`: hit ⇒ emit noise_resp envelope + state → `open` (log `event=v2.handshake.accept`, `conn_id`, `device_name`); miss ⇒ emit noise_resp first (so AEAD channel exists on wire), then AEAD-seal an `auth.invalid_token` error envelope under `s.send`, wrap as `noise_msg`, emit ONE `RoutingEnvelope{ConnID, Frame: noiseMsg, CloseCode: 4401}` so the error frame and the close are **atomic on the wire** (phone observes error envelope BEFORE WS close per spec § Failure modes line 436; two-call `Send+CloseConn` would race the relay's output paths — mirrors v1 dispatcher's `outcome.Code` → `Response.CloseCode` mapping in `cmd/pyry/relay.go:38` + `dispatch.go:498-512`). Step-5 reconciliation: spec originally read "decode early-data as Envelope; on err OR non-hello: take AEAD-sealed `auth.invalid_token` path" — implementation follows the reconciled rule that at step 5 CipherStates do NOT yet exist (no key to seal under), so early-data decode failure or non-hello at handshake-time is a 4421 handshake-layer protocol violation (close-only); the AEAD-sealed-error path is reserved for after step 7 when CipherStates exist. `Responder.ReadInit` failure (MAC fail, wrong static pubkey, malformed IK message 1) → close(4426). `Responder.WriteResp` failure (defensive; practically unreachable under correct flynn/noise) → close(4426). The "gating" row `noise_msg in handshakeComplete`: AEAD-decrypt-then-decode-as-Envelope; decrypt failure → close(4421); decrypt success → seal `auth.invalid_token` under live `send` + emit ONE envelope `{Frame, CloseCode: 4401}` regardless of envelope type — handler chain unreached structurally. `noise_resp` from a phone (only the binary writes it) → close(4421) in every state with `reason=noise_resp_from_phone` log field. `noise_msg` in `awaitingInit` (no CipherStates) → close(4421). `noise_init` after `handshakeComplete` or `open` → close(4421) (rekey is #435, not here). Unknown `type` / bad `v` / malformed JSON / oversized `data` → close(4421). State `closed` drops late frames silently. `V2SessionConfig.StaticPriv` is the binary's 32-byte X25519 static private key; doc-comment declares MUST-NOT-log / MUST-NOT-wrap-into-error / MUST-NOT-emit-on-wire (same contract as `internal/keys` + `internal/noise`). Log discipline: ACCEPT logs `device_name` (operator-actionable); REJECT logs `conn_id` + `close_code` only, **NO `device_name`** even when early-data carried one — anti-enumeration of paired-device names from binary logs (mirrors `auth.go:129-132`). `HelloClientPayload.Token`, `cfg.StaticPriv`, raw `Frame` bytes, AEAD ciphertext, plaintext payload bytes, and base64 forms thereof are MUST-NOT-log at any level (including `%w`-wrapped errors). AEAD-sealed error envelope on 4401 emits static `MsgInvalidToken` + fixed `CodeAuthInvalidToken` — no attacker-influenced content echoed; close-only paths (4421/4426) emit no envelope. **Not yet wired in production** — `cmd/pyry/relay.go` continues to run the v1 `internal/dispatch.Dispatcher` against `/v1/server`; the v2 manager is reachable only through test wiring today. Cutover lives with #446 (which lands `noise_msg`-in-open-state handler dispatch) or a sibling, gated by [`pyry pair preflight`](features/pyry-pair-command.md) (#436). Fakerelay extensions: `mux.HandleFunc("/v2/server", s.handleBinary)` shares the existing handler (relay-side routing-envelope wire unchanged in v2), and `binaryRecvPump` now treats `json.RawMessage` that marshals to the literal token `"null"` as "no frame to forward" — close-only `{ConnID, CloseCode: code}` envelopes marshal with `Frame: nil` (which round-trips through the WS as `"frame": null`); without the guard fakerelay would forward `null` as a phone-side WS text payload, breaking the production close-only contract. Fakephone extensions: `SendBytes(data []byte)` / `ReceiveBytes(timeout)` byte-oriented siblings of `Send(env)` / `Receive(timeout)` so v2 tests build the `InnerFrameV2` (not a `protocol.Envelope`) as raw bytes and bypass the envelope marshal/unmarshal; v1 envelope-oriented variants delegate to the byte variants so v1 behaviour is byte-identical. Same-package unit tests in `internal/relay/v2session_test.go` (~730 LOC, stdlib only): happy-path / bad-token-AEAD-error-then-4401 / IK-reject-4426 / noise_init-after-open-4421 / gating-noise_msg-in-handshakeComplete-4401 / out-of-state-rejections table-driven / config-validation panics + wrapped errors. E2E in `internal/e2e/relay_v2_handshake_test.go` (~354 LOC, build tag `e2e`): wires `relay.Connect` + `V2SessionManager` inline against fakerelay's new `/v2/server` (no daemon — `cmd/pyry/relay.go` still wires v1), dials a fakephone against `/v1/client` (unchanged routing wire), drives a Noise_IK handshake from the phone side via `internal/noise.Initiator` + an in-test `crypto/ecdh.X25519` keypair; three subtests cover happy / bad-token (phone reads AEAD-sealed `auth.invalid_token` then `LastCloseStatus() == 4401`) / IK-reject (phone reads close 4426, no prior frame). Out of scope: open-state handler dispatch (#446), encrypted echo round-trip + tampered-`noise_msg` AEAD-failure 4421 teardown (#446 — no dispatch path exists here to fail), rekey timer + `rekey_request` envelope (#435), per-phone-conn 10s handshake timeout (requires relay→binary "phone connected" forward signal which doesn't exist in v2 wire today), `V2Session` cleanup on phone-initiated WS close (same root cause), `RoutingEnvelope.Token` removal (v1 hard-cutover slice). **#446 fills the open-state `noise_msg` row**: `V2SessionConfig` gains optional `Handlers map[string]dispatch.Handler`; `V2Session` captures the matched device snapshot (`s.device`) in the token-accept branch of `handleNoiseInit` (right before state advances to `V2StateOpen`) and surfaces it through `*dispatch.Conn.Auth()` on each per-frame dispatch. New `dispatchAppFrame` AEAD-decrypts the inbound `noise_msg` via `s.recv.Decrypt`, allocates a per-frame `outbound chan protocol.RoutingEnvelope` of capacity `handlerOutboundBuf = 8` and a per-frame `*dispatch.Conn` via `dispatch.NewConn` (the new production-allowed sibling of `NewTestConn` — same body, different policy), calls `dispatch.Route(ctx, logger, conn, cfg.Handlers, plaintext)` synchronously, then drains `outbound` non-blockingly (`select { case env := <-outbound: ...; default: return }` — the channel is intentionally NOT closed so a misbehaving handler that forks a sender after dispatchAppFrame returns writes into a leaked-but-capacity-bounded channel the GC reclaims, rather than panicking on a closed-channel send), AEAD-encrypts each reply under `s.send.Encrypt`, wraps as `noise_msg` via `marshalInnerFrameV2`, and emits via `m.send` with `CloseCode: 0` (the reply's `CloseCode` is ignored — handlers do not signal closes through `c.Send`/`c.Reply`, that field is reserved for the manager's own `closeWith` close-intent emissions). AEAD-failure teardown on tampered/replayed/truncated `noise_msg`: `s.recv.Decrypt` returns non-nil → log `v2.aead.fail` with `conn_id`+`close_code=4421` (NO error text — flynn/noise error may carry counter indices that aren't operator-actionable) → `closeWith(ctx, s, StatusProtocolMismatch, nil)` emits one close-only routing envelope AND deletes the session entry from `m.sessions`, so the next `noise_init` for the same `conn_id` lazy-creates a fresh `awaitingInit` with no carry-over CipherStates (AC #3, structurally provided by reusing the existing `closeWith` rather than adding a parallel cleanup path). The handler chain is structurally unreachable on AEAD failure: the decrypt-failure branch returns BEFORE `dispatchAppFrame` is called — pinned by `TestV2Session_OpenState_TamperedFrame_HandlerNotReached`'s `atomic.Bool` side-effect flag. Transition table updated: `noise_msg, decrypt succeeds` in `open` becomes "dispatch via handler chain; sealed reply emitted; state stays open" (previously "drop, open-state dispatch deferred"); `noise_msg, decrypt fails` in `open` becomes "close(4421), → closed (AEAD-failure teardown; session entry dropped)" (previously "drop"). Concurrency: handlers run synchronously on the single dispatch goroutine — a slow handler stalls dispatch for ALL `conn_id`s (worst-case `send_message`'s 30s `Activate` timeout). Per-conn fan-out (one goroutine per `conn_id` with a per-session mutex guarding `s.send`/`s.recv`, mirroring `dispatch.Dispatcher.runConn`) is the priority follow-up before production cutover, NOT in this slice. Log policy extension: AEAD plaintext, ciphertext, handler reply envelope bytes (pre-encrypt), encrypted reply bytes (post-`Encrypt`), and base64 forms of any of those MUST NOT appear in any logged field; no per-envelope log on the open-state happy path (high-frequency message traffic would spam the log channel — existing per-handler logs surface that diagnostic instead). Refactor companion in `internal/dispatch`: extract `Dispatcher.handleOne` body into the new exported `Route(ctx, logger, conn, handlers, frame)` free function and `Dispatcher.sendError` into a package-private free `sendError(ctx, logger, conn, ...)` — mechanical (≤10 LOC moved), preserves behaviour, existing dispatcher tests stay green as the regression guard; `Dispatcher.handleOne` becomes a 1-line wrapper over `Route`. New tests: `TestV2Session_OpenState_EncryptedRoundTrip` (paired-device happy path through `dispatchAppFrame`, stub handler keyed by `TypeListConversations` replies via `c.Reply`, phone-side decrypt asserts inner envelope `Type`/`InReplyTo`/`Payload` match), `TestV2Session_OpenState_TamperedNoiseMsg_4421` (flip one byte of real ciphertext → exactly one outbound envelope with `CloseCode == uint16(StatusProtocolMismatch)` and nil Frame, registered handler's flag stays false, `mgr.sessions[v2TestConnID]` absent), `TestV2Session_OpenState_FreshNoiseInitAfterAEADClose` (after 4421+cleanup, second `noise_init` on same `conn_id` completes a fresh handshake; ciphertext sealed under OLD `initSend` fails against new session's `s.recv` — proof of no carry-over), `TestV2Session_OpenState_UnknownEnvelopeType_SealedUnsupportedReply` (open-state envelope with `Handlers = nil` → AEAD-sealed `Envelope{Type: TypeError, Payload.Code: CodeProtocolUnsupported}`, state stays `open`), plus dispatch unit tests `TestRoute_StandaloneInvocation` + `TestRoute_NoHandler_UnsupportedReply` pinning the new exported contract, and e2e `testV2EncryptedEchoRoundTrip` + `testV2TamperedNoiseMsg_4421` extending `TestRelayV2_Handshake` (the `v2Harness` constructor grows a `handlers map[string]dispatch.Handler` parameter — nil for existing tests; the fresh-`noise_init`-after-tampered-close case stays unit-shape only because `fakerelay` assigns a new `conn_id` per dial). Production daemon still NOT wired to the v2 manager — cutover follow-up re-wires `cmd/pyry/relay.go` to construct `V2SessionManager` instead of `Dispatcher` and registers handlers against `V2SessionConfig.Handlers`. **#454 lands the v2 control-envelope discriminator** at the top of `dispatchAppFrame`: a five-line `json.Unmarshal` probe on the AEAD-decrypted plaintext intercepts `Envelope.Type == protocol.TypeRekeyRequest` and routes it to a new `handleRekeyRequest` method, short-circuiting `dispatch.Route` so the application handler chain never sees the envelope. The probe is a re-decode (`dispatch.Route` decodes the same plaintext again); architect chose against refactoring `Route` to accept a pre-decoded `Envelope` so `internal/dispatch` stays v1-pure. JSON decode failures deliberately fall through to preserve #446's `protocol.malformed` reply path. `handleRekeyRequest` is logs-only — no `m.send`, no `m.closeWith`, no `s.state` mutation, no outbound frame: the binary is the IK responder per [ADR 024](decisions/024-noise-ik-mobile-e2e.md) and re-keys are initiated by phone-side `noise_init`, so an inbound `rekey_request` is informational. Decodes `env.Payload` as `struct{ Reason string }`; logs at INFO for the closed set `{scheduled, manual, compromise}` from `docs/protocol-mobile.md` § Re-key, WARN for empty / unknown / non-string / decode-failure (forward-compat for future mobile reasons the binary doesn't recognise yet). Three structured log fields: `event="v2.rekey.request.received"`, `conn_id`, `reason`. Architectural asymmetry "`TypeRekeyRequest` exists as a `Type*` constant but is deliberately absent from `v1TypeSet`" pinned in three structurally-different places: doc-comment on the constant ("MUST NOT add to v1TypeSet"), doc-comment **above** `v1TypeSet` ("v2-control types like TypeRekeyRequest stay out"), and a deterministic `TestTypeConstants_V1V2Partition` partition test in `compat_test.go` that asserts every `Type*` constant lives in exactly one of `v1TypeSet` / `v2OnlyTypes` (never both, never neither). The `v2OnlyTypes` allowlist literal lives test-side so production callers can't accidentally import it for dispatch logic. Tests under the `// --- v2 control-envelope tests (#454) ---` header: `TestV2Session_OpenState_RekeyRequest_Intercepted` (handler-chain `atomic.Bool` stays false; session stays open; no outbound close), `TestV2Session_OpenState_RekeyRequest_UnknownReasonTolerated` (buffer-logger captures `level=WARN`, `event=v2.rekey.request.received`, `reason=lunar-eclipse`), `TestV2Session_OpenState_RekeyRequest_RecognisedReasons` (table-driven over the three recognised reasons, asserts INFO + correct field). New `bufferLogger()` / `syncLogBuffer` test helpers (mutex-wrapped `bytes.Buffer` so the dispatch goroutine writes while the test goroutine reads). Concurrency unchanged — probe and handler both run on the manager's single dispatch goroutine. **#453 lands the v2 re-key responder swap** on open conns: the top of `handleNoiseInit` is refactored from `if s.state != V2StateAwaitingInit` into a `switch s.state` that routes the `V2StateOpen` arm to a new `handleRekeyInit` method. `handleRekeyInit` runs IK responder again against `cfg.StaticPriv` (`noise.NewResponder` → `ReadInit` → peer-static continuity check → `WriteResp` → marshal `noise_resp` → atomic CipherState swap → emit) with empty early-data on both directions per spec § Re-key (hello validation and token re-check are skipped — they ran at initial handshake; the per-rekey identity gate is the peer-static check, not the token). The peer-static continuity check is `bytes.Equal(resp.PeerStatic(), s.peerStatic)` (the field captured by #452 at initial handshake); mismatch closes the conn at WS 4426 with `reason=rekey_peer_static_mismatch`, implementing the threat-model's "no impersonation succeeds" residual-risk claim on the relay-MITM surface (`docs/protocol-mobile.md` § Threat #3). `bytes.Equal` is intentionally variable-time-acceptable (both operands are public keys); a one-line code comment names the choice to forestall a "should be `subtle.ConstantTimeCompare`" review nit. The CipherState swap is the single tuple assignment `s.send, s.recv = newSend, newRecv` on the manager's single dispatch goroutine — no half-mixed state where one direction uses new keys and the other uses old, because the loop IS the lock for `flynn/noise`'s non-goroutine-safe `CipherState`; a tuple assignment on this goroutine cannot be observed half-applied by any other code path, so the spec's atomic-switchover requirement is structural. The old `*CipherState` pointers are dropped from the struct; Go's GC reclaims the underlying memory. No explicit `Wipe()` of the key bytes is exposed (would require touching #433's surface — deferred); the single-owner-goroutine invariant means no code path reads the old state after the swap, which is the practical zeroisation property — documented in the `V2Session` and `V2SessionManager` package comments. State stays `V2StateOpen`; `s.device` and `s.peerStatic` are preserved (the #452 lifetime contract — a successful re-key MUST NOT overwrite `s.peerStatic`); log `v2.rekey.accept` at INFO with `conn_id` + `device_name` (operator-actionable; SAME device as initial handshake by construction). Five reject branches in `handleRekeyInit` (`rekey_responder_init_failed`, `rekey_read_init_failed`, `rekey_peer_static_mismatch`, `rekey_write_resp_failed`, `rekey_marshal_noise_resp`) all close at **4426** (the SAME code initial handshake uses for IK-pattern failure — no new close code introduced) via `closeWith`, which removes the session entry so the next inbound frame lazy-creates a fresh `awaitingInit`. The mismatch reject log line deliberately omits `device_name` (anti-enumeration discipline — the re-key initiator's identity is unknown / hostile). `noise_init` in `V2StateHandshakeComplete` still rejects at 4421 (the `default` arm of the `switch s.state`, with `reason=noise_init_in_handshake_complete` — the previous `reason=noise_init_after_handshake` label is dropped). **Old-key frames after the swap fail AEAD and tear down the conn via the inherited #446 tampered-frame branch** — no new code path; once `s.recv` points at the new CipherState, any frame sealed under the old keys fails `s.recv.Decrypt` and lands in `closeWith(StatusProtocolMismatch, nil)` → session removal. Tests under `// --- re-key responder tests (#453) ---`: `TestV2Session_RekeyResponder_HappyPath_RoundTripUnderNewKeys` (SAME initiator static; round-trips an application frame under the new CipherStates; asserts state stays open + device + peerStatic snapshot preserved), `TestV2Session_RekeyResponder_DifferentPeerStatic_4426` (DIFFERENT initiator static → close 4426; reject log contains `reason=rekey_peer_static_mismatch` but NOT `device_name` — security-load-bearing for Threat #3), `TestV2Session_RekeyResponder_OldKeyFrameAfterSwap_4421` (pre-rekey ciphertext fed AFTER swap → inherited #446 tampered-frame branch closes at 4421; verifies inheritance at the new authenticated-state boundary). The pre-#453 test `TestV2Session_NoiseInitAfterOpen_4421` is **removed** — it pinned exactly the behaviour this slice intentionally changes. Out of scope: binary-initiated re-key (1h timer + emit + 30s reply timeout) is #450 using the same `protocol.TypeRekeyRequest` constant landed by #454; the `pyry rekey <conn_id>` operator verb is #451; explicit `Wipe()` of old CipherState key bytes is deferred (#433 surface touch); persistence of new CipherStates across binary restarts is out for v2 per ADR 024 § Re-key policy (in-memory only). **#450 (initiator-side re-key, ships parallel with #453):** each `V2Session` that reaches `V2StateOpen` arms a per-session 1-hour `*time.Timer` (`rekeyInterval`, lowercase package var so tests substitute via `t.Cleanup` like `connection.go:43`'s `handshakeTimeout`); on fire the `time.AfterFunc` callback pushes a `wakeSignal{s, wakeRekeyEmit}` onto a per-manager buffered channel `m.wake` (cap `wakeBufferSize = 16`) instead of touching session state directly — the dispatch goroutine pops the wake on a new third select arm in `Run` and runs `emitRekeyRequest` under the single-owner-goroutine invariant for `s.send`. The emit AEAD-seals `Envelope{ID: 1, Type: protocol.TypeRekeyRequest, TS: now, Payload: {"reason": "scheduled"}}` under `s.send`, wraps as `marshalInnerFrameV2(TypeNoiseMsg, ciphertext)`, forwards via `m.send`, sets `s.awaitingRekeyReply = true`, and arms `s.rekeyReplyTimer` (`rekeyReplyTimeout = 30s`); ID=1 because there is no `rekey_ack` to correlate by InReplyTo per spec (the next AEAD round-trip under the new keys is the implicit ack); only `"scheduled"` is emitted by this slice — `"manual"` and `"compromise"` belong to the future `pyry rekey <conn_id>` verb (#451). On phone-side reply, `handleRekeyInit`'s success tail now calls a new `s.rekeyComplete(m, ctx)` hook (one line added at #453's existing line :632 success tail, unconditional on the swap path — spontaneous phone-initiated re-keys that the binary did not request still re-base the 1-hour cadence; clearing an unset `awaitingRekeyReply` is a no-op, stopping a nil `rekeyReplyTimer` is a no-op) which clears `awaitingRekeyReply`, stops `rekeyReplyTimer`, and re-arms `rekeyTimer` from the swap moment. On reply-timeout (`rekeyReplyTimer` fires and the wake-channel arm finds `awaitingRekeyReply` still true), the manager emits a `noise.rekey_failed` WARN log line with `conn_id` + `close_code=4426` (NO `err=` field — anti-leakage of flynn-noise error text per security review) and calls `closeWith(ctx, s, StatusHandshakeFailure, nil)`; the existing `closeWith` cleanup path now ALSO stops both per-session timers before the existing `delete(m.sessions, s.connID)` — `Stop()` is safe on a fired timer (returns false, no-op); the callback's `ctx.Done` arm is the load-bearing teardown. `Run` derives `runCtx, cancelRun := context.WithCancel(ctx); defer cancelRun()` and passes `runCtx` downstream so `time.AfterFunc` callback goroutines unblock cleanly on Frames-channel close (which doesn't cancel `ctx`); `Run`'s observable behaviour is unchanged (still returns `ctx.Err()`). Three new `V2Session` fields all owned by the dispatch goroutine: `rekeyTimer *time.Timer`, `rekeyReplyTimer *time.Timer`, `awaitingRekeyReply bool` — the bool is load-bearing AND distinct from `rekeyReplyTimer != nil` because `Stop()` returns false on a fired timer whose callback may still be in flight, and the bool gives `handleWake`'s timeout arm a stable late-arrival predicate to ignore stale wakes after `rekeyComplete` already won. Emit-side seal/marshal failures Warn-and-drop (three distinct events: `v2.rekey.emit.seal_failed`, `v2.rekey.emit.marshal_failed`, `v2.rekey.emit.skipped_already_awaiting`, carrying `event` + `conn_id` only — no error text), NEVER close the conn (same posture as `sealError`'s line 498-502: internal seal failures are not wire-protocol failures). No new close code introduced — reply-timeout reuses `StatusHandshakeFailure` (4426). No new exported symbols. Tests under `// --- re-key initiator tests (#450) ---`: `TestV2Session_RekeyInitiator_Emit_ReArmViaResponder` (joint AC#5 bullets 1+2 — substitute `rekeyInterval = 20ms`, drive to open, wait for first emit and decode under `sess.initRecv` asserting `Type == TypeRekeyRequest` + `reason == "scheduled"`, construct second `noise.Initiator` reusing the SAME `initPriv`, feed fresh `noise_init`, derive `initRecv2` via `initiator2.ReadResp`, wait for SECOND emit and decode under post-swap `initRecv2`; merger of bullets 1+2 because the only natural production caller of `rekeyComplete` is `handleRekeyInit` — a test-goroutine call would violate the single-owner-goroutine invariant), `TestV2Session_RekeyInitiator_ReplyTimeout_4426` (substitute `rekeyReplyTimeout = 40ms`, `bufferLogger()`, drive to open, do NOT feed reply, wait via new `waitForOutboundCount(t, rec, n, deadline)` helper, assert third envelope `CloseCode == uint16(StatusHandshakeFailure)` + nil Frame + `mgr.sessions[v2TestConnID]` absent, log contains `event=noise.rekey_failed` + `close_code=4426` + `conn_id=…`, and the `noise.rekey_failed` line specifically does NOT contain `err=` via per-line substring check), `TestV2Session_RekeyInitiator_TimerCleanup_NoGoroutineLeak` (two sub-tests `close_via_manager_exit` and `close_via_reply_timeout`, both assert `runtime.NumGoroutine()` returns to within +1 of the pre-test baseline after `runtime.Gosched(); runtime.GC(); time.Sleep(20ms)`). Tests are explicitly NOT `t.Parallel()` — they mutate package-level vars that the dispatch goroutines of other parallel tests would read. Concurrency invariant preserved: `Run` is still the only long-term goroutine the manager owns; `time.AfterFunc` callbacks are transient (spawned only when the timer fires, exit after one channel send) and NEVER touch session state directly — the wake-channel routing IS the single-owner-goroutine guarantee. Closes the last v2 re-key piece on the binary modulo the operator verb #451. |
| [noise-package.md](features/noise-package.md) | `internal/noise` (#433, extended #452) — narrow Go wrapper around `github.com/flynn/noise v1.1.0` for the `Noise_IK_25519_ChaChaPoly_BLAKE2s` cipher suite pinned by Mobile Protocol v2. Owns the suite name in exactly one source location (`var cipherSuite = flynnNoise.NewCipherSuite(DH25519, CipherChaChaPoly, HashBLAKE2s)`) — a future suite migration is a one-line edit. Eleven exports total: `KeyLen = 32` const, `ErrInvalidKeyLength` sentinel, `Responder` / `Initiator` / `CipherState` types, `NewResponder(staticPriv []byte) (*Responder, error)` / `Responder.ReadInit(initMsg []byte) ([]byte, error)` / `Responder.WriteResp(earlyData []byte) (respMsg []byte, send, recv *CipherState, err error)` (the binary side), `NewInitiator(staticPriv, peerStaticPub []byte) (*Initiator, error)` / `Initiator.WriteInit(earlyData []byte) ([]byte, error)` / `Initiator.ReadResp(respMsg []byte) (earlyData []byte, send, recv *CipherState, err error)` (the phone side — production initiator is the mobile-team's Kotlin/Swift impl, this Go side exists for full round-trip tests without a phone), `CipherState.Encrypt(plaintext []byte) ([]byte, error)` / `Decrypt(ciphertext []byte) ([]byte, error)`. No `HandshakeState` / `Config` / `HandshakePattern` / `CipherSuite` / `DHKey` re-export — flynn types do not leak through the API; a migration off `flynn/noise` touches only `noise.go`. **Empty associated-data is enforced at the type system** — `Encrypt`/`Decrypt` have no AD parameter, internally call flynn's `Encrypt(nil, nil, plaintext)` / `Decrypt(nil, nil, ciphertext)`; the v2 spec mandate (`docs/protocol-mobile.md:197` — "Implementations MUST NOT pass a non-empty AD without a corresponding spec amendment") is structurally impossible to violate without editing this package. Per-handshake key derivation + per-session nonce-counter discipline make outer-envelope binding redundant; the outer routing envelope (`conn_id`, `frame`) is plaintext and intentionally not AEAD-bound. **Constructors length-check before any flynn call** — `len(staticPriv) != KeyLen` (or `len(peerStaticPub) != KeyLen` on initiator) returns wrapped `ErrInvalidKeyLength` (`errors.Is`-matchable) with prefix naming which key tripped (`"noise: responder static key:"`, `"noise: initiator peer static pub:"`). Public-key derivation via stdlib `crypto/ecdh.X25519().NewPrivateKey(staticPriv).PublicKey().Bytes()`, not flynn — matches `internal/keys.StaticKey.PrivateKey()` byte-for-byte (same `crypto/ecdh` source). **Defensive `append([]byte(nil), key...)` copy of every key argument** into the `DHKey{Private, Public}` struct so a caller-side mutation after construction (or hypothetical future `StaticKey` zeroisation) cannot corrupt the live handshake state. `Random: rand.Reader` explicit in both `Config` literals — flynn falls back to a default when nil, but the explicit wire removes one "did the default change?" question and pins entropy source visibly. **Responder swaps cs1/cs2; initiator does not** (flynn returns `(cs1, cs2)` where cs1 carries initiator→responder traffic and cs2 carries responder→initiator traffic — asymmetric per role; the wrapper collapses to symmetric `(send, recv)` shape on both sides). Swap pinned structurally by `TestRoundTrip_BothDirections` (if `WriteResp` returned `(cs1, cs2)` instead of `(cs2, cs1)`, the very first cross-side `Decrypt` fails with a MAC error) — no comment-only assertion; the test is the load-bearing pin. Single sentinel `ErrInvalidKeyLength`; everything else (MAC failure, malformed message, out-of-order handshake call, AEAD failure, counter exhaustion, oversize message) returns bare wrapped `fmt.Errorf` because the caller (#434) closes the WS with one close code per surface (4426 handshake, 4421 transport) regardless of the underlying flynn reason — finer-grained sentinels are deferred until a caller needs to branch. **Zero `slog` calls** in the package; every error has shape `"noise: <op>: <flynn message>"` and never echoes plaintext / ciphertext / key bytes / early-data payload (pinned by `TestErrorMessages_DoNotLeakPlaintextOrKey` which encrypts a 16-byte high-entropy `crypto/rand` probe, tampers the ciphertext, asserts the resulting error string contains neither the hex nor the base64 encoding of the probe). **Not safe for concurrent use** — each `Responder` / `Initiator` / `CipherState` is owned by one goroutine (flynn's `CipherState` carries a mutable 64-bit nonce counter; concurrent access would corrupt it); the wrapper adds no locks because adding them would mask programming errors at the caller layer (#434 maintains a single dispatch loop per `conn_id` satisfying this structurally). Single production file `internal/noise/noise.go` (~215 LOC) — splitting into `responder.go` / `initiator.go` / `cipher.go` was rejected (each section is ~30 LOC, three-file overhead buys no readability). Package location `internal/noise/` chosen over `internal/encryption/noise/` (the alternative offered by the ticket body) — every internal-package occupant is one level deep; an `encryption/` intermediate for a single occupant breaks the established layout. `flynn/noise v1.1.0` is the only new direct dependency (latest stable, released Feb 2024, pure Go, no CGo, used by Tailscale's control protocol with the same cipher suite — production precedent); `golang.org/x/crypto v0.51.0` promoted from indirect to direct by `go mod tidy` (flynn imports `chacha20poly1305` + `blake2s`). Tests are same-package (white-box) with `t.Parallel()` everywhere, stdlib `testing` only, no mocks — the test "phone" is a real `Initiator` built in the same test; 11 tests cover full round-trip + both-directions transport + 32-frame loop + tampered/truncated handshake messages + wrong-responder-static rejection (test doc-comment cites the spec reconciliation: AC said "initiator's `ReadResp` errors" but the natural failure surface in IK is responder's `ReadInit` because DH outputs disagree when decrypting the encrypted-`s` field of message 1 — the responder never produces a `respMsg`) + bad-key-length matrices for both constructors + out-of-order / replayed transport frame rejection + the no-leak hygiene assertion. No production consumer in this PR — `internal/relay`'s per-`conn_id` state machine (#434) and the re-key state machine (#435) are the planned consumers. |
| [dispatch-package.md](features/dispatch-package.md) | `internal/dispatch` (#307, extended #308 with first-frame auth gate + closed-conn drop + per-conn id advance past gate `hello_ack`; #318 adds the per-conn auth slot `*dispatch.Conn.Auth() *devices.Device` populated by `runGate`'s unexported `setAuth` on the accept-and-continue branch only — handlers read the matched device snapshot instead of re-validating; first registered handler `list_conversations` landed #303 — see `internal/relay/handlers` in [relay-package.md](features/relay-package.md); #319 adds the test-fixtures-only `NewTestConn(id, outbound, auth) *Conn` constructor consumed by `internal/relay/handlers`'s sibling-package handler tests — three field assignments, `nextID=0` so the first `NextID()` returns 1, doc-comment opens with "Test fixtures only — do not call from production code", does NOT mutate auth on a dispatcher-owned `*Conn` (only builds a fresh one whose outbound channel the caller supplies); also lands the second registered handler `register_push_token` (rewrite of #250's pure handler against the `dispatch.Handler` signature, wired in `cmd/pyry/relay.go`'s `startRelay` alongside `list_conversations`)) — per-phone-conn demultiplexer + handler-table seam between `relay.Connection.Frames()` and per-envelope-type processors. Pure (`internal/protocol` only — no I/O, no transport, no `internal/relay`); carrier-agnostic via generic `<-chan protocol.RoutingEnvelope` in / `Outbound() <-chan protocol.RoutingEnvelope` out. `Handler func(ctx, *Conn, protocol.Envelope) error`; `Conn{ConnID() string, NextID() uint64 (atomic.Uint64, starts at 1), Send(ctx, env), Reply(ctx, req, respType, payload) (sets ID=NextID + InReplyTo=&req.ID + TS=time.Now().UTC())}`; `Dispatcher{New(cfg), Register(envType, h), Run(ctx) error, Outbound() <-chan}`. Concurrency: one demux goroutine (`Run`); N per-conn goroutines (one per `conn_id`, size-8 buffered input, serial `handleOne` preserving arrival order within a conn — no cross-conn ordering); shared bounded outbound (default 32, configurable via `Config.OutboundBuffer`) — slow consumer pauses producers as intended flow control; per-conn `getOrCreateConn` lookup-insert-and-`go runConn` is a single critical section under `d.mu` to prevent a two-goroutine-per-conn race. Per-frame routing in `handleOne`: malformed JSON → `protocol.malformed` (no `in_reply_to`); `payload_encrypted=true` → `protocol.unsupported`; unknown/empty `Type` → `protocol.unknown_type`; v1 type without registered handler → `protocol.unsupported`; v1 type with handler → invoked. Sentinel-to-`Code*` mapping at the dispatcher call site per `docs/PROJECT-MEMORY.md` convention; encrypted-wins-over-unknown check order inherited from `protocol.IsV1Compatible`. Error envelopes carry only `Code*` + static `Message` (no decode-error text / stack / untrusted bytes echoed). `Register`-only-before-`Run` enforced via `atomic.Bool` flipped at `Run` top; late `Register` panics (same posture as duplicate-type registration) — turns the handlers-map lock-free read into a defensible invariant. Shutdown: `ctx.Done` closes per-conn inputs + `wg.Wait` + closes `Outbound` + returns `ctx.Err`; `Frames` close → same teardown, returns nil. Daemon wiring in `cmd/pyry/relay.go` replaces #301's drain-and-discard with three goroutines (dispatcher / outbound forwarder draining `d.Outbound()` into `conn.Send` / `Wait` classifier) chained via channel-close lifecycle (`conn.Close` → `Frames` closed → `Run` exits → `Outbound` closed → forwarder exits → `Wait` returns); handler table empty in #307 (intentional `protocol.unsupported` until #303/#304/#305 register routes). Tests: empty-table-unsupported / unknown-type / encrypted-refusal / malformed-frame / id-counter-monotonic-per-conn (asserts each `conn_id` starts at 1) / reply-in-reply-to / ctx-cancel-teardown / frames-close-teardown / two-conns-arrival-order / register-duplicate-panics / register-after-run-panics / new-nil-frames|logger-panics, all under `go test -race`. New `internal/relay.Connection.Send(env protocol.RoutingEnvelope) error` (`connection.go:170-184`) — JSON-marshal + `transport.Client.Send` wrapper; returns `transport.ErrDisconnected`/`ErrNotConnected`/`ErrClosed` verbatim, frames sent while disconnected are lost (consistent with v1 reconnect semantics). Per-conn close intent on handler error / per-conn close signal from relay still deferred to future protocol-spec work. #308 plugs the auth gate in: `Config.FirstFrame FirstFrameGate` (optional) is invoked once per new `conn_id` before normal handler-table dispatch; outcomes are `{Response, CloseConn, Code, Err}` with three behaviours — accept (publish `Response`, advance per-conn id past gate's `hello_ack`=1 so the next handler reply gets id=2), reject (set `Response.CloseCode = Code`, publish, mark `connState.closed = true` under `d.mu` and exit the per-conn goroutine — demux drops subsequent frames for that `conn_id` so a send into a dead goroutine cannot block the fan-in), gate-Err (emit `protocol.malformed`, consume gate-status, conn stays open). One envelope carries both `Frame=<error>` AND `CloseCode=4401` on reject so "write error then close" is atomic on the wire (no race between two separate `conn.Send` calls). `CloseCode` is binary→relay-only — dispatcher ignores it on inbound frames (test-pinned). Daemon wiring builds `authGate(registry, serverID, logger)` in `cmd/pyry/relay.go` bridging `dispatch.FirstFrameGate` → `relay.AuthenticateFirstFrame` (token sourced from `env.Token`); registry loaded once at daemon startup from `~/.pyry/<name>/devices.json` via `devices.Load` (ENOENT → empty registry → every phone rejects; malformed JSON fails fast). Token logging forbidden at any layer. #311 adds `Dispatcher.ActiveConns() []*Conn` (broadcast-eligibility snapshot under `d.mu`, filters `gateCompleted && !closed`) consumed by `cmd/pyry`'s assistant-turn bridge to fan a `message` envelope from PTY output to every active phone conn; splits `gateRun bool` into local-only `gateStarted` (prevents re-running the gate inside `runConn`, set at the top of the gate path) and cross-goroutine `gateCompleted` (set under `d.mu` AFTER `runGate`'s `_ = c.NextID()` advance returns, OR immediately on the gate-disabled tail) so a broadcast call to `c.NextID()` cannot race the gate's literal-id=1 hello_ack and stamp two envelopes with `id=1`; regression test `TestDispatcher_ActiveConns_ExcludesPreGateConn` blocks the gate on a test channel and asserts `ActiveConns()` returns 0 mid-flight, then 1 after release — fails under any single-flag implementation that sets the flag before `runGate` runs. **#446 extends with two exported surfaces** for callers that own their own per-conn goroutine (the v2 session manager): `func Route(ctx, *slog.Logger, *Conn, map[string]Handler, json.RawMessage)` runs the same malformed/unsupported/unknown-type/no-handler error-envelope paths as `Dispatcher.handleOne` against a caller-supplied handler table and a caller-supplied `*Conn` whose `outbound` channel the caller drains, and `func NewConn(id, outbound, auth) *Conn` is the production-allowed sibling of `NewTestConn` (same body, different policy — the test-only restriction stays cleanly on `NewTestConn` so reviewers grepping for misuse in `cmd/` still get a single-word match). `Dispatcher.handleOne` becomes a 1-line wrapper over `Route`; the prior `Dispatcher.sendError` method becomes a package-private free function `sendError(ctx, logger, conn, ...)` so both `Route` and the gate path call it. Mechanical (≤10 LOC moved), preserves observable behaviour — existing dispatcher tests stay green as the regression guard. New tests pin the externally-exposed `Route` contract: `TestRoute_StandaloneInvocation` (handler reply via `c.Reply` echoed onto the caller-owned outbound channel with `InReplyTo` set to the request id) and `TestRoute_NoHandler_UnsupportedReply` (nil-handlers branch emits a sealed `protocol.unsupported` reply, no panic). The doc comment on `Route` names the synchronous-handler invariant: `Route` does not block on `conn.outbound` capacity behaviour, the caller is responsible for sizing the channel; handlers MUST be synchronous and MUST NOT spawn long-lived goroutines retaining `*Conn`. |
| [devices-package.md](features/devices-package.md) | `internal/devices` — `Device` struct (`TokenHash`, `Name`, `PairedAt`, `LastSeenAt`; snake_case JSON tags; #282 appends optional `Platform string json:"platform,omitempty"` (`"fcm"`/`"apns"`) and `PushToken string json:"push_token,omitempty"` — the on-disk counterpart of `protocol.RegisterPushTokenPayload` from #275, written by the future #250 handler; `omitempty` keeps pre-#282 disk files round-trip-clean, zero-migration change), `HashToken(plain) string` (lowercase SHA-256 hex), `VerifyToken(plain, hash) bool` (constant-time via `crypto/subtle.ConstantTimeCompare`, length-mismatch falls out via the same path); pure functions, stdlib only, foundation for Phase 3 `pyry pair` + WS-handshake auth, no consumers wired in this slice |
| [devices-registry.md](features/devices-registry.md) | `~/.pyry/<name>/devices.json` — `internal/devices.Registry` (`Load` / `Save` / `Add` / `Remove` / `List` / `FindByTokenHash` / `Validate` / `UpdatePushRegistration`); mutex-guarded slice + atomic-rename save (`{"devices": [...]}` envelope, sort by `PairedAt` then `Name` for byte-deterministic output, parent `0o700` / file `0o600`); `Save` snapshots under lock + writes outside (auth path stays unblocked, see [ADR 020](decisions/020-devices-registry-snapshot-then-write.md)); `(*Registry).Validate(plain)` (#210) is the WS-perimeter auth predicate — empty-plain early-out before lock/hash, then `HashToken` outside the lock + scan + in-memory `LastSeenAt = time.Now()` advance + snapshot under `mu`, no `Save` (caller schedules durability); `(*Registry).UpdatePushRegistration(tokenHash, platform, pushToken, name) bool` (#250) is the in-memory mutator keyed by `TokenHash` — sets `Platform` / `PushToken` / `Name` together under `r.mu` (phone is source of truth for self-reported name), returns `true` iff a row matched, caller chains `Save` for durability; consumed by `internal/relay/handlers.Handle`; empty-or-missing file → empty registry, malformed → wrapped `registry: parse` error; storage primitive for Phase 3 `pyry pair` / `pyry pair revoke` / WS-handshake auth, no consumers wired in this slice |
| [conversations-package.md](features/conversations-package.md) | `internal/conversations` — `ConversationID` typedef + `Conversation` struct (ID, Name *string, Cwd, CurrentSessionID, SessionHistory []string oldest-first, IsPromoted, LastUsedAt) with snake_case JSON tags + selective `omitempty` (#216); plus `NewID()` / `ValidID(s)` UUIDv4 generator + validator cloned byte-for-byte from `internal/sessions/id.go` (#217); foundation for the Phase 3 `conv:` series, greenfield alongside `internal/sessions` |
| [conversations-auto-archive.md](features/conversations-auto-archive.md) | `internal/conversations.ShouldArchive(c, now)` pure predicate (#219; unpromoted ≥30 days idle archives, promoted exempt; threshold const, `>=` boundary, `now` injected) + `Sweep(reg, now) int` pure iterate-and-apply primitive (#237; snapshots `reg.List()`, applies the predicate, calls `reg.Delete`, returns the count; no I/O, no `Save`) + `RunSweepLoop(ctx, reg, path, interval, log) error` ticker wrapper (#242; ticks every `interval`, calls `Sweep(reg, time.Now())`, conditionally `reg.Save(path)` only on non-zero count, INFO on success / ERROR-and-continue on Save failure, returns nil on ctx cancellation, no final on-shutdown sweep) + `SweepInterval = time.Hour` exported const + `sweepOnce` unexported single-tick body factored for tests + daemon wiring (#243): `cmd/pyry/main.go` resolves `~/.pyry/<name>/conversations.json` via `resolveConversationsRegistryPath` (sibling of `resolveRegistryPath`), calls `conversations.Load` after `tryAutoAttach` (malformed JSON wraps as `loading conversations: %w` and fails startup; missing file is benign cold start), plumbs `*Registry` + path through `sessions.Config.ConversationsRegistry` / `ConversationsRegistryPath` into matching unexported `Pool` fields; `Pool.Run` gates `g.Go(RunSweepLoop)` on `p.convReg != nil` so existing tests stay untouched + sweep-interval seam (#262): `sessions.Config.SweepInterval time.Duration` (zero = use `SweepInterval` default) plumbs through to the unexported `Pool.convSweepInterval` field consulted by `Pool.Run`; surfaced via the `cmd/pyry` `-pyry-conv-sweep-interval` flag (visible, `(testing; 0 = production default of 1h)`) for out-of-process e2e tests downstream of #251; the original `var convSweepInterval` package-level seam + `withConvSweepInterval` helper are removed (one seam, not two) |
| [conversations-registry.md](features/conversations-registry.md) | `~/.pyry/<name>/conversations.json` — `internal/conversations.Registry` (`Load` / `Save` / `Create` / `Get` / `List` / `Update` / `Delete` / `Promote`); mutex-guarded slice + atomic-rename save (`{"conversations": [...]}` envelope, sort by `LastUsedAt` then `ID` for byte-deterministic output, parent `0o700` / file `0o600`); `Save` snapshots under lock + writes outside (mirrors devices, see [ADR 020](decisions/020-devices-registry-snapshot-then-write.md)); `Update(id, fn func(*Conversation)) bool` runs `fn` with `r.mu` held + pointer to slice element (no-callback-back, no-pointer-retention contract — see [ADR 022](decisions/022-conversations-update-callback-under-lock.md)); `List(filter ...ListFilter)` variadic-for-ergonomics with `IsPromoted *bool` filter (`filter[0]` only); `Promote(id, name) error` (#218) flips `IsPromoted` and sets `Name` under `r.mu` with a same-lock uniqueness scan against other *promoted* records, four exported sentinels (`ErrConversationNotFound` / `ErrConversationAlreadyPromoted` / `ErrPromotionNameInUse` / `ErrPromotionNameEmpty`) for `errors.Is` mapping to wire codes by later CLI/wire bindings, no implicit `Save`; empty-or-missing file → empty registry, malformed → wrapped `registry: parse` error; `Delete(id) bool` (#237) is the slice-element-removal mutation primitive consumed by `Sweep` (also #237 — see [conversations-auto-archive.md](features/conversations-auto-archive.md)), no `Save` (caller's concern), order-preserving, snapshot-safe (mid-iteration `Delete` does not disturb a `List()` snapshot already in flight); storage primitive for Phase 3 auto-archive (#219/#237), no daemon-side consumers wired in this slice (CLI `pyry conv promote` + mobile `promote_conversation` frame are deferred consumers) |
| [pair-package.md](features/pair-package.md) | `internal/pair` — `Payload{Server identity.ServerID, Relay, Token, ServerStaticPubkey string}` (#432 added `ServerStaticPubkey` for Mobile Protocol v2 Noise_IK trust anchor) + `Encode(p) string` (JSON → base64url no-pad) + `Decode(s) (Payload, error)` round-trip with `ErrInvalidPayload` sentinel; rejects non-base64, non-JSON-object, trailing bytes, missing/empty fields, invalid server-id, **non-base64 pubkey, wrong-length pubkey (≠ 32 bytes)**; error strings never echo input or decoded fields (token visibility discipline). Alphabet asymmetry: outer envelope is `base64.RawURLEncoding` (URL-safe, no pad) but the `server_static_pubkey` *value* inside the JSON is `base64.StdEncoding` (padded, standard alphabet) — same string form as `internal/keys`' on-disk encoding for cross-system debugging. Plus `Render(p, w io.Writer) error` (#212, extended #432) drawing a `qrterminal.GenerateHalfBlock` QR symbol + blank line + `Encode(p)` line + **`Static-key fp: aa:bb:cc:dd:ee:ff:11:22  (verify this matches the fingerprint shown on your phone)` line** + fixed instruction line; unexported `errTrackingWriter` adapter propagates writer errors through qrterminal's no-error-return API and short-circuits subsequent writes; non-panicking `<invalid>` fallback for hand-built payloads with malformed pubkey (Decode-validated payloads cannot reach this branch). Plus `Fingerprint(pubkey [32]byte) string` (#432, exported) returning `BLAKE2s-256(pubkey)[:8]` as colon-separated lowercase hex (exactly 23 chars: `8*2 + 7`); `[32]byte` (not `[]byte`) parameter makes wrong-length input a compile error. **The 8-byte / 64-bit truncation is load-bearing security** per `docs/protocol-mobile.md` § *Security review* → *[Cryptographic primitives]* — a 32-bit fingerprint is brute-forceable (≈ 2³² preimage search ≈ a few CPU-hours); test vector hardcodes the BLAKE2s digest of 32 zero bytes (`32:0b:5e:a9:9e:65:3b:c2`) rather than recomputing at test time from the same primitive (which would be tautological). Deps: stdlib + `internal/identity` + `github.com/mdp/qrterminal/v3` + `golang.org/x/crypto/blake2s` (#432; same hash mandated by `Noise_IK_25519_ChaChaPoly_BLAKE2s` per [ADR 024](decisions/024-noise-ik-mobile-e2e.md), forward-compatible with the upcoming #433 Noise wrapper). Consumed by `cmd/pyry/pair.go`'s `runPairDefault`. |
| [pyry-pair-command.md](features/pyry-pair-command.md) | `cmd/pyry/pair.go` — `pyry pair` CLI verb family (no daemon). Bare `pyry pair [-pyry-name] [--name] [--relay]` (#213, extended #432) mints a 256-bit `crypto/rand` token, hex-encodes it, persists `Device{TokenHash, Name, PairedAt: now.UTC()}` to `~/.pyry/<name>/devices.json` (sanitized name), and renders `Payload{Server, Relay, Token, ServerStaticPubkey}` (QR + paste-fallback + fingerprint) to stdout — loads config + relay + devices.json + server-id + **static keypair (`keys.LoadOrCreate(resolveStaticKeyBaseDir(), sanitizeName(name))` after `identity.LoadOrCreate`, before `crypto/rand`; #432)** BEFORE the token draw, and `Save`s BEFORE `Render` so the plaintext token never escapes the process if any I/O fails (see [ADR 021](decisions/021-pair-cli-order-of-operations.md)); `keys.LoadOrCreate` failures (`ErrInvalidDaemonName` if `sanitizeName` produces a name `keys.validDaemonName` rejects — `PYRY_NAME=Foo` is the canonical case; `ErrInsecureKeyDirMode` / `ErrInsecureKeyFileMode` on weak perms; `ErrCorruptKeyFile` on parse failure) surface as `pyry: pair: keys: <wrapped>`; `staticKey.PublicKey() [32]byte` is base64-StdEncoded into the new `ServerStaticPubkey` field; `StaticKey.PrivateKey()` is never read by this verb (grep-enforceable); auto-name `device-<hash[:8]>` when `--name` omitted; relay precedence `--relay` → `Config.RelayURL` → `DefaultConfig().RelayURL`. `resolveStaticKeyBaseDir()` returns just `~/.pyry`; the `<sanitized-name>/static_key.json` suffix is owned by `internal/keys` (cannot bypass allowlist via caller-precomputed path). Sub-verb `pyry pair list [-pyry-name]` (#214) is read-only — `devices.Load` + `renderPairList` writes a `text/tabwriter`-padded `NAME / PAIRED / LAST SEEN / TOKEN-PREFIX` listing sorted by `(PairedAt, Name)` ascending, with `never` for zero `LastSeenAt` and the first 8 hex chars of `TokenHash` for visual ID; empty registry (file missing, zero-byte, or empty `devices` slice) prints exactly `No paired devices.\n` and exits 0; pure formatter `renderPairList(list, w io.Writer) error` is unit-testable byte-for-byte. Sub-verb `pyry pair revoke [-pyry-name] <name>` (#215) is destructive — `devices.Load` + `Registry.Remove(name)` + (only on `true`) `Registry.Save`, prints `Revoked <name>.\n` to stdout on success; not-found (incl. missing/empty registry) exits 1 via direct `os.Exit` with byte-exact `pyry pair revoke: no device named <name>\n` and leaves the on-disk file untouched; I/O errors exit 1 via wrapped returned error (`pyry: pair revoke: …`); usage errors exit 2. `runPair` is a sub-router (mirrors `runSessions` per [ADR 010](decisions/010-sessions-cli-sub-router.md), without `parseClientFlags` since pair doesn't dial the daemon); flags-leading args (`pyry pair --name=foo`) fall through to bare-pair via a `strings.HasPrefix(args[0], "-")` check; `pairVerbList` const updates in lockstep when sub-verbs land. Exit codes 0 / 1 (I/O or render error) / 2 (parse, empty relay, or unknown sub-verb). Sibling `RunBareIn(t, home, args...)` helper in `internal/e2e/harness.go` for HOME-isolated daemon-free e2e. Sub-verb `pyry pair preflight [-pyry-name]` (#436) is the v2 release gate — strictly opt-in, silent stdout, exit 0 on empty registry, exit 2 with byte-pinned stderr line `pyry pair preflight: <N> paired device(s); v2 release gate requires zero.` on non-empty, exit 1 (wrapped via main) on registry I/O / malformed JSON; 0/1/2 split mirrors `grep(1)` so CI can distinguish "check itself failed" from "gate fired"; pure verdict helper `preflightVerdict(count) (exitCode, stderrLine)` extracted so byte-exact contract is unit-testable without `os.Exit` subprocess; `pair list` table output unchanged. |
| [transport-package.md](features/transport-package.md) | `internal/transport` (#247, extended #248/#290) — long-lived WSS client to the relay: native ping/pong heartbeat (30s/30s), exponential backoff with ±20% jitter (1s/2s/4s/8s/16s/30s cap, reset to attempt 1 after ≥60s stable connection), `Connect(ctx)` blocking lifecycle, `Send([]byte)` / `Receive(ctx) []byte` channel-proxy methods, generic over frame payload (no envelope/handshake — those live in `internal/relay`); `Config{URL, Headers, WriteTimeout, Logger, FatalCloseCodes}` (ReadTimeout dropped: heartbeat is the inactivity contract; FatalCloseCodes #248 lists WS close codes that terminate the reconnect loop with `ErrFatalClose` — relay passes `{4409}`); three pump goroutines per conn (`recvPump`/`sendPump`/`pingLoop`) under a child ctx, installed BEFORE `setConn` makes the conn observable to concurrent `Send` callers (lessons.md:290 precedent); `conn.SetReadLimit(1 MiB)` as the inbound DoS cap; `1011` close-code on reconnect (`1006` is received-only); function-typed `dialFn` seam + per-Client RNG with `newClientForTest` constructor for sub-second cadence assertions; sentinels `ErrNotConnected` / `ErrClosed` / `ErrDisconnected` (#248, surfaced from `Receive`/`Send` when the live conn drops, via a per-conn `connDone` channel; pre-closed at construction so calls before `Connect` return the sentinel rather than blocking) / `ErrFatalClose` (#248); `Connected() <-chan struct{}` buffer-1-drop-on-full signal (#248) fires on every fresh conn for the relay handshake loop; `DropConn()` (#248) force-closes via `conn.CloseNow()` (abrupt 1006-equivalent) so application-layer handshake failures recycle through the dial loop's reconnect; fatal-close classification PREFERS the recvPump error among the three pump-return errors (sendPump/pingLoop return generic `use of closed network connection` while recvPump observes the `CloseError`; without preferring, 4409 classification flakes under `-race`); new dep `github.com/coder/websocket v1.8.13` (MIT, ~2k LOC, context-first; first network-protocol dep in the project) |
| [relay-package.md](features/relay-package.md) | `internal/relay` (#248, extended #249/#250/#301/#303/#307/#308/#318/#319) — binary side of the binary↔relay wire protocol. Two surfaces: (1) outbound dial with server-id handshake — wraps `internal/transport.Client` with the v1 handshake state machine: `Connect(ctx, Config{ServerID identity.ServerID, RelayURL, BinaryVersion, Logger})` validates (`wss://`-only, all fields required → `ErrInvalidConfig`) and spawns a `run` goroutine; builds `x-pyrycode-server` / `x-pyrycode-version` / `user-agent: pyry/<v>` upgrade headers; on every `transport.Client.Connected()` signal sends `Envelope{ID:1, Type:"hello", Payload: HelloServerPayload{Role:"server", ServerID, BinaryVersion, ProtocolVersions:["v1"]}}` then awaits `hello_ack` within 5s (wire-spec deadline, exposed as package `var handshakeTimeout` for test substitution); two-pass JSON decode at the trust boundary (relay-to-binary frames are ALWAYS wrapped in `RoutingEnvelope`, including handshake response with `conn_id: "-"`); `Frames() <-chan protocol.RoutingEnvelope` exposes post-handshake inbound frames in order, transparently across reconnects; `Wait() error` blocks on terminal classification (`ErrServerIDConflict` for fatal 4409 / `ctx.Err()` / wrapped transport error); `Close()` idempotent via `sync.Once`; handshake failure (timeout / wrong type / malformed JSON) logs WARN + `client.DropConn()` + lets transport's backoff recycle the conn. (2) `AuthenticateFirstFrame(env, token, reg, serverID, logger) (AuthOutcome, error)` (#249, `auth.go`) — pure per-conn token-validation predicate composing A5's `devices.Registry.Validate` with #271's `HelloAckPayload` / `ErrorPayload`. Carrier-agnostic (`token` is an explicit `string` argument; the function never parses WS headers / `env.Frame` payload / hello payload — it only reads the outer envelope's `id` to echo into `in_reply_to`); two-state semantics — `Validate` returns `(Device, bool)` and the handler folds empty / never-paired / removed-after-pair all into the same `auth.invalid_token` reject (spec § Error codes line 535 same-UX equivalence locked by `TestAuthenticateFirstFrame_RevokedTokenSameUX`); `AuthOutcome{Response protocol.RoutingEnvelope, CloseConn bool}` carries protocol-level intent and the relay-conn caller closes the phone WS with the exported `StatusUnauthorized websocket.StatusCode = 4401`; outer envelope `ID` fixed at 1; `MsgInvalidToken` const pins the spec sentence; `ErrMalformedHelloFrame` sentinel for the JSON-undecodable-frame path only (missing/empty/unknown/revoked tokens are valid `auth.invalid_token` outcomes with `nil` error); logging discipline forbids `token` everywhere and forbids `device_name` on reject to prevent name-enumeration probes from binary logs (name IS logged on accept — operator-actionable, attacker already holds a valid token); supervisor wiring + per-envelope dispatch + outbound `Send` + the relay-conn wiring that actually delivers the token to `AuthenticateFirstFrame` are deferred to consuming tickets. (3) `internal/relay/handlers` (#250, sub-package `handlers/`) — first per-envelope-type processor: `Handle(routing, *Device, *Registry, registryPath, nextID, logger) (RoutingEnvelope, error)` for `register_push_token`. Five behavioural branches: malformed-frame → `ErrMalformedFrame` sentinel (dispatcher owns the `protocol.malformed` response); unauth (`device == nil`, dispatcher bug) → `auth.invalid_token` error envelope, no registry touch; dedupe (`(payload.Platform, payload.Token, payload.DeviceName) == (device.Platform, device.PushToken, device.Name)`) → `ack` envelope, **structurally NO `UpdatePushRegistration` and NO `Save`** (the load-bearing optimisation: ~100B/conn rewrite avoided, test pins `errors.Is(os.Stat, fs.ErrNotExist)` with the file pre-state set to never-existed as the spy); gone-mid-conn (`UpdatePushRegistration` returns `false`, concurrent revoke between auth-accept and frame) → `auth.invalid_token` (same UX); save-failure → `server.binary_busy` error envelope, `Retryable=true`, `RetryAfterS=nil`, **in-memory IS mutated** (post-condition: in-memory is the runtime source of truth, next successful Save catches disk up — phone retries, dedupe will succeed on retry); happy write → `ack`. `Name` is part of the dedupe triple because the protocol's `device_name` makes the phone source-of-truth for its self-reported name (iOS Settings rename propagates). Sub-package depends on `internal/devices` + `internal/protocol` only — no import of `internal/relay`, keeping the future dispatcher cycle-free; `auth.go` stays in `internal/relay` proper because it is the gate into the dispatcher, not a per-type handler. `wrap(connID, inReplyTo, nextID, envType, payload)` is a file-local helper deliberately duplicating `relay.buildResponse` rather than importing across the sub-package boundary (premature to lift for one handler). Logging: push token NEVER logged (FCM/APNs registration id is opaque infra data, not a secret on par with the auth-time device token, but no operational signal worth the noise); `device_name` IS logged on every path that has one (write/dedupe/save-failed/gone-mid-conn) because handler runs post-auth — caller already cleared the auth gate, nothing to enumerate (inverse of #249's reject-path discipline). `msgUnauthorized` / `msgBinaryBusy` are unexported file-local consts (AC asserts `Code` + `Retryable` only, never `Message`). Tests: six flat (`first-time write` + reload assertion, `re-register identical` no-write spy, `re-register changed` content-equality not mtime to dodge CI flakes, `save-failure` via regular-file-blocks-parent `MkdirAll` failure + in-memory-still-mutated post-condition, `unauth` no-write + count-unchanged, `malformed-frame` pins sentinel). #301 adds `Config.AllowInsecureScheme bool` (test-only seam letting `ws://` pass `Connect`'s scheme check when true; env-gated via `PYRY_ALLOW_INSECURE_RELAY=1` at the cmd/pyry layer) and wires the daemon-side consumer: `cmd/pyry/main.go` + `cmd/pyry/relay.go` (new file) resolve `-pyry-relay` > `PYRY_RELAY_URL` > `cfg.RelayURL`, call `relay.Connect` against the `identity.LoadOrCreate`'d server-id, and spawn one supervisor-owned goroutine that drains `Frames()` and reads `Wait()` — `ErrServerIDConflict` cancels the daemon ctx (4409 → clean exit), other terminals log warn and exit without restart (transport already handled non-fatal closes), empty URL = disabled; #307 adds `(*Connection).Send(env protocol.RoutingEnvelope) error` (`connection.go:170-184`, JSON-marshal + `transport.Client.Send` wrapper; returns `transport.ErrDisconnected`/`ErrNotConnected`/`ErrClosed` verbatim, frames sent while disconnected are lost per v1 reconnect semantics — first consumer is `internal/dispatch`'s outbound forwarder in `cmd/pyry/relay.go`); #308 adds `(*Connection).CloseConn(connID string, code uint16) error` (`connection.go:203+`) — close-only `RoutingEnvelope{ConnID, CloseCode}` (no `Frame`) marshal + `transport.Client.Send`; fire-and-forget at this layer; the dispatcher's auth-reject path does NOT call `CloseConn` (it publishes one `Frame+CloseCode` envelope so the wire op is atomic), `CloseConn` is the surface reserved for direct callers that want close-without-payload (idle/inactivity sweep future consumer); cmd/pyry/relay.go grows the registry load (`devices.Load(resolveDevicesPath(instanceName))` between `relay.Connect` and `dispatch.New`; ENOENT → empty registry, malformed → wrapped `load device registry: %w` fails fast) and the `authGate` closure that bridges `dispatch.FirstFrameGate` → `relay.AuthenticateFirstFrame` mapping `outcome.CloseConn` to `Code: uint16(relay.StatusUnauthorized)` (4401); `AuthenticateFirstFrame`'s deferred wire-mechanism choice is settled here as option (a): the token rides `RoutingEnvelope.Token` populated by the relay from the phone's `x-pyrycode-token` header on the first frame per `conn_id`. #303 adds a second per-envelope-type processor to `internal/relay/handlers/`: `ListConversations(ConversationLister) dispatch.Handler` (`list_conversations.go`) — closure over a minimal-surface `ConversationLister` interface (`List(filter ...conversations.ListFilter) []Conversation`, declared at the handler call site so `*conversations.Registry` satisfies it structurally with no new export); reads the registry, `sort.SliceStable` by `LastUsedAt` asc / `ID` asc mirroring `Registry.Save`'s on-disk order byte-for-byte, projects each row into a `protocol.ConversationSummary` (collapsing `LastMessageTS` onto `LastUsedAt` since `Conversation` has no distinct last-message timestamp today — comment at projection site names the collapse), `json.Marshal`s `ConversationsPayload{Conversations: out}` with `make([]ConversationSummary, 0, len(list))` so empty registries serialise as `[]` not `null`, and replies via `c.Reply` so `id`/`ts`/`in_reply_to` are stamped by the dispatcher (handler never stamps them itself); `cmd/pyry/relay.go`'s `startRelay` grows a trailing `convReg *conversations.Registry` parameter (threaded from the existing `runSupervisor` load) and registers `d.Register(protocol.TypeListConversations, handlers.ListConversations(convReg))` after `dispatch.New` and before `go d.Run(ctx)`; relay-disabled early-return still fires when `relayURL == ""` (uniform signature, `convReg` accepted but unused on that path); tests use the real dispatcher as the seam (`dispatch.New` + `Register` + `Run` goroutine, frames pushed on input channel, response decoded from `d.Outbound()`), four flat tests cover empty-registry-as-empty-array / single-row-round-trip / deterministic ordering with `LastUsedAt` tie broken by `ID` / two-consecutive-requests-per-conn `in_reply_to` correctness + monotonic per-conn `id`. #319 rewrites the #250 pure handler against `dispatch.Handler`: factory `RegisterPushToken(reg *devices.Registry, registryPath string, logger *slog.Logger) dispatch.Handler` returning a closure with `func(ctx, *dispatch.Conn, protocol.Envelope) error`; `wrap()` helper and `ErrMalformedFrame` sentinel are gone (the dispatcher's `handleOne` already decoded `env`, the only remaining decode is the inner `RegisterPushTokenPayload` which now replies `protocol.malformed` via `c.Reply` — static `msgMalformed` message, decode-error text NOT echoed); the device is read via `c.Auth()` (per #318) with a nil-device defence-in-depth branch that still emits `auth.invalid_token`; `replyAck` / `replyError` are the file-local helpers that marshal the payload and call `c.Reply(ctx, env, type, payloadJSON)` so the dispatcher stamps `id`/`in_reply_to`/`ts`; registered in `cmd/pyry/relay.go`'s `startRelay` immediately after `list_conversations` with `d.Register(protocol.TypeRegisterPushToken, handlers.RegisterPushToken(registry, resolveDevicesPath(instanceName), logger))`; the new `internal/dispatch` import edge in `internal/relay/handlers` is cycle-free (`dispatch` doesn't import the sub-package). Test surface rewritten as seven flat tests (six behavioural branches + the new malformed-payload case replacing the deleted malformed-frame-sentinel test), driving a real `*dispatch.Conn` constructed via the new `dispatch.NewTestConn(testConnID, out, dev)` with `_ = c.NextID()` called once after construction so the first handler reply observes `id=2` (mirrors the gate's `hello_ack=1` accounting); new e2e `internal/e2e/register_push_token_test.go` (build tag `e2e`) pairs a device via `RunBareIn pair`, dials a `fakephone` against a `fakerelay` with `PYRY_ALLOW_INSECURE_RELAY=1`, sends `hello`→`hello_ack` (`*InReplyTo == 1`) then `register_push_token`→`ack` (`*InReplyTo == request.ID`, `env.ID >= 2` — strictly-greater leaves room for future dispatcher-side replies), and reloads `~/.pyry/test/devices.json` to assert the `(Platform, PushToken, Name)` triple is persisted. #322 adds a third handler `SendMessage(w TurnWriter, logger *slog.Logger) dispatch.Handler` in `internal/relay/handlers/send_message.go` — inbound-only slice (phone → ack or `conversation.not_found` refusal; assistant-turn delivery deferred to #311). Handler-owned `TurnWriter interface { WriteUserTurn(conversationID string, payload []byte) error }` keeps the `handlers` sub-package free of `internal/sessions` / `internal/supervisor` imports; `*sessions.Session` satisfies it structurally via a one-line `(*Session).WriteUserTurn` passthrough added in the same slice (`internal/sessions/session.go`, alongside `State()` in the delegation cluster). Four behavioural branches: pre-decode malformed → `protocol.malformed` (writer NOT reached, stub.calls==0 in tests); `WriteUserTurn` nil → `ack` (INFO log with `conversation_id` + `message_id` — phone-supplied opaque ids, not credentials); `errors.Is(err, conversations.ErrConversationNotFound)` → `conversation.not_found` error envelope `Retryable=false`; any other err → return wrapped to dispatcher (NO wire reply — load-bearing contract pinned by a 50ms drain-window assertion in `TestSendMessage_WrappedError_PassesThroughNoWireReply`). `payload.Text` is passed verbatim — no transformation, no length cap beyond transport's 1 MiB ceiling, no trailing newline appended (child's parser owns turn-framing), NEVER logged at any level. Sentinel-to-wire-code mapping at the consumer per `docs/PROJECT-MEMORY.md` convention (`internal/conversations` does NOT depend on `internal/protocol`). Writer is invoked BEFORE sentinel-mapping — validation lives upstream at `Supervisor.WriteUserTurn`'s `ValidateConversation` closure wired by `pool.go:355-362`; the not-found test pins `stub.calls == 1` to keep that discipline. Factory name `SendMessage` (mirrors `RegisterPushToken` / `ListConversations`, not the AC's literal `NewSendMessageHandler`); `replyAck` / `replyError` reused directly from `register_push_token.go:106-124` (same `handlers` package — no helpers.go extracted). Static messages `msgSendMessageMalformed = "malformed send_message payload"` + `msgConversationNotFound = "conversation not found"` — decode-error text NEVER echoed back to phone. Dispatcher registration in `cmd/pyry/relay.go` adds one line `d.Register(protocol.TypeSendMessage, handlers.SendMessage(sess, logger))` after the `register_push_token` line; `startRelay`'s signature grows a trailing `sess handlers.TurnWriter` parameter (typed as the interface so `relay.go` does not import `internal/sessions`); `cmd/pyry/main.go` reorders `runSupervisor` so `sessions.New` runs BEFORE `startRelay` and passes `pool.Default()` (the bootstrap session) — none of `sessions.New`'s inputs depend on `startRelay`'s outputs, two-line reorder, zero behavioural fallout. Test surface (4 flat `t.Parallel()` tests in `send_message_test.go`, all race-clean, no e2e in this slice — assistant-turn roundtrip is #311): success-ack with stub-captured `(conversation_id, text)` round-trip + `id==2`/`InReplyTo==requestID` shape; `conversations.ErrConversationNotFound` → error envelope + `stub.calls==1`; wrapped-err pass-through with 50ms drain-window for no-outbound; malformed payload → `protocol.malformed` + `stub.calls==0`. Helpers `newSendMsgConn` / `assertSendMsgEnvelopeShape` mirror `register_push_token_test.go`'s pattern (per-file copies, not shared — Go does not propagate `_test.go` symbols across files in the same package by convention). Two named-out-of-scope security follow-ups inherited from the protocol surface: (a) mobile-origin system-message prepend (`docs/protocol-mobile.md` threat #1, requires `Device.Name` plumbing through `TurnWriter`); (b) per-conn rate-limiting + ctx-aware `WriteUserTurn` cancel (`send_message` is the first verb to perform potentially-blocking PTY I/O; N hostile phones × 1 MiB frames pin N goroutines on backpressure; residual risk bounded by auth gate + revocation). |
| [protocol-package.md](features/protocol-package.md) | `internal/protocol` (#255, extended #271/#272/#273/#274/#275/#308/#454) — wire-format leaf package for the mobile WebSocket protocol v1: `Envelope` (id/type/ts/payload + omitempty in_reply_to/payload_encrypted; `Payload json.RawMessage` for deferred decode; `TS time.Time` so the dispatcher's clock-skew check doesn't re-parse), `RoutingEnvelope` (binary↔relay-only `{conn_id, frame}` wrapper, `Frame json.RawMessage` so the relay splices byte-for-byte without parsing), `IsV1Compatible(env) error` predicate (sentinel `ErrUnsupported` when `payload_encrypted=true`, `ErrUnknownType` when `Type` empty/unknown, `nil` otherwise; encrypted-wins-over-unknown check order pinned by test); 12 `Code*` error-code string constants + 16 `Type*` envelope-type string constants grouped by spec table order; refusal-to-wire-code mapping stays at the dispatcher call site (consumer convention pinned by `docs/PROJECT-MEMORY.md` § "Refusal-to-wire-code mapping is the consumer's job"); package-private `v1TypeSet` map literal as the predicate's O(1) lookup; deliberate triple-copy of the type list (constants + map + test slice) as three explicit drift detectors that fail loudly in CI; pure data — no I/O, no goroutines, no `context`, no `slog`; sentinel error strings carry no input bytes and no payload contents (no token leakage via error message); golden round-trip tests against `testdata/envelope_full.json` / `envelope_minimal.json` / `routing_envelope.json` pin the wire bytes (canonical `json.Compact` comparison; `time.Time.Equal` for `TS` to dodge monotonic-clock strip); first per-type payload `RegisterPushTokenPayload` (#275, `push.go`, three required `string` fields `platform`/`token`/`device_name`, pure DTO no methods, golden round-trip via `Envelope.Payload` against `testdata/register_push_token.json`) is the first slice of the #256 catalog — sibling slices land per-ticket in their own `*.go` files; second slice `messaging.go` (#272) carries `SendMessagePayload` / `MessagePayload` / `BackfillSincePayload` / `MessageChunkPayload` / `BackfillDonePayload` (five pure DTOs grouped because `MessageChunkPayload.Messages []MessagePayload` reuses the row type per spec "same shape as `message.payload`, multiple"; `BackfillSincePayload.ConversationID` is `*string` WITHOUT `omitempty` so a nil pointer marshals as literal `null` matching the spec wire shape, with a mandatory WHY comment guarding against a future `omitempty`-re-add silent breakage; `MessagePayload.Role` stays `string` not an enum type; `BackfillSincePayload.SinceTS time.Time` follows the RFC3339Nano envelope rule; golden round-trip against five `testdata/*.json` fixtures including `message_chunk.json` with 2 messages); third slice `conversations_read.go` (#273) carries `ListConversationsPayload` (empty `struct{}` for the request) / `ConversationsPayload` (slice of summaries for the response) / `ConversationSummary` (row type — `ID`, `Name *string` WITHOUT `omitempty` for the same `null`-on-wire discipline as `BackfillSincePayload.ConversationID`, `IsPromoted`, `Cwd`, `LastMessageTS time.Time`, `LastUsedAt time.Time`; declaration order matches fixture key order for byte-stable round-trip; `ConversationsPayload` does not reorder — binary owns ordering); golden round-trip against `testdata/list_conversations.json` and `testdata/conversations.json` (two rows, one `name=<string>` one `name=null`, envelope rides `in_reply_to: 3`); fourth slice `conversations_write.go` (#274) carries `CreateConversationPayload` (phone → binary, three `*T` fields all WITHOUT `omitempty` — the maximal-optionality case, with `IsPromoted *bool` proving the pointer-to-zero round-trip) / `ConversationCreatedPayload` (binary → phone reply, `{ID, IsPromoted, Cwd, Name *string, LastUsedAt time.Time}`) / `PromoteConversationPayload` (phone → binary, three required `string` fields, no optional anywhere) / `ConversationUpdatedPayload` (binary → phone broadcast, `{ID, IsPromoted, Name *string, Cwd, LastUsedAt time.Time}` — note `Name` / `Cwd` order swap vs `_created`, dictated by spec example); declaration order matches each spec example verbatim, four flat test functions in `conversations_write_test.go`, `conversation_created.json` is the only fixture in the slice carrying `in_reply_to: 4`; fifth slice handshake/control payload structs (#271; `HelloServerPayload` / `HelloClientPayload` / `HelloAckPayload` / `ErrorPayload` / `AckPayload` in `handshake.go` — pure DTOs, optional fields are `*T` + `omitempty`, two `Hello*Payload` structs over a union, `AckPayload` is `struct{}`, golden round-trip via per-type fixtures under `testdata/`); WS close codes (#247) and dispatch wiring (#248–#250) remain explicit out-of-scope. #308 extends `RoutingEnvelope` with two optional `omitempty` fields: `Token string` (phone→binary direction, first frame per `ConnID` only; populated by the relay from the phone's `x-pyrycode-token` HTTP header at WS upgrade; consumed only by `relay.AuthenticateFirstFrame`; doc-comment forbids logging at any layer) and `CloseCode uint16` (binary→relay direction only; when non-zero asks the relay to forward `Frame` (if non-empty) and then close that phone's WS with this code; dispatcher ignores on inbound — defends against a malicious relay injecting `CloseCode=4401` on a phone→binary frame); both fields omitempty so pre-#308 fixtures + wire bytes are byte-identical; `TestRoutingEnvelope_TokenOmitempty` + `TestRoutingEnvelope_CloseCodeOmitempty` pin the omit shape. **#454 adds the first Mobile Protocol v2 control-envelope type** `TypeRekeyRequest = "rekey_request"` to `codes.go` in a new "v2 control" group below the v1 `Push` group. The constant is **deliberately NOT** added to `v1TypeSet` — a leak would route the envelope to `dispatch.Route`'s handler chain. The architectural asymmetry is pinned by belt-and-suspenders rails of different fabric: (1) doc-comment on the constant ("MUST NOT add to v1TypeSet"), (2) doc-comment above `v1TypeSet` ("v2-control types like `TypeRekeyRequest` stay out"), and (3) deterministic `TestTypeConstants_V1V2Partition` in `compat_test.go` — every `Type*` constant must be in `v1TypeSet` OR in a test-local `v2OnlyTypes` allowlist; never both, never neither. The `v2OnlyTypes` literal lives test-side so production callers can't import it for dispatch logic (v2 dispatch switches on individual constants, not on partition membership). The interception happens at the v2 dispatch boundary in `internal/relay/v2session.go`'s `dispatchAppFrame` (not in this package). No v1 `Type*` constant changes; existing fixtures + drift detectors unchanged. |
| [jsonl-tail-watcher.md](features/jsonl-tail-watcher.md) | `internal/agentrun/jsonl/tail` (#349) — fsnotify watcher tying `agentrun.EncodeProjectDir` (#347) and `jsonl.Reader` (#348) together. Surface: `Config{Workdir, SessionID, HomeDir, StartOffset, OnEvent, OnEndOfTurn, Logger}` + `New(cfg) (*Watcher, error)` + `Run(ctx) error` + `Offset() int64`. `New` validates required fields, computes `dir = HomeDir + "/.claude/projects/" + agentrun.EncodeProjectDir(Workdir)`, `MkdirAll(dir, 0o700)` BEFORE `fsnotify.Add(dir)` (closes architect's "may project-dir not exist on first run?" question — mirrors `internal/sessions/rotation/watcher.go:97-107` precisely; idempotent vs. claude lazily creating the dir; no two-level watch). `Run` is single-goroutine: `defer fsw.Close()` + close any opened file, then `(1)` `os.Stat(expectedPath)` to absorb the pre-existing-file race (any CREATE that races the stat is queued in `fsw.Events` and replayed below — no double-open), `(2)` `for { select ctx.Done / fsw.Events / fsw.Errors }` matching `ev.Op.Has(fsnotify.Create)` for the first transition (`file==nil`) and `Write|Create` for further drains; CREATE-before-content race absorbed by `openWithRetry` walking `probeRetryDelays = []time.Duration{0, 50*ms, 200*ms}` (copy-paste from rotation watcher, 250ms total worst-case; ctx-aware sleep via `select case <-time.After(d)`); retry exhausted with `fs.ErrNotExist` → Warn-log + continue (later WRITE may surface); non-ENOENT open errors bail; `fsw.Errors` channel → Warn-log + continue (transient on linux/macOS, ENOSPC-like not our problem). `OnEvent` + `OnEndOfTurn` invoked synchronously from the Run goroutine — callers wanting cross-goroutine handoff send on a channel from the callback; the watcher stays single-threaded. `drain` calls `reader.Next()` in a loop: each `(Event, nil)` → `cfg.OnEvent(ev)`; on `ev.EndOfTurn` → `cfg.OnEndOfTurn()` + return `(done=true, nil)`; `io.EOF` → `(false, nil)` (file hasn't grown, wait for next fsnotify event); any other error wraps as `tail: read %s: %w` so `errors.Is(err, jsonl.ErrLineTooLarge)` chains through. Run-return cases: `nil` after OnEndOfTurn fired and returned / `ctx.Err()` on cancellation / wrapped error on unrecoverable I/O. Resume contract wired through two places: `f.Seek(StartOffset, io.SeekStart)` on open AND `jsonl.Config.StartOffset` on reader construction so `Offset()` reports absolute file positions; the watcher itself doesn't persist anything (caller responsibility, out of scope for #349). **Caller MUST call Run** for cleanup — `fsnotify.NewWatcher()` allocates in `New`, only `Run`'s `defer fsw.Close()` releases it (same contract risk + same mitigation as `rotation.Watcher`; tests enforce via `startWatcher` + `t.Cleanup` cancel + 2s exit deadline). `Offset()` safe to call only after Run returns; NOT safe concurrently with Run (doc-commented on the method). Trust boundary: same `MUST NOT log file contents at any layer` stance as the parser — logs only path / error / offset, never line bytes. Tests (6, all `t.Parallel`): `TestNew_ValidationErrors` row-table (empty Workdir / empty SessionID / nil OnEvent / nil OnEndOfTurn); `TestNew_RealpathEncoding` (darwin-only via `runtime.GOOS` skip — `t.TempDir()` under `/var/folders/...` watches `-private-var-folders-...` NOT `-var-folders-...`); `TestWatcher_LateCreate` writes synthetic 3-line JSONL (tool_use + user + end_turn) AFTER `time.Sleep(50ms)` head-start so fsnotify is armed before the file appears; `TestWatcher_ExistingFile` pre-creates the JSONL before Run starts to pin the initial-stat path; `TestWatcher_FixtureIntegration` replays `internal/agentrun/jsonl/testdata/clean.jsonl` line-by-line via `bufio.Scanner` (buffer `1<<20` initial, `16<<20` max to match the parser's `maxLineBytes`) with 3ms inter-write delay, asserts 25 events + 1 end-of-turn fire + last-event EndOfTurn=true; `TestWatcher_ContextCancellation` cancels while waiting for the file and asserts `errors.Is(err, context.Canceled)`. Test scaffolding: `eventRecorder` mutex-wrapped snapshot, `startWatcher(t, cfg)` Run-in-goroutine + `t.Cleanup` cancel + 2s exit deadline + a `wait` closure for tests that want to assert Run's return value, `waitForEndOfTurn` polls with 20ms sleep (no `time.Sleep`-then-check anti-pattern; mirrors `internal/sessions/rotation/watcher_test.go`), `writeLineByLine` uses `O_CREATE\|O_WRONLY\|O_APPEND` between writes with `Sync` after each. Subpackage import discipline: `tail` imports `jsonl` + `agentrun`; neither imports `tail` — keeps the parser fs-free + testable against `io.Reader` fixtures. Consumer: the `pyry agent-run` verb's max-turn enforcement loop + dispatcher hand-off (out of scope for #349). Out of scope: resume-offset persistence, max-turn loop, two-level watch (deferred unless empirically observed claude refusing pre-created dir — per project's "Evidence-Based Fix Selection" principle), multi-session orchestration. |
| [jsonl-reader.md](features/jsonl-reader.md) | `internal/agentrun/jsonl` (#348, extended #353) — pure stdlib JSONL line reader over claude session output. Surface: `Event{StopReason, TextChars, EndOfTurn, Raw json.RawMessage, Kind string, Usage *UsageBlock}` (#353 adds the trailing three as an additive extension for a future stream-json emitter split from #335: `Raw` = verbatim line bytes with trailing `'\n'` stripped, CRLF `'\r'` preserved, **freshly-allocated** via `make`+`copy` so subsequent `Next` calls cannot mutate caller-held bytes via backing-array reuse — pinned by `TestReader_RawByteEquivalence` which calls `Next` again before inspecting earlier `Raw` slices; `Kind` whitelisted to `"assistant"|"user"|"tool_use"|"tool_result"|"system"|"attachment"|""` — anything else / missing `type` field (incl. `{"type":"summary",…}`) collapses to `""` and bytes survive on `Raw` so downstream re-emitters can pass unrecognised kinds through byte-equivalent to `claude -p --output-format stream-json`; `Usage` non-nil **only** on assistant entries that carry a `usage` object — pointer-typed at both the inner-decode struct (`rawAssistantMessage.Usage *struct{…}` so `encoding/json` leaves it nil when absent) and the exported field (to distinguish "field absent" from "field present with all zeros"); `UsageBlock{InputTokens, OutputTokens, CacheCreationInputTokens, CacheReadInputTokens int}` mirrors the assistant `message.usage` JSON shape verbatim; non-assistant Events carry only `Raw`+`Kind` with all other fields zero/nil — existing `StopReason`/`TextChars`/`EndOfTurn` semantics for assistant entries preserved verbatim) + `Config{Logger, StartOffset}` + `NewReader(io.Reader, Config) *Reader` with `Next() (Event, error)` / `Offset() int64` / `AssistantCount() int` + sentinel `ErrLineTooLarge` (16 MiB single-line cap). #353 flipped one semantic: the previous `if raw.Type != "assistant" { continue }` is replaced by classification + early return, so every well-formed line surfaces as an Event (the only in-tree consumer `tail.Watcher` already forwarded every Event unconditionally; no `drain` code change, only test-assertion updates — `TestWatcher_LateCreate` 2→3, `TestWatcher_FixtureIntegration` 25→62 since `clean.jsonl` has 64 lines and the sole `end_turn` sits at line 62 where the watcher stops). Deterministic end-of-turn rule (Phase A spike #329, verified across 1151 real session JSONLs, zero false-positives / false-negatives): `EndOfTurn = Kind == "assistant" && StopReason == "end_turn" && TextChars > 0`. Empty-content `end_turn` entries (transitional thinking-block resolutions) are surfaced as `EndOfTurn=false` events but DO advance `AssistantCount` so sibling #349's max-turn enforcement counts every model output the operator paid for. Two-pass JSON decode (`rawLine{Type, Message json.RawMessage}` outer + `rawAssistantMessage{StopReason, Content[]{Type, Text}, Usage *struct{…}}` inner only when `Type == "assistant"`) — single-pass would fail on `user` lines whose `content` is a string not an array; non-assistant lines short-circuit before paying the inner decode. `TextChars = sum(len(c.Text) for c in Content)` — `thinking` / `tool_use` blocks have `text` absent, contribute 0 naturally, matches spec's literal `sum(len(content[i].text))`. Hand-rolled `[]byte` buffer + `scratch [4096]byte` struct field (no per-`Read` heap alloc); EOF is **not sticky** — only surfaced when `src.Read` returns `(0, io.EOF)` AND no complete line is buffered, so a later `Next` may pull more bytes from a growing `*os.File` (the standard `tail -f` contract sibling #349's fsnotify driver relies on). Resume contract: `Config.StartOffset` informational (caller must `Seek` first); `Offset()` reflects it pre-Next, advances past every consumed line's trailing `'\n'` (incl. silently-skipped non-assistant / malformed lines), never advances into a partial-line buffer. Malformed JSON line → Warn-logged (rate-limited: first + every 100th occurrence) + skipped, does NOT advance `AssistantCount`, does NOT terminate iteration; non-EOF read error wrapped `"jsonl: read at offset %d: %w"`. Trust boundary: package doc-comment carries verbatim `MUST NOT log file contents at any layer` from `internal/agentrun/trust.go:1-9` (claude session JSONL may contain user prompts, file contents, operator-pasted secrets); logs only offsets + the json package's error message string, never the source line bytes. Not safe for concurrent use — construct one Reader per source. No `context.Context` — cancellation is structural (consumer closes the underlying `*os.File`, next `Read` errors, `Next` surfaces it; the fsnotify driver owns the context). Why not `bufio.Scanner` / `bufio.Reader.ReadBytes('\n')`: Scanner has 64 KiB token cap (real lines reach ~80 KiB) AND cannot distinguish "drained mid-line, more may come" from "stream ended" — both surface as `Scan() returning false`; `ReadBytes` returns partial bytes + EOF mid-line, requiring an `io.MultiReader` re-feed dance. Hand-rolled buffer is ~15 lines and removes the dance. Fixtures (`internal/agentrun/jsonl/testdata/`): `clean.jsonl` (single `end_turn` last assistant entry, 25 entries, fires once), `double_end_turn.jsonl` (transitional 0-text `end_turn` + real `end_turn` with text, 6 entries, fires only on the second), `no_end_turn.jsonl` (max_turns-like, 20 entries observed — architect spec said 34, developer corrected from observation, same shape as #347's "ticket bodies on filesystem layout are advisory" lesson). Consumer: sibling #349 (fsnotify wrapper) wraps this reader with a `Watcher` over `~/.claude/projects/<EncodeProjectDir(workdir)>/`, calls `Next` synchronously on each Write event, persists `Offset()` as the resume point, surfaces `EndOfTurn` to the dispatcher's max-turn enforcement loop. Greenfield package — zero existing call sites, no migrations, no shims. |
| [agentrun-package.md](features/agentrun-package.md) | `internal/agentrun` — stdlib-only pre-spawn primitives for `pyry agent-run`. **Trust (#341):** `ResolveWorkdir(workdir) (string, error)` (`filepath.Abs` → `filepath.EvalSymlinks` for the macOS `/var → /private/var` rule, mirroring `internal/sessions/rotation/watcher.go`'s key shape) + `EncodeProjectDir(workdir) (string, error)` (#347 — chains `ResolveWorkdir` then applies `strings.NewReplacer("/", "-", ".", "-")` to produce the dashed directory-name segment claude uses under `~/.claude/projects/<encoded-cwd>/<sid>.jsonl`; rule covers BOTH `/` and `.` per direct observation of `~/.claude/projects/` — the #347 AC specified only `/` → `-`, architect spec amended from observation per `docs/lessons.md:53` (`/.pyrycode-worktrees/` → `--pyrycode-worktrees-`); returns `ResolveWorkdir`'s error verbatim so `errors.Is(err, fs.ErrNotExist)` chains through; no prefix, no `.jsonl` suffix — directory segment only; consumed by JSONL watcher #333) + `MarkWorkdirTrusted(homeDir, workdir) error` which sets `projects[<resolved>].hasTrustDialogAccepted = true` in `<homeDir>/.claude.json` under `syscall.Flock` on sibling `~/.claude.json.lock` (NOT the data file — atomic-rename invalidates "lock the file you're writing"; sibling-inode is stable across replaces), round-trips via `map[string]any` with `json.Decoder.UseNumber()` to preserve int64-sized values pyry does not own (e.g. claude-written tokens / counters). Pre-existing file mode preserved (or `0o600` on create); lock file always `0o600`; idempotent (byte-equal repeated calls); missing file → fresh skeleton; malformed JSON / non-object `projects` / non-object entry → wrapped error with file untouched (pinned via `bytes.Equal` pre/post). **Settings (#339):** `WriteSettings(workdir, allowed []string) (string, error)` + exported `SettingsFilename = ".pyry-agent-run-settings.json"` constant; writes `{"permissions":{"allow":[...],"defaultMode":"deny"}}` (compact via `json.NewEncoder`, trailing `\n` from `Encode`, struct field order load-bearing) atomically at `0o600` to `<workdir>/<SettingsFilename>`; nil and `[]string{}` both normalise at the entry boundary to `"allow":[]` (NOT `"allow":null` — `encoding/json` would otherwise serialise `nil` as `null`); no lock (single-producer per workdir, dispatcher-serialised); errors wrap a step name (`create temp` / `chmod temp` / `encode` / `fsync` / `close temp` / `rename`) under prefix `agentrun: write settings:`. The deny-default settings file is the load-bearing mechanism for the permission boundary — Phase A spike #329 verified `--allowedTools` is additive in interactive claude (`Bash` ran despite `--allowedTools "Read"`); the `--settings` JSON path replicates `-p` mode's native deny-default empirically. Both helpers share the canonical atomic-write recipe (`os.CreateTemp` → `Chmod` → encode → `Sync` → `Close` → `Rename`) mirrored from `internal/devices/registry.go:Save`. `homeDir` / `workdir` are explicit so tests run `t.Parallel` without `t.Setenv`. Logging discipline pinned in the package doc-comment: file contents MUST NOT be logged at any layer; error wraps name a step + path, never bytes / unmarshalled fields. Consumers: `pyry agent-run` calls `MarkWorkdirTrusted(os.UserHomeDir(), parsed.workdir)` then `WriteSettings(parsed.workdir, parsed.allowedTools)` after flag validation (mark-trust → settings order so a trust-mark failure short-circuits before any per-spawn artefact lands in the workdir; both wired in #339+#342); JSONL watcher (#333) will consume `EncodeProjectDir` (#347) for the `~/.claude/projects/<encoded-cwd>/` directory name and `ResolveWorkdir` for any `projects[...]` key comparison. **Driver (#332):** `Drive(ctx, DriveConfig) error` spawns claude via `supervisor.SpawnPTY` (one-shot `exec.CommandContext` + `pty.Start` with `cmd.Cancel = SIGTERM`, `cmd.WaitDelay = 5s`), defers `ptmx.Close`, spawns one background goroutine `io.Copy(io.Discard, ptmx)` (load-bearing — without it claude blocks at ~16-64 KiB of TUI output and `cmd.Wait` deadlocks; pinned by 256 KiB `blast` helper test), then `sleepOrCancel(TrustDialogDelay) → write "\r"` (defensive trust-dialog dismissal idempotent against #342) → `sleepOrCancel(PromptDelay) → write PromptBytes + "\r"` (single combined write matching spike byte-shape) → `cmd.Wait`. `waitAndMap` collapses to nil when `ctx.Err() != nil` (operator-driven SIGTERM is success at this verb). PTY-write failures during the sequence log WARN and proceed (the eventual `cmd.Wait` surfaces the operator-actionable child exit). Defaults: `TrustDialogDelay=2500ms`, `PromptDelay=3500ms` (spike-validated); zero-fallback fields on `DriveConfig` so tests compress to ~50ms. PTY input line discipline rewrites CR→LF on the slave (`ONLCR`/`ICRNL`) — bytes written as `"\rping\r"` arrive at the child as `"\nping\n"`; capture tests assert post-translation shape (empirical surprise — the spike's Python wrapper did the inverse translation invisibly). `PromptBytes` operator-controlled, not sanitised — control sequences flow into claude's TUI parser (same trust boundary as `--workdir`). |
| [streamrunner-package.md](features/streamrunner-package.md) | `internal/agentrun/streamrunner` (#390, consumed #391) — no-PTY sibling of `agentrun.Drive`. `Run(ctx, Config) error` spawns claude as a plain subprocess (caller-assembled argv, expected to include `--input-format stream-json --output-format stream-json --dangerously-skip-permissions`), writes one stream-json user-turn JSON envelope to stdin and closes stdin, forwards stdout/stderr to caller-supplied writers, waits for the child to exit. Envelope shape: `{"type":"user","message":{"role":"user","content":[{"type":"text","text":"<prompt>"}]}}` + `'\n'`; `PromptBytes` is **JSON-encoded** (`encoding/json`) — not shell-escaped — so embedded `"`, `\n`, `\\`, and control characters round-trip cleanly (pinned by `TestRun_StdinEnvelopeRoundTrip` with a deliberately tricky prompt). Stdin write is synchronous in the calling goroutine; `Write` / `Close` failures Warn-log-and-continue (child's exit code is the authoritative outcome — identical pattern to `drive.go`'s PTY writes). Return contract: `nil` on clean (exit 0) child termination; `nil` on ctx-cancel-driven teardown (operator shutdown is success — `ctx.Err() != nil` masks the post-`WaitDelay` signal-exit code); `*exec.ExitError` verbatim on non-zero exit not triggered by ctx cancel; wrapped `streamrunner: stdin pipe:` / `streamrunner: start:` for pre-Start setup failures; wrapped `streamrunner: marshal envelope:` only reachable if `encoding/json` rejects the input. ctx-cancel teardown delegated entirely to stdlib: `cmd.Cancel = func() error { return cmd.Process.Signal(syscall.SIGTERM) }` overrides stdlib's default SIGKILL so claude gets a graceful SIGTERM first; `cmd.WaitDelay = 5 * time.Second` (the `killGrace` constant; hardcoded per AC#2 "no timing knobs", mirrors `budget.GracePeriod` for symmetry) is stdlib's "after Cancel runs, wait this long for the process to exit, then SIGKILL and close pipes" — no package-owned goroutine, timer, or state machine. (Orthogonal to `budget.Counter`'s hand-rolled timer: that path terminates on a different signal — turn count — independent of ctx cancellation.) No `New() *Runner` constructor; pure function call shape. `Config` (required: `ClaudeBin` / `WorkDir` / `Stdout` / `Stderr`; optional: `Args` / `PromptBytes` / `Env` / `Logger`); writers MUST NOT block (stdlib forwarder blocks on writing into them while the child is still running → would deadlock `cmd.Wait`) — pinned in doc-comments. Logging discipline: Warn on stdin write/close failure (error message only); **NEVER** logs prompt bytes, the marshalled envelope bytes, or any substring of the event stream (writers are opaque `io.Writer` values; the package never sees event payload content in a parseable form — absence-asserted by construction). Dependency direction: stdlib only (`context`, `encoding/json`, `errors`, `fmt`, `io`, `log/slog`, `os`, `os/exec`, `syscall`, `time`); **does not import** `internal/supervisor` (the PTY helper) nor any sibling `internal/agentrun/*` subpackage — leaf primitive; verifiable by `go list -deps ./internal/agentrun/streamrunner/... | grep pyrycode/internal/supervisor` (expected: empty). Tests: same-package `TestStreamRunnerHelperProcess` re-exec fake claude with four `GO_STREAMRUNNER_HELPER_MODE` modes — `clean` (three-line stream-json sequence: `system init` / `assistant text` / `result success` + exit 0), `exit1` (drain stdin + exit 1), `sleep` (SIGTERM handler prints `"got SIGTERM"` to stderr + exits 0 within ~50ms; 30s timeout fallback), `echo_stdin` (copies stdin to `GO_STREAMRUNNER_HELPER_STDIN_FILE` mode 0o600 + exit 0) — drive four `t.Parallel` test cases mapping one-to-one to the architect spec's AC trace: `TestRun_CleanExit` (stdout substring checks for `"type":"system"` / `"type":"assistant"` / `"type":"result"`; stderr empty), `TestRun_NonZeroExit` (`errors.As(&exitErr)` succeeds, `ExitCode() == 1`), `TestRun_CtxCancelMidRun` (parent cancels ctx 100ms after Run starts; Run returns nil, elapsed < 6s, `"got SIGTERM"` on stderr — sanity-checks SIGTERM not SIGKILL was the trigger), `TestRun_StdinEnvelopeRoundTrip` (prompt is `"hello \"world\"\n\\backslash\x01end"`; helper writes received stdin to a tempfile; test unmarshals into a local `userTurn` mirror and asserts byte-for-byte text equality + newline-terminated envelope). Consumer (#391): `cmd/pyry/agent_run.go` assembles the full claude argv (`--input-format stream-json --output-format stream-json --verbose --dangerously-skip-permissions --allowed-tools … --model … --effort … --max-turns … --append-system-prompt-file …`), passes prompt bytes through `Config.PromptBytes`, and forwards claude's stream-json stdout byte-for-byte to the dispatcher — eliminating the 2026-05-14 `/doctor` PTY-poisoning regression by structural design rather than schema validation. Sibling primitive `agentrun.Drive` (PTY-driven, interactive bridge mode) stays as-is — split rather than overloaded so each function has a clean test surface and a clean failure-mode story. |
| [streamjson-package.md](features/streamjson-package.md) | `internal/agentrun/streamjson` (#354) — leaf package that re-emits the parsed `jsonl.Event` stream onto stdout as line-delimited stream-json (byte-equivalent to `claude -p --output-format stream-json`) and composes a single `type:"result"` trailer on close. `Emitter.Emit(ev)` writes `ev.Raw + '\n'` verbatim — no re-encoding under any circumstance — aggregates `ev.Usage` across all four `UsageBlock` int fields, counts `Kind == "assistant"` events for `num_turns`, tracks the last assistant's `StopReason`, and records whether `EndOfTurn` was observed. Sticky write error (broken pipe → subsequent `Emit`s no-op, watcher drains to EOT or ctx-cancel without thrashing). `Emit` after `Close` no-ops (returns nil) — defends against the slow-watcher-unwind race. `Close()` writes the trailer line and is idempotent (second call returns the first's error, no double-write). `SetExitReason(ExitReason)` is the pluggable setter for #334's future budget Counter integration: `Counter.Terminate` will call `SetExitReason(ExitReasonMaxTurns)` before SIGTERM so the trailer reports `subtype:"error_max_turns"` / `terminal_reason:"max_turns"`. Default classification in `Close` when `SetExitReason` was never called: EOT observed → `ExitReasonCompletion` (`subtype:"success"`, `terminal_reason:"completed"`, `is_error:false`); EOT not observed → `ExitReasonError` (`subtype:"error_during_execution"`, `terminal_reason:""`, `is_error:true`). Trailer field set was **derived from captured `claude -p` fixtures, not invented** — emits the subset the v1 dispatcher parser (`agent-dispatcher/src/dispatch.ts`) reads plus the fields the v2 fixture-compare test asserts on; both `subtype` and `terminal_reason` are emitted because v1 reads one and v2 asserts on the other. Trailer struct fields are JSON-tagged with fixed order (`type, subtype, is_error, duration_ms, num_turns, result, stop_reason, session_id, total_cost_usd, usage, terminal_reason`) so `TestCapturedFixture_ByteEquivalence` diffs cleanly. `total_cost_usd` is always `0` (dispatcher owns the rate table) and `result` is always `""` (pyry doesn't synthesise final assistant text; dispatcher logs assistant events independently — defer adding it until a consumer requires it). `usage` always emits all four keys (`input_tokens` / `output_tokens` / `cache_creation_input_tokens` / `cache_read_input_tokens`) with integer totals — never `omitempty`, since a missing key is a different wire shape than zero. Single mutex held only for one `Emit` write+state-update or one `Close` write+flag-flip; no nested locks; concurrency-safe across `Emit` (watcher goroutine), `SetExitReason` (future budget Counter goroutine), and `Close` (driver goroutine). Package doc-comment is load-bearing: MUST NOT log `Event.Raw` bytes nor per-event `Usage` values — totals and counts only. Tests: 17 in `emitter_test.go` covering raw-byte passthrough (incl. non-canonical whitespace preservation), usage aggregation across multiple assistants (incl. nil-Usage assistants), `num_turns` filtering, `stop_reason` last-wins (and `""` when no assistant seen), all three trailer compositions + the default-error fallback, idempotence of `SetExitReason` and `Close`, sticky-write-error, `Emit`-after-`Close` no-op, `cfg.Now` clock seam, `SessionID` round-trip, constant `total_cost_usd`/`result`, and the load-bearing `TestCapturedFixture_ByteEquivalence` which replays `testdata/captured_run.jsonl` (synthesised matching real captured shape) through `Emit`+`Close` and asserts non-result lines byte-equivalent + structural equality on `type`/`subtype`/`is_error`/`num_turns`/`stop_reason`/`terminal_reason` + four usage int fields, with known-expected diff list documented inline (timestamps, session ids, claude-only fields like `modelUsage`/`permission_denials`/`uuid`/`api_error_status`/`duration_api_ms`, extra usage subfields). Wire-up consumer: [pyry-agent-run-command.md](features/pyry-agent-run-command.md). |
| [budget-package.md](features/budget-package.md) | `internal/agentrun/budget` (#334) — `Counter` enforces the pyry-side `--max-turns` cap for `pyry agent-run` (interactive claude doesn't self-enforce `--max-turns` the way `claude -p` does, so pyry has to). Surface: `Reason{ReasonCompletion, ReasonMaxTurns}` (zero value `""` for ctx-cancel teardown) + `Config{MaxTurns int, Terminate func() error, Kill func() error, GracePeriod time.Duration, Logger *slog.Logger}` + `New(cfg) (*Counter, error)` + `(*Counter).OnEvent(jsonl.Event)` / `OnEndOfTurn()` / `Reason() Reason` / `Stop()`. Wire `OnEvent` / `OnEndOfTurn` to the same-named `tail.Config` callbacks; wire `Terminate` / `Kill` to `cmd.Process.Signal(syscall.SIGTERM)` / `syscall.SIGKILL` in production (tests inject `signalRecorder` stubs). Counting rule from parent #329's Phase A spike (verified across 1151 real session JSONLs): each `Kind == "assistant"` JSONL entry counts as one turn, **including** empty-content transitional `end_turn` blocks; non-assistant kinds (`user`, `tool_use`, `tool_result`, `system`, `attachment`, `""`) return immediately without incrementing — defensive filter at the top of `OnEvent` because post-#353 the upstream `tail.Watcher.OnEvent` callback fires for every `Kind`, not just assistant. `OnEvent` increments-then-branches under the lock: `EndOfTurn=true` returns (lets `OnEndOfTurn` classify completion); `fired=true` returns (idempotent — one SIGTERM per Counter); `count < MaxTurns` returns; else set `fired=true`, set `reason=ReasonMaxTurns`, arm `time.AfterFunc(GracePeriod, killAfterGrace)`, unlock, invoke `cfg.Terminate()` (Warn-log on error — grace timer is still armed so Kill follows even if Terminate fails with ESRCH). `OnEndOfTurn` is first-terminal-wins: sets `reason=ReasonCompletion` only if `reason==""`, never overwrites `ReasonMaxTurns`. Budget-boundary natural completion (the `MaxTurns`-th event itself carrying `EndOfTurn=true`) classifies as `ReasonCompletion`, not `ReasonMaxTurns` — natural `end_turn` is a stronger signal than the turn count is; one-line flip in `OnEvent` if the future stream-json emitter (split from #335) needs the inverse semantic. `Stop()` cancels the pending SIGKILL timer (captures `killTimer` under the lock, nils it, releases the lock, calls `t.Stop()`); idempotent; called by the integration ticket after `cmd.Wait` returns. `killAfterGrace` re-checks `killTimer == nil` under the lock so a `Stop` racing a firing timer leaves the system consistent; external callbacks (Terminate, Kill) always invoked unlocked to avoid deadlocking a caller that holds the mutex indirectly. `defaultGracePeriod = 5 * time.Second` copied from `supervisor.spawnWaitDelay` per the spec ("the units are independent and we should not couple the budget package to the supervisor package") — same copy-don't-import discipline as `agentrun`'s atomic-write recipe vs `internal/devices/registry.go`. Single `sync.Mutex` guards `count`, `reason`, `fired`, `killTimer`; in production `OnEvent`/`OnEndOfTurn` fire from `tail.Watcher.Run`, the grace timer fires from `time.AfterFunc`, `Stop`/`Reason` fire from the driver goroutine. No `os/exec` / `syscall` imports — Counter is a pure budget enforcer; process lifecycle stays in supervisor/driver. Tests (12, all `t.Parallel`): `TestNew_Validation` row-table (zero/negative `MaxTurns`, nil `Terminate`, nil `Kill`); `TestOnEvent_NonAssistantKindsDoNotCount` (interleaves whitelist kinds, asserts Terminate fires only at the budget-th assistant event); `TestOnEvent_SIGTERMFiresExactlyAtBudget` (not at budget-1, exactly once at budget, not again at budget+1/+2); `TestOnEvent_SIGKILLFiresAfterGrace` (Kill not called at grace/2, called once after grace, elapsed `killAt - termAt >= grace`; `grace=80ms`); `TestStop_CancelsPendingSIGKILL` (hit budget → Stop → sleep 3×grace, no Kill; second Stop is no-op); `TestStop_WithoutBudgetHit`; `TestOnEndOfTurn_ReasonCompletion` (natural end before budget); `TestOnEndOfTurn_DoesNotOverwriteMaxTurns` (budget hit, then OnEndOfTurn — `Reason` stays `ReasonMaxTurns`); `TestOnEvent_BudgetBoundaryEndOfTurnIsCompletion`; `TestReason_ZeroValueBeforeTerminalEvent`; `TestTerminateError_DoesNotBlockKill` (Terminate returns ESRCH-like error → Kill still fires after grace); `TestKillError_IsLogged` (asserts "kill failed" in slog Warn output via `syncWriter`-wrapped `strings.Builder` — slog handlers may write concurrently from `time.AfterFunc` and the test goroutine, so the buffer needs a mutex; `io.Discard` from `discardLogger()` is safe for concurrent use without wrapping). `signalRecorder` (mutex + counts + first-call timestamps for elapsed-time assertions) is the workhorse stub. Grace-period tests compress to 50-80ms; suite stays sub-second. No `Clock` interface — per evidence-based-fix-selection, defer until flakiness observed. Out of scope for #334: Drive integration (needs session-ID discovery — claude's UUID isn't known at spawn time, deliberate leaf-vs-integration split per spec), `exit_reason` on-the-wire trailer (future stream-json emitter split from #335), end-to-end real-claude/real-signal verification, two-deadline reconciliation between `cmd.WaitDelay` and Counter's grace timer (integration ticket's call). |
| [pyry-agent-run-command.md](features/pyry-agent-run-command.md) | `cmd/pyry/agent_run.go` — `pyry agent-run` verb that replaces `claude -p` in the dispatcher. Flag surface (#337) covers `--prompt-file` / `--system-prompt-file` / `--allowed-tools` / `--max-turns` / `--effort {low\|medium\|high\|xhigh\|max}` / `--model` / `--workdir` / `--output-format stream-json`, all validated at parse time with one-line errors that name the offending flag; daemon-free verb shape mirroring `runUpdate` (no `parseClientFlags`); `splitAllowedTools` pure tokeniser accepts comma- and whitespace-separated tool lists; `agentRunArgs` field names are frozen as the contract sibling tickets read against. **#391 cut over the runtime path** from the PTY-blind-typing drive (which caused the 2026-05-14 `/doctor` prompt-poisoning regression) to the stream-json subprocess pipeline. `runAgentRun(stdout io.Writer, args []string)` body collapsed from ~150 lines of errgroup orchestration to ~30 lines: `--self-check` short-circuit (#336) → `parseAgentRunArgs` → `os.ReadFile(parsed.promptFile)` → resolve `PYRY_CLAUDE_BIN` (test seam; default `"claude"`) → `signal.NotifyContext(SIGTERM, SIGINT)` → `streamrunner.Run(ctx, Config{ClaudeBin, WorkDir: parsed.workdir, Args: buildClaudeArgs(parsed), PromptBytes, Stdout: stdout, Stderr: os.Stderr})` (#390 leaf primitive) → return nil on nil or `errors.Is(err, context.Canceled)` (operator teardown), else wrap `fmt.Errorf("agent-run: %w", err)`. `buildClaudeArgs(parsed) []string` (signature shrunk from `(parsed, settingsPath, sessionID)`) emits exactly `--input-format stream-json --output-format stream-json --verbose --dangerously-skip-permissions --append-system-prompt-file <sp> --model <m> --effort <e> --max-turns <strconv.Itoa(n)> --allowed-tools <strings.Join(tools,",")>` — `--verbose` is load-bearing under stream-json mode (without it claude emits only the final `result`, no per-assistant events; spike-verified 2026-05-14); `--dangerously-skip-permissions` is acceptable because `--allowed-tools` is the authoritative tool gate under `-p`-style stream-json and the dispatcher operates inside an isolated worktree; three legacy flags (`--settings`, `--permission-mode`, `--session-id`) are negatively pinned by `TestBuildClaudeArgs_Shape` and MUST NEVER reappear. Stdout contract: **claude's stdout, byte-for-byte forwarded** — no verb-prepended `settings-file:` marker (removed #391; the `system init` event takes over its dispatcher-metadata role), no verb-composed `type:"result"` trailer (claude emits it itself). Removed from the verb's runtime path in #391 (packages stay compiled because `agent_run_selfcheck.go` (#336) still imports `internal/agentrun`; cleanup is a follow-up tracked under #375): `agentrun.WriteSettings` + `.pyry-agent-run-settings.json` (replaced by `--allowed-tools`), `agentrun.MarkWorkdirTrusted` + `~/.claude.json` mark (replaced by `--dangerously-skip-permissions`), `newSessionUUID` + `crypto/rand` (claude mints its own under stream-json), `internal/agentrun/jsonl/tail` watcher + `internal/agentrun/streamjson.Emitter` (claude emits stream-json directly), `golang.org/x/sync/errgroup`, `classifyForEmitter`. Concurrency model is now a single goroutine: `streamrunner.Run` blocks until the child exits; SIGTERM-grace-SIGKILL is delegated entirely to stdlib via `cmd.Cancel` + `cmd.WaitDelay = 5s`; the old three-actor errgroup is gone. Exit-code mapping: clean → exit 0; non-zero claude exit → wrapped `*exec.ExitError` → exit 1 (exact non-zero code preserved in the chain via `errors.As` but not propagated; AC#4's "no changes required on the dispatcher's salvage path" is the binding constraint); operator SIGTERM mid-run → ctx-cancel → streamrunner SIGTERMs the child, waits 5s, returns nil → exit 0. `parseDurationEnv` was deleted in #375 alongside the self-check rewrite (sole remaining callers were the now-removed `PYRY_AGENT_RUN_TRUST_DELAY` / `PYRY_AGENT_RUN_PROMPT_DELAY` reads in `agent_run_selfcheck.go`); the stream-json runtime path has no timing knobs. Tests (#391 rewrite): kept byte-identical `TestParseAgentRunArgs_*` / `TestSplitAllowedTools`; deleted `TestRunAgentRun_EmitsSettingsFile` / `TestRunAgentRun_MarksWorkdirTrusted` / `TestNewSessionUUID_Shape` / `writeFakeClaudeJSONL`; rewrote `TestBuildClaudeArgs_Shape` for the new argv (table-driven `slices.Equal` + named structural assertions + negative pins on the three banned legacy flags); new `TestAgentRunStreamJSONFake` re-execs the test binary as claude (gated by `PYRY_AGENT_RUN_FAKE=1`, modes `clean`/`exit1`/`sleep` keyed by `GO_AGENT_RUN_FAKE_MODE`, optional argv + stdin captures); simplified `configureFakeClaude` shell wrapper drops production argv on the floor and re-execs with `-test.run=^TestAgentRunStreamJSONFake$`; `TestRunAgentRun_StreamJSON_Clean` end-to-end asserts stream-json event order (`system init` → `assistant` → `result success`), negative pins (stdout does NOT start with `settings-file: `; neither `<workdir>/.pyry-agent-run-settings.json` nor `<HOME>/.claude.json` is written), and stdin envelope round-trip (prompt file's `"hello"` JSON-wraps into `{"type":"user","message":{"role":"user","content":[{"type":"text","text":"hello"}]}}` — confirms the verb uses `streamrunner.Run` rather than a path that bypasses the envelope); `TestRunAgentRun_StreamJSON_NonZeroExit` pins `errors.As(err, &exitErr)` + `ExitCode() == 1` + `agent-run: ` prefix. Verb-level ctx-cancel test deliberately omitted: streamrunner owns the SIGTERM grace (`TestRun_CtxCancelMidRun`); the verb's only ctx-cancel logic is one line. Production claude under `--output-format stream-json` still writes `~/.claude/projects/<encoded-cwd>/<session-id>.jsonl`; the verb no longer reads it and the fake doesn't produce it — verification deferred to manual smoke against a real `claude` (see #375). |
| [agentrun-selfcheck-package.md](features/agentrun-selfcheck-package.md) | `internal/agentrun/selfcheck` (#336, rewritten #375 for stream-json) — deterministic safety net for the per-agent tool-allowlist enforcement contract. Conceptual guard unchanged from #336 ("catch silent Claude Code regressions that would let our agents call tools we never allowed"); mechanism shifted from "spawn under `permissions.defaultMode: \"deny\"` settings file in PTY-bridged interactive mode + tail on-disk JSONL" to "spawn with `--allowed-tools \"Read\" --dangerously-skip-permissions` in stream-json mode + parse `tool_use` events directly off stdout" — the post-#391 production runtime no longer produces or consumes the per-spawn settings file. Surface narrowed: `Config{ClaudeBin, WorkDir, Prompt, Logger, OverallTimeout (90s default), Env}` (dropped `HomeDir` / `TrustDialogDelay` / `PromptDelay`) + `Result{BashInvoked, Evidence json.RawMessage, EndOfTurnObserved, AssistantCount}` (unchanged) + sentinels `ErrBashInvoked` / `ErrTimeout` (unchanged; `ErrUnknownTUI` deleted as PTY-only) + `SelfCheckDenyDefault(ctx, Config) (Result, error)`. Lifecycle: validate → defaults → compose argv `--input-format stream-json --output-format stream-json --verbose --dangerously-skip-permissions --allowed-tools Read --model sonnet --effort low --max-turns 1` (mirrors `cmd/pyry/agent_run.go:buildClaudeArgs`, omitting `--append-system-prompt-file` / `--session-id` / `--settings` which don't apply to the diagnostic verb) → `context.WithTimeout(ctx, OverallTimeout)` → `io.Pipe` bridges spawner → watcher → errgroup with two goroutines. **Spawner**: `streamrunner.Run` (#390) with `Stdout: pw`, `Stderr: io.Discard` (SECURITY: stderr structurally unable to leak into pyry logs), `PromptBytes: []byte(prompt)`; `defer pw.Close()` so the watcher unblocks on EOF. **Watcher**: `jsonl.NewReader(pr, …)` (#348, the parser sub-package — `jsonl/tail` import gone) + loop on `reader.Next()`; on `Kind == "assistant"`: increment `AssistantCount`, run `bashInvokedInRaw(ev.Raw)` (byte-identical to #336 — re-decode into `{Message: {Content: [{Type, Name}]}}` and walk content for `Type == "tool_use" && Name == "Bash"`, exact-case); on hit: `make+copy` Evidence + `cancel()`; on `ev.EndOfTurn`: set flag + `cancel()`; on `io.EOF`: return nil; `defer pr.Close()` so a stalled `pw.Write` fails fast. **Pipe-close discipline is symmetric** — both ends `defer Close`; without `defer pr.Close()`, a stalled spawner-side `pw.Write` blocks forever and `errgroup.Wait()` hangs (silent regression class). Outcome priority unchanged from #336: BashInvoked → wrapped `ErrBashInvoked` (FAIL) > EndOfTurnObserved → `(result, nil)` (PASS) > `timeoutCtx.Err() == DeadlineExceeded` → `ErrTimeout` (INCONCLUSIVE) > non-Canceled `runErr` → wrapped infra error > defensive fallthrough. **Sub-package boundary preserved**: post-#375 the helper no longer imports `tail` so the original cycle reason is gone, but the responsibility split (verify production allowlist contract vs. compose primitives) is kept; imports `cmd/pyry → selfcheck → {streamrunner, jsonl}`. **CLI wrapper** `cmd/pyry/agent_run_selfcheck.go:runAgentRunSelfCheck(stdout io.Writer) error` narrowed: `os.MkdirTemp` + `defer RemoveAll` → resolve `PYRY_CLAUDE_BIN` (test seam) → `captureClaudeVersion` (best-effort 5s `claude --version`, returns `"<unavailable>"` on any failure) → `SelfCheckDenyDefault(...)` → `errors.Is` switch rendering PASS (3 lines: marker + version + `"deny-default whitelist held: N assistant event(s)…"`), FAIL (`writeSelfCheckFailMessage` multi-line operator affordance — "What was tested" describes `--allowed-tools "Read" --dangerously-skip-permissions` argv + canned prompt; "What was observed" + verbatim Evidence trailing-`\n` trimmed; "What to check" points at `cmd/pyry/agent_run.go`'s `buildClaudeArgs` and references #329/#336/#375), INCONCLUSIVE (4 lines, advises retry-once before paging), or propagate. Dropped from #336: `agentrun.WriteSettings(workdir, []string{"Read"})` (no settings file), `os.UserHomeDir()` (no `HomeDir` field), the two `parseDurationEnv` env-knob reads (helper itself deleted from `cmd/pyry/agent_run.go` — no other callers survived). FAIL message **negative substrings** are pinned by `TestRunAgentRunSelfCheck_FAIL`: must NOT contain `permissions.defaultMode`, `.pyry-agent-run-settings.json`, `per-spawn settings file`, `PTY` — enforces the operator-string AC structurally. **CI**: `.github/workflows/self-check-daily.yml` cron `13 6 * * *` (06:13 UTC daily, off-peak) + `workflow_dispatch`; exit-code contract unchanged (`0` PASS, non-zero FAIL/inconclusive); workflow commentary still mentions `permissions.defaultMode` and is tracked as a separate ops sibling per #375 AC. Tests: helper-level `TestSelfCheck_{Pass, BashInvoked, Timeout, MalformedAssistantLineSkipped, ConfigValidation}` + `TestBashInvokedInRaw` table via `TestSelfCheckHelperProcess` stdout-fake (re-execs the test binary through a `/bin/sh` wrapper that drops production claude argv — Go test binary's flag parser would reject `--input-format` etc.); CLI-level `TestRunAgentRunSelfCheck_{PASS, FAIL}` + `TestRunAgentRun_SelfCheckShortCircuit` via `TestSelfCheckCLIFakeClaude` (CLI variant additionally short-circuits `--version` to `echo "fake-claude 0.0.0"` so `captureClaudeVersion` produces a clean line without re-execing into the stream-json fixture — the version capture is a separate process invocation from the self-check spawn, both keyed off `PYRY_CLAUDE_BIN`). Removed: `BashInvokedUnderMisformattedSettings` (no settings file to misformat). No e2e in `go test`: the daily workflow IS the e2e — fake claude proves the harness; real claude proves the contract; `internal/e2e/realclaude/allowed_tools_enforcement_test.go` (#365) is the per-PR real-claude variant of the same boundary check on the production agent-run codepath. |
| [pyry-update-command.md](features/pyry-update-command.md) | `cmd/pyry/update.go` (#189, extended #190) — user-facing `pyry update [--check] [--version <v>] [--no-restart]` CLI verb that wires the `internal/update` primitives into a single self-update flow: print current version → resolve target tag (latest or pinned) → short-circuit on `Same`/dev-build → `AssetName` + URL-template against the GitHub releases-download base → `Fetcher.FetchAsset` (tarball + checksums) → `VerifySHA256` → `ExtractBinary` → `AtomicReplace` at `os.Executable()` → (#190) probe `~/Library/LaunchAgents/dev.pyrycode.pyry.plist` + `~/.config/systemd/user/pyry.service` inline → `DetectRestartCommand` (#181, pure) → `==> Restarting daemon (<manager>: <unit>)...` + `exec.CommandContext` the launchctl/systemctl argv (skipped silently if no managed unit or `--no-restart` is set); `updateOptions` struct as the integration-test seam (httptest.NewServer + tempdir target + `probeRestart` / `runRestart` function fields); Homebrew hint when `os.Executable()` resolves under `/opt/homebrew/`; restart-failure error message foregrounds `binary replaced to <v>, but daemon restart failed: <inner>` so the user knows the new bytes landed and only the kick failed; e2e happy-path `cmd/pyry/update_e2e_test.go` (#260, build tag `(darwin || linux) && e2e_update`) drives the full chain end-to-end against an in-process fake release server (reusing `update_test.go`'s `fakeRelease`/`newFakeReleaseServer` verbatim), spawns a daemon from `<home>/bin/pyry`, calls `doUpdate` with a kill-and-respawn `runRestart` stand-in, and asserts inode-changed + PID-changed + `pyry status` Phase advances past `starting` (3s polling deadline catches the v0.10.1-shaped startup hang) + `pyry sessions list` exits 0 — release-acceptance gate without touching the operator's real launchd/systemd; extended #261 with three sibling failure-path tests (`TestUpdate_FetchFailure_E2E` HTTP 500 on asset download, `TestUpdate_VerifyFailure_E2E` 64-zero checksums body, `TestUpdate_BrokenNewBinary_E2E` tarball carrying `internal/brokenpyry`'s `os.Exit(1)`-stand-in) that pin "binary unchanged + daemon alive" on the pre-AtomicReplace paths and the no-rollback contract (`binary replaced to …` + `daemon restart failed`, version-agnostic) on the post-AtomicReplace path via a `BROKEN_PYRY_TOKEN` stderr-diagnostic guard |
